---
phase: 03.1-display-ux-fixes-inserted
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/phonology/presentation/sound_rules/word_generator_panel.dart
  - lib/features/lexicon/data/anki_exporter.dart
autonomous: true
requirements: [FIX-02, FIX-03]

must_haves:
  truths:
    - "Word generator shows romanization as primary text and [IPA] in square brackets as secondary"
    - "When no romanization mapping exists, word generator shows plain IPA only"
    - "Anki card front field has romanization on line 1 and [IPA] on line 2 separated by HTML br"
    - "No slashes appear around romanization anywhere"
  artifacts:
    - path: "lib/features/phonology/presentation/sound_rules/word_generator_panel.dart"
      provides: "Corrected word display format"
      contains: "[${word}]"
    - path: "lib/features/lexicon/data/anki_exporter.dart"
      provides: "Corrected Anki front field format"
      contains: "<br>"
  key_links:
    - from: "word_generator_panel.dart"
      to: "romanizeProvider"
      via: "romanization lookup for display"
      pattern: "romanized"
---

<objective>
Fix word display format in the word generator and Anki card front field to follow standard linguistic notation: romanization plain (no delimiters), IPA in square brackets.

Purpose: Correct notation convention — slashes are reserved for phonemic transcription, not romanization (per D-03, D-04, D-05).
Output: Word generator and Anki export produce correctly formatted output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-display-ux-fixes-inserted/03.1-CONTEXT.md
@.planning/phases/03.1-display-ux-fixes-inserted/03.1-UI-SPEC.md

<interfaces>
<!-- Current code patterns to modify -->

From lib/features/phonology/presentation/sound_rules/word_generator_panel.dart (_WordRow):
```dart
// Current: IPA primary, romanized secondary with / / slashes
ViolationText(text: word, violations: violations),  // IPA primary
if (romanized.isNotEmpty && romanized != word) ...[
  const SizedBox(width: 8),
  Text('/ $romanized /', style: ...bodySmall alpha 0.55),
],
```

From lib/features/lexicon/data/anki_exporter.dart (_insertNotes):
```dart
// Current: IPA first, romanization in brackets
final front = e.romanization != null && e.romanization!.isNotEmpty
    ? '${_htmlEscape(e.ipa)} [${_htmlEscape(e.romanization!)}]'
    : _htmlEscape(e.ipa);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix word generator display to romanization-first format</name>
  <files>lib/features/phonology/presentation/sound_rules/word_generator_panel.dart</files>
  <read_first>
    - lib/features/phonology/presentation/sound_rules/word_generator_panel.dart
    - .planning/phases/03.1-display-ux-fixes-inserted/03.1-UI-SPEC.md
  </read_first>
  <action>
Modify the `_WordRow` widget build method (around line 197-236) to swap display order per D-03 and D-04:

**Current format:** `{IPA}` primary + `/ {romanized} /` secondary
**New format:** `{romanized}` primary + `[{IPA}]` secondary

Replace the Row children in _WordRow.build():

1. When romanized is not empty AND differs from the IPA word:
   - First child: `Text(romanized, style: theme.textTheme.bodyMedium?.copyWith(color: cs.onSurface))` — romanization as primary, plain text, no delimiters, bodyMedium (13px, weight 400)
   - Second child: `const SizedBox(width: 8)` — 8px gap per UI-SPEC spacing sm
   - Third child: `ViolationText(text: '[$word]', violations: violations, style: theme.textTheme.bodySmall?.copyWith(color: cs.onSurface.withValues(alpha: 0.55)))` — IPA in square brackets as secondary, bodySmall (12px), alpha 0.55
     - Note: wrap the word in square brackets in the text string: `'[$word]'`
     - ViolationText should still apply violation underlines to the IPA portion

2. When romanized is empty OR equals the IPA word:
   - Single child: `ViolationText(text: word, violations: violations)` — plain IPA, bodyMedium, no brackets (no romanization to show)

3. Keep the violation warning icon (Icons.warning_amber_outlined, size 14, cs.error) at the end of the Row, unchanged.

4. Remove the old `'/ $romanized /'` text — no slashes anywhere.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && grep -n "/ \$romanized /" lib/features/phonology/presentation/sound_rules/word_generator_panel.dart; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - word_generator_panel.dart does NOT contain `/ $romanized /` (old slash format removed)
    - word_generator_panel.dart contains `'[$word]'` or equivalent square-bracket wrapping of the IPA word
    - word_generator_panel.dart shows romanized text BEFORE ViolationText in the Row children order (when romanization exists)
    - word_generator_panel.dart uses `bodySmall` for the IPA secondary text (was previously used for romanization)
    - word_generator_panel.dart uses `bodyMedium` for the romanization primary text
    - `flutter analyze lib/features/phonology/presentation/sound_rules/word_generator_panel.dart` reports no errors
  </acceptance_criteria>
  <done>Word generator shows `romanization [IPA]` format — romanization plain as primary, IPA in square brackets as secondary. No slashes. When no romanization, shows plain IPA only.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Anki card front field format to romanization-first with HTML br</name>
  <files>lib/features/lexicon/data/anki_exporter.dart</files>
  <read_first>
    - lib/features/lexicon/data/anki_exporter.dart
    - .planning/phases/03.1-display-ux-fixes-inserted/03.1-UI-SPEC.md
  </read_first>
  <action>
Modify the `_insertNotes` method in `AnkiExporter` (around line 229-231) to fix the front field format per D-05:

**Current format:** `'${_htmlEscape(e.ipa)} [${_htmlEscape(e.romanization!)}]'`
**New format:** `'${_htmlEscape(e.romanization!)}<br>[${_htmlEscape(e.ipa)}]'`

Replace the front field construction:

```dart
final front = e.romanization != null && e.romanization!.isNotEmpty
    ? '${_htmlEscape(e.romanization!)}<br>[${_htmlEscape(e.ipa)}]'
    : _htmlEscape(e.ipa);
```

Changes:
1. Romanization comes first (was IPA first)
2. `<br>` HTML line break between them (was space)
3. IPA wrapped in `[` `]` square brackets (was romanization wrapped)
4. Romanization has NO delimiters (was none, still none)
5. When no romanization: plain IPA only, no brackets (same as before)
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && grep -c '<br>\[' lib/features/lexicon/data/anki_exporter.dart</automated>
  </verify>
  <acceptance_criteria>
    - anki_exporter.dart contains `<br>[` in the front field construction (HTML break before bracketed IPA)
    - anki_exporter.dart front field line contains `e.romanization!` BEFORE `e.ipa` (romanization first)
    - anki_exporter.dart does NOT contain `'${_htmlEscape(e.ipa)} [${_htmlEscape(e.romanization!)}]'` (old format removed)
    - `flutter analyze lib/features/lexicon/data/anki_exporter.dart` reports no errors
  </acceptance_criteria>
  <done>Anki card front field shows romanization on line 1, [IPA] on line 2, separated by HTML br. No delimiters around romanization. When no romanization, shows plain IPA only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anki export data | User-defined lexicon data from local SQLite — HTML-escaped before insertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.1-03 | I (Information Disclosure) | Anki front field | accept | Data is user's own lexicon, exported to local .apkg file — no external disclosure |
| T-3.1-04 | T (Tampering) | HTML in Anki field | mitigate | `_htmlEscape` already applied to all user strings before HTML insertion — preserves existing mitigation |
</threat_model>

<verification>
- `flutter analyze` passes with no errors on modified files
- Word generator panel shows `romanization [IPA]` format (visual check)
- Anki export produces cards with romanization on line 1, [IPA] on line 2
</verification>

<success_criteria>
Word display format corrected in both the word generator panel and Anki card front field: romanization appears first without delimiters, IPA appears second in square brackets. No slashes used for romanization.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-display-ux-fixes-inserted/03.1-02-SUMMARY.md`
</output>
