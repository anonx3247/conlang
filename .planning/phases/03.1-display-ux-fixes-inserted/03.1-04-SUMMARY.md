---
phase: 03.1-display-ux-fixes-inserted
plan: "04"
subsystem: phonology-ui
tags: [alt-key-toggle, romanization, ipa-display, keyboard-listener]
dependency_graph:
  requires: [trapezoid-vowel-chart-inventory]
  provides: [alt-key-ipa-toggle]
  affects: [inventory_page]
tech_stack:
  - Flutter HardwareKeyboard API
  - Riverpod (romanizeProvider)
key-files:
  modified:
    - lib/features/phonology/presentation/inventory/inventory_page.dart
  created: []
---

## Summary

Alt/Option key toggle for phoneme inventory display was implemented as part of plan 03.1-01 execution, since both plans modify `inventory_page.dart`. All acceptance criteria are satisfied:

- Page-level `_isAltHeld` state with `HardwareKeyboard.instance.addHandler`/`removeHandler` lifecycle in `_InventoryPageState`
- `isAltHeld` threaded through `_AllPhonemesRow`, `_ConsonantSection`, `_ConsonantGrid`, `_VowelSection`, `_VowelChart`, `_PhonemeCell`, and `_PhonemeChip`
- `_PhonemeChip` reads `romanizeProvider` — shows romanization by default, `/{IPA}/` in slashes when Alt held
- When no romanization mapping exists, plain IPA shown without slashes (identity fallback)
- IPA reference side panel (`ipa_chart_panel.dart`) unaffected — always shows raw IPA

## Deviations

- **Implementation bundled with plan 03.1-01** — the 03.1-01 executor implemented all Alt-key toggle functionality alongside the trapezoid chart work to avoid a second pass over the same file. No code changes needed for this plan.

## Self-Check: PASSED

All must_haves verified:
- [x] Phoneme chips show romanization by default
- [x] Holding Alt/Option switches to /IPA/ in slashes
- [x] Releasing Alt returns to romanization
- [x] Toggle applies to both chips and grid cells
- [x] IPA reference panel unaffected
- [x] No-mapping fallback shows plain IPA without slashes
