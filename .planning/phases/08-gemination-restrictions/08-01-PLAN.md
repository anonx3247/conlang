---
phase: 08-gemination-restrictions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/app_database.dart
  - lib/db/app_database.g.dart
  - lib/features/phonology/domain/phonotactic_dsl.dart
  - lib/features/phonology/domain/word_generator.dart
  - lib/features/phonology/data/phonotactic_providers.dart
  - lib/features/lexicon/data/phonotactic_validation_provider.dart
  - test/phonology/gemination_test.dart
autonomous: true
requirements:
  - GEM-01
must_haves:
  truths:
    - "Gemination constraints can be stored and retrieved from the database with type=gemination"
    - "The DSL parser understands !GG notation with position suffixes"
    - "Word generator rejects words with geminate consonants when constraint is active"
    - "Phonotactic validator flags geminate violations with position-specific descriptions"
  artifacts:
    - path: "lib/features/phonology/domain/phonotactic_dsl.dart"
      provides: "GeminationConstraint data class + parseGeminationConstraint() parser"
      contains: "class GeminationConstraint"
    - path: "lib/features/phonology/domain/word_generator.dart"
      provides: "Gemination detection in validateWord and generateWords"
      contains: "gemination"
    - path: "lib/db/app_database.dart"
      provides: "Schema v14 with type column on PhonotacticConstraints"
      contains: "schemaVersion => 14"
    - path: "test/phonology/gemination_test.dart"
      provides: "Unit tests for gemination detection and DSL parsing"
      contains: "gemination"
  key_links:
    - from: "lib/features/phonology/data/phonotactic_providers.dart"
      to: "lib/features/phonology/domain/word_generator.dart"
      via: "parsedConstraintsProvider feeds gemination constraints to generator"
      pattern: "gemination"
---

<objective>
Add gemination constraint domain logic, DSL parsing, schema migration, and word generator/validator integration.

Purpose: Enable the data layer and engine to store, parse, and enforce gemination restrictions so the UI layer (Plan 02) can wire them up.
Output: Schema v14 with type column, GeminationConstraint model, !GG DSL parser, gemination-aware word generator and validator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/db/app_database.dart
@lib/features/phonology/domain/phonotactic_dsl.dart
@lib/features/phonology/domain/word_generator.dart
@lib/features/phonology/data/phonotactic_providers.dart
@lib/features/phonology/data/phonotactic_dao.dart
@lib/features/lexicon/data/phonotactic_validation_provider.dart
@test/phonotactic_dsl_smoke_test.dart

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/features/phonology/domain/phonotactic_dsl.dart:
```dart
enum ConstraintPosition { anywhere, start, end }

class ConstraintRule {
  const ConstraintRule({
    required this.pattern,
    required this.description,
    required this.isForbidden,
    required this.source,
    this.position = ConstraintPosition.anywhere,
  });
  final List<Slot> pattern;
  final String description;
  final bool isForbidden;
  final ConstraintPosition position;
  final String source;
}
```

From lib/features/phonology/domain/word_generator.dart:
```dart
class Violation {
  const Violation({required this.position, required this.length, required this.ruleDescription});
}

class ValidationResult {
  const ValidationResult({required this.violations});
  final List<Violation> violations;
  bool get isValid => violations.isEmpty;
}

class WordGenerator {
  List<String> generateWords({...constraints...});
  ValidationResult validateWord({...constraints...});
}
```

From lib/db/app_database.dart (PhonotacticConstraints table):
```dart
class PhonotacticConstraints extends Table {
  IntColumn get id => integer().autoIncrement()();
  TextColumn get pattern => text()();
  TextColumn get description => text().nullable()();
  BoolColumn get isActive => boolean().withDefault(const Constant(true))();
  TextColumn get position => text().withDefault(const Constant('anywhere'))();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema v14 + gemination DSL data model + parser</name>
  <files>
    lib/db/app_database.dart,
    lib/db/app_database.g.dart,
    lib/features/phonology/domain/phonotactic_dsl.dart,
    test/phonology/gemination_test.dart
  </files>
  <read_first>
    lib/db/app_database.dart,
    lib/features/phonology/domain/phonotactic_dsl.dart,
    lib/features/phonology/data/phonotactic_dao.dart
  </read_first>
  <behavior>
    - Test: parseGeminationConstraint('!GG') returns GeminationConstraint with positions={everywhere}
    - Test: parseGeminationConstraint('!GG/coda') returns positions={coda}
    - Test: parseGeminationConstraint('!GG/onset,initial') returns positions={onset, initial}
    - Test: parseGeminationConstraint('invalid') returns failure
    - Test: GeminationConstraint.toSource() round-trips correctly for all position combos
    - Test: serializeGeminationPositions/deserializeGeminationPositions round-trip
  </behavior>
  <action>
    1. **Schema migration (per D-04):** In `app_database.dart`:
       - Add `TextColumn get type => text().withDefault(const Constant('sequence'))();` to PhonotacticConstraints table. Values: 'sequence' (existing forbidden-sequence constraints) and 'gemination'.
       - Bump `schemaVersion` to 14.
       - Add `if (from < 14)` migration block: `await m.addColumn(phonotacticConstraints, phonotacticConstraints.type);`
       - Run `dart run build_runner build --delete-conflicting-outputs` to regenerate.

    2. **Gemination data model (per D-05, D-06):** In `phonotactic_dsl.dart`, add:
       - `enum GeminationPosition { everywhere, coda, onset, initial, final_ }` (use `final_` to avoid Dart keyword conflict, display as "final")
       - `class GeminationConstraint` with fields: `Set<GeminationPosition> positions`, `String source`
       - Static method `GeminationConstraint.toSource()` that serializes to `!GG` or `!GG/coda,onset` etc.
       - `class ParsedGeminationConstraint` (success/failure like ParsedConstraint)
       - `ParsedGeminationConstraint parseGeminationConstraint(String input)` function:
         - If input starts with `!GG`: parse optional `/pos1,pos2` suffix
         - "everywhere" is the default when no suffix
         - If "everywhere" is in the set, it is the only position (per D-03 mutual exclusion)

    3. **Serialization helpers:** Add `String serializeGeminationPositions(Set<GeminationPosition>)` and `Set<GeminationPosition> deserializeGeminationPositions(String)` for the `position` column. Store as comma-separated lowercase names. The `position` column on PhonotacticConstraints already exists as text -- for gemination constraints, store the comma-separated position set (e.g. "everywhere" or "coda,onset").

    4. **Write tests first** in `test/phonology/gemination_test.dart`, then implement.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && dart test test/phonology/gemination_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "schemaVersion => 14" lib/db/app_database.dart
    - grep -q "class GeminationConstraint" lib/features/phonology/domain/phonotactic_dsl.dart
    - grep -q "parseGeminationConstraint" lib/features/phonology/domain/phonotactic_dsl.dart
    - grep -q "GeminationPosition" lib/features/phonology/domain/phonotactic_dsl.dart
    - grep -q "from < 14" lib/db/app_database.dart
    - grep -q "gemination" test/phonology/gemination_test.dart
  </acceptance_criteria>
  <done>Schema v14 has type column on PhonotacticConstraints. GeminationConstraint model and !GG DSL parser exist with full test coverage. Round-trip serialization works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gemination detection in word generator + validation provider wiring</name>
  <files>
    lib/features/phonology/domain/word_generator.dart,
    lib/features/phonology/data/phonotactic_providers.dart,
    lib/features/lexicon/data/phonotactic_validation_provider.dart,
    test/phonology/gemination_test.dart
  </files>
  <read_first>
    lib/features/phonology/domain/word_generator.dart,
    lib/features/phonology/data/phonotactic_providers.dart,
    lib/features/lexicon/data/phonotactic_validation_provider.dart,
    lib/features/phonology/domain/phonotactic_dsl.dart
  </read_first>
  <behavior>
    - Test: WordGenerator.validateWord detects "kk" as gemination violation when everywhere constraint active
    - Test: WordGenerator.validateWord allows "kk" when only coda position restricted and "kk" is word-initial
    - Test: WordGenerator.validateWord flags "kk" at word-final position when final_ restricted
    - Test: WordGenerator.generateWords filters out words with geminates when constraint active
    - Test: No false positive on different adjacent consonants like "kt"
    - Test: Multi-character phoneme geminates detected (e.g. "t͡ʃt͡ʃ")
  </behavior>
  <action>
    1. **Extend WordGenerator (per D-06, D-07, D-08):** Add a `List<GeminationConstraint> geminationConstraints` parameter to `generateWords()` and `validateWord()`. Keep backward-compatible with default empty list.

    2. **Gemination detection algorithm:** In `validateWord()`, after existing constraint pattern matching, add gemination checking:
       - Tokenize the word (reuse existing `_tokenize`).
       - For each pair of adjacent tokens, check if both are the same IPA symbol AND both are consonants (check against `inventory.consonants`).
       - For position-based checking, determine token position context:
         - `everywhere`: flag any adjacent identical consonant pair
         - `initial`: flag geminate at token index 0-1
         - `final_`: flag geminate at last two tokens
         - `onset`: flag geminate that starts a syllable (simplified: at word start or after a vowel)
         - `coda`: flag geminate that ends a syllable (simplified: at word end or before a vowel)
       - Create `Violation` with `ruleDescription: 'gemination violation (${positionName})'` per D-08.

    3. **Wire into generateWords (per D-07):** In `generateWords()`, add gemination constraint checking alongside existing `activeConstraints` pattern checking. Reject generated words that have geminate violations.

    4. **Add `_checkGemination` private method** that takes tokens, inventory, and geminationConstraints and returns List<Violation>.

    5. **Provider wiring:** In `phonotactic_providers.dart`:
       - Add `parsedGeminationConstraintsProvider` that watches `activeConstraintsProvider`, filters for `type == 'gemination'`, parses each via `parseGeminationConstraint`, returns `List<GeminationConstraint>`.
       - Update `generatedWordsProvider` to pass gemination constraints to word generator.

    6. **Validation provider:** In `phonotactic_validation_provider.dart`, update `phonotacticValidatorProvider` to also pass gemination constraints to `validateWord()` so ViolationText throughout the app picks up gemination violations automatically.

    7. **Append tests** to `test/phonology/gemination_test.dart`.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && dart test test/phonology/gemination_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "geminationConstraints" lib/features/phonology/domain/word_generator.dart
    - grep -q "_checkGemination" lib/features/phonology/domain/word_generator.dart
    - grep -q "parsedGeminationConstraintsProvider" lib/features/phonology/data/phonotactic_providers.dart
    - grep -q "gemination" lib/features/lexicon/data/phonotactic_validation_provider.dart
    - grep -q "gemination violation" lib/features/phonology/domain/word_generator.dart
  </acceptance_criteria>
  <done>Word generator rejects words with geminate consonants when gemination constraint active. Validation provider flags geminates with position-specific tooltip text. All gemination tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User DSL input -> parser | User types constraint patterns that get parsed |
| DB stored constraints -> engine | Stored constraint strings parsed at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (Tampering) | PhonotacticConstraints.type column | accept | Low risk: local-only desktop app, no network access. Column values validated at parse time. |
| T-08-02 | D (Denial of Service) | Gemination detection loop | accept | O(n) scan over tokenized word; words are short strings (<100 chars). No amplification risk. |
</threat_model>

<verification>
1. `dart test test/phonology/gemination_test.dart` passes all gemination unit tests
2. `dart run build_runner build` succeeds (Drift codegen)
3. Schema version is 14 with type column migration
</verification>

<success_criteria>
- GeminationConstraint model and !GG DSL parser exist with tests
- Word generator rejects geminate words
- Validator flags geminates with descriptive tooltips
- Schema v14 adds type column to PhonotacticConstraints
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-gemination-restrictions/08-01-SUMMARY.md`
</output>
