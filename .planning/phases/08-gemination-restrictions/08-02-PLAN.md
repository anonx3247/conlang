---
phase: 08-gemination-restrictions
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - lib/features/phonology/presentation/sound_rules/constraint_editor.dart
  - lib/features/phonology/presentation/sound_rules/sound_rules_page.dart
  - lib/features/phonology/data/phonotactic_dao.dart
autonomous: false
requirements:
  - GEM-01
must_haves:
  truths:
    - "User can add a 'No gemination' constraint from the forbidden sequences section"
    - "User can select positions via multi-select chips (Everywhere, Coda, Onset, Word-initial, Word-final)"
    - "Gemination constraints display with type-specific rendering in the constraint list"
    - "Word generator preview respects gemination constraints"
    - "Violation highlighting flags geminate words with position tooltip"
  artifacts:
    - path: "lib/features/phonology/presentation/sound_rules/constraint_editor.dart"
      provides: "Gemination constraint add/edit dialog with position chips"
      contains: "GeminationPosition"
    - path: "lib/features/phonology/data/phonotactic_dao.dart"
      provides: "DAO methods that handle gemination type constraints"
      contains: "gemination"
  key_links:
    - from: "lib/features/phonology/presentation/sound_rules/constraint_editor.dart"
      to: "lib/features/phonology/data/phonotactic_dao.dart"
      via: "insertConstraint with type=gemination"
      pattern: "gemination"
    - from: "lib/features/phonology/presentation/sound_rules/constraint_editor.dart"
      to: "lib/features/phonology/domain/phonotactic_dsl.dart"
      via: "GeminationPosition enum for chip rendering"
      pattern: "GeminationPosition"
---

<objective>
Wire gemination constraints into the UI: add constraint type picker, gemination dialog with position chips, constraint list rendering, and end-to-end verification.

Purpose: Complete the user-facing gemination feature so users can define and manage gemination restrictions from the Sound Rules page.
Output: Working gemination constraint UI with position multi-select, integrated with word generator and violation highlighting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-gemination-restrictions/08-01-SUMMARY.md
@lib/features/phonology/presentation/sound_rules/constraint_editor.dart
@lib/features/phonology/presentation/sound_rules/sound_rules_page.dart
@lib/features/phonology/data/phonotactic_dao.dart
@lib/features/phonology/data/phonotactic_providers.dart
@lib/features/phonology/domain/phonotactic_dsl.dart
@lib/features/phonology/domain/word_generator.dart

<interfaces>
<!-- From Plan 01 output -->

From lib/features/phonology/domain/phonotactic_dsl.dart (new):
```dart
enum GeminationPosition { everywhere, coda, onset, initial, final_ }

class GeminationConstraint {
  final Set<GeminationPosition> positions;
  final String source;
  String toSource(); // serializes to !GG or !GG/coda,onset
}

ParsedGeminationConstraint parseGeminationConstraint(String input);
String serializeGeminationPositions(Set<GeminationPosition> positions);
Set<GeminationPosition> deserializeGeminationPositions(String raw);
```

From lib/db/app_database.dart (updated):
```dart
class PhonotacticConstraints extends Table {
  IntColumn get id => integer().autoIncrement()();
  TextColumn get pattern => text()();
  TextColumn get description => text().nullable()();
  BoolColumn get isActive => boolean().withDefault(const Constant(true))();
  TextColumn get position => text().withDefault(const Constant('anywhere'))();
  TextColumn get type => text().withDefault(const Constant('sequence'))();
}
```

From lib/features/phonology/presentation/sound_rules/constraint_editor.dart (existing):
```dart
class ConstraintEditor extends ConsumerWidget // displays list + add button
class _ConstraintRow extends ConsumerWidget // per-row render
class _ConstraintEditDialog extends StatefulWidget // add/edit dialog
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constraint type picker + gemination dialog with position chips</name>
  <files>
    lib/features/phonology/presentation/sound_rules/constraint_editor.dart,
    lib/features/phonology/data/phonotactic_dao.dart
  </files>
  <read_first>
    lib/features/phonology/presentation/sound_rules/constraint_editor.dart,
    lib/features/phonology/data/phonotactic_dao.dart,
    lib/features/phonology/data/phonotactic_providers.dart,
    lib/features/phonology/domain/phonotactic_dsl.dart
  </read_first>
  <action>
    1. **Add constraint type picker (per D-01, D-02):** In `ConstraintEditor._showAddDialog()`, instead of directly opening `_ConstraintEditDialog`, show a small dialog or bottom sheet that asks "What type of constraint?" with two options:
       - "Forbidden sequence" (existing behavior) -> opens `_ConstraintEditDialog`
       - "No gemination" -> opens new `_GeminationEditDialog`
       Use a simple `SimpleDialog` with two `SimpleDialogOption` items for clean UX.

    2. **Create `_GeminationEditDialog` (per D-03):** A new `StatefulWidget` inside `constraint_editor.dart`:
       - Title: "Add gemination restriction" (or "Edit gemination restriction" if editing)
       - Position selector: `Wrap` of `FilterChip` widgets for each `GeminationPosition`:
         - Labels: "Everywhere", "Coda", "Onset", "Word-initial", "Word-final"
         - State: `Set<GeminationPosition>` initialized to `{GeminationPosition.everywhere}`
         - Mutual exclusion per D-03: selecting "Everywhere" deselects all others; selecting any specific position deselects "Everywhere". If last specific position is deselected, auto-select "Everywhere".
       - Description field (optional, pre-filled with "No geminate consonants")
       - Save button: constructs the `!GG` DSL source string via `GeminationConstraint.toSource()`, serializes positions via `serializeGeminationPositions()`, and inserts via DAO with `type: 'gemination'`.

    3. **Update `_ConstraintRow` rendering (per D-01):** Detect `constraint.type == 'gemination'` and render differently:
       - Icon: `Icons.block` (solid) instead of `Icons.block_outlined`
       - Pattern display: show human-readable "No gemination" + position chips instead of raw DSL
       - Parse gemination positions from the `position` column via `deserializeGeminationPositions()`
       - Show position chips as small `Chip` widgets below the label
       - Edit button opens `_GeminationEditDialog` (not `_ConstraintEditDialog`)

    4. **DAO awareness:** The existing `PhonotacticDao.insertConstraint` and `updateConstraint` already accept the full companion/row. No DAO changes needed -- the `type` column is set via `PhonotacticConstraintsCompanion.insert(pattern: dslSource, type: Value('gemination'), position: Value(serializedPositions))`. But add a convenience method `insertGeminationConstraint({required String dslPattern, required String positions, String? description})` in `PhonotacticDao` for clarity.

    5. **Prevent duplicate gemination constraints:** Before saving a new gemination constraint, check if one already exists (query count where type='gemination'). If so, show a snackbar "A gemination constraint already exists. Edit the existing one." and don't save.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && dart analyze --fatal-infos lib/features/phonology/presentation/sound_rules/constraint_editor.dart lib/features/phonology/data/phonotactic_dao.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "GeminationEditDialog\|_GeminationEditDialog" lib/features/phonology/presentation/sound_rules/constraint_editor.dart
    - grep -q "GeminationPosition" lib/features/phonology/presentation/sound_rules/constraint_editor.dart
    - grep -q "FilterChip" lib/features/phonology/presentation/sound_rules/constraint_editor.dart
    - grep -q "gemination" lib/features/phonology/presentation/sound_rules/constraint_editor.dart
    - grep -q "No gemination" lib/features/phonology/presentation/sound_rules/constraint_editor.dart
  </acceptance_criteria>
  <done>Users can add a "No gemination" constraint type from the constraint editor, with FilterChip position picker supporting mutual exclusion. Gemination rows render with human-readable labels and position chips. Editing works for both constraint types.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end gemination verification</name>
  <files>none (verification only)</files>
  <action>
    Human verifies the complete gemination restriction feature end-to-end.

    Steps:
    1. Open an existing project (or create new).
    2. Go to Phonology > Sound Rules.
    3. In the "Forbidden Sequences" section, click the "+" button.
    4. Verify a type picker appears with "Forbidden sequence" and "No gemination" options.
    5. Select "No gemination". Verify a dialog appears with position chips: Everywhere, Coda, Onset, Word-initial, Word-final.
    6. Verify "Everywhere" is selected by default. Click "Coda" -- verify "Everywhere" deselects. Click "Everywhere" -- verify "Coda" deselects.
    7. Leave "Everywhere" selected and save.
    8. Verify the constraint appears in the list with a recognizable gemination icon and human-readable label.
    9. Check the word generator preview -- words with geminate consonants (like "kka" or "tta") should not appear.
    10. Go to Lexicon, add a word with a geminate (e.g. IPA "akka"). Verify it shows red violation highlighting with tooltip "gemination violation (everywhere)".
    11. Edit the gemination constraint -- change position to "Coda" only. Save.
    12. Verify the word generator now allows some geminates (only filtering coda-position ones).
    13. Toggle the constraint active/inactive via the switch. Verify word generator responds.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && dart test test/phonology/gemination_test.dart && dart analyze --fatal-infos lib/features/phonology/</automated>
  </verify>
  <done>User confirms gemination constraints work end-to-end: adding, editing, position selection, word generator filtering, and violation highlighting all function correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI chip selection -> DB storage | Position chips serialized to text column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | T (Tampering) | Position chip state | accept | Desktop app, no network. Serialization validated at parse time. |
</threat_model>

<verification>
1. `dart analyze` passes on modified files
2. Gemination constraint can be added via UI with position selection
3. Word generator filters geminate words
4. Violation highlighting shows gemination violations
5. Position mutual exclusion works (Everywhere vs specific)
</verification>

<success_criteria>
- "No gemination" appears as a constraint type option in the add flow
- Position chips with mutual exclusion work correctly
- Gemination constraints render with human-readable labels in the list
- Word generator preview excludes geminate words
- Violation highlighting flags geminates throughout the app
- Editing and toggling gemination constraints works
</success_criteria>

<output>
After completion, create `.planning/phases/08-gemination-restrictions/08-02-SUMMARY.md`
</output>
