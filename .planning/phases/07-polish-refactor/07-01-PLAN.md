---
phase: 07-polish-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/phonology/presentation/phonology_shell.dart
  - lib/features/phonology/presentation/inventory/inventory_page.dart
  - lib/features/phonology/presentation/inventory/natural_classes_page.dart
  - lib/router/app_router.dart
autonomous: true
requirements: [NIT-01, NIT-02, NIT-03]
must_haves:
  truths:
    - "Phonology sidebar shows 3 tabs: Inventory, Natural Classes, Sound Rules"
    - "Inventory page shows consonant grid, vowel chart, then romanization section below — no natural classes section"
    - "Natural Classes has its own dedicated page accessible from sidebar"
    - "Phoneme chips display romanization as primary text with /IPA/ in muted text to the right — only when they differ"
    - "Alt-key handler is completely removed from inventory_page.dart — no HardwareKeyboard listener, no _isAltHeld state"
    - "Romanization section is no longer a separate sub-tab — it renders below the vowel chart on the Inventory page"
  artifacts:
    - path: "lib/features/phonology/presentation/inventory/natural_classes_page.dart"
      provides: "Dedicated natural classes page"
      min_lines: 30
    - path: "lib/features/phonology/presentation/phonology_shell.dart"
      provides: "3-tab sidebar navigation"
      contains: "Natural Classes"
    - path: "lib/features/phonology/presentation/inventory/inventory_page.dart"
      provides: "Inventory page without natural classes, with inline romanization"
  key_links:
    - from: "lib/router/app_router.dart"
      to: "natural_classes_page.dart"
      via: "GoRoute for /phonology/natural-classes"
      pattern: "natural.classes"
    - from: "lib/features/phonology/presentation/phonology_shell.dart"
      to: "/phonology/natural-classes"
      via: "sidebar item"
      pattern: "Natural Classes"
---

<objective>
Restructure the Phonology tab: extract natural classes to their own sub-tab, move romanization inline below inventory grids, and replace the alt-key phoneme display with always-visible inline rom+IPA.

Purpose: Clean up phonology tab organization per user decisions D-01 through D-04. Natural classes are currently buried inside the inventory page; romanization is a separate sub-tab but should be inline; alt-key toggle is being replaced by persistent display.
Output: Phonology tab with 3 sidebar entries (Inventory, Natural Classes, Sound Rules), inline romanization on inventory, and rom-primary phoneme display.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/features/phonology/presentation/phonology_shell.dart
@lib/features/phonology/presentation/inventory/inventory_page.dart
@lib/features/phonology/presentation/inventory/natural_class_editor.dart
@lib/features/phonology/presentation/inventory/romanization_section.dart
@lib/router/app_router.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract natural classes to own page and restructure phonology tabs (D-01, D-02)</name>
  <files>
    lib/features/phonology/presentation/inventory/natural_classes_page.dart
    lib/features/phonology/presentation/inventory/inventory_page.dart
    lib/features/phonology/presentation/phonology_shell.dart
    lib/router/app_router.dart
  </files>
  <read_first>
    lib/features/phonology/presentation/phonology_shell.dart
    lib/features/phonology/presentation/inventory/inventory_page.dart
    lib/features/phonology/presentation/inventory/natural_class_editor.dart
    lib/features/phonology/presentation/inventory/romanization_section.dart
    lib/router/app_router.dart
  </read_first>
  <action>
1. Create `natural_classes_page.dart` as a new ConsumerWidget page:
   - Move the `_NaturalClassesSection` widget (currently at line ~684 in inventory_page.dart) into this new file as the main content
   - Include the full natural class editor functionality (default classes display, user-defined class CRUD, the NaturalClassEditor dialog launcher)
   - Import `natural_class_editor.dart` and `default_natural_classes.dart` as the existing inventory page does
   - Wrap in a SingleChildScrollView with padding matching the inventory page style

2. Update `inventory_page.dart`:
   - Remove the `_NaturalClassesSection` widget class and its reference from the build method (around line 198)
   - Move the `RomanizationSection()` widget (currently imported from `romanization_section.dart`) to render BELOW the vowel chart section, inside the same `SingleChildScrollView`. Add a `const SizedBox(height: 24)` separator and a section header "Romanization" before it
   - Remove the natural_class_editor.dart import and default_natural_classes.dart import if no longer needed in this file
   - Per D-02: romanization is now inline on the inventory page, not a separate tab

3. Update `phonology_shell.dart`:
   - Change `_sidebarItems` from 2 items to 3: Inventory (/phonology/inventory), Natural Classes (/phonology/natural-classes), Sound Rules (/phonology/sound-rules)
   - Use Icons.category for Natural Classes icon

4. Update `app_router.dart`:
   - Add a third StatefulShellBranch inside the Phonology branch for the Natural Classes page
   - Route: `/phonology/natural-classes` -> `NaturalClassesPage()`
   - Import `natural_classes_page.dart`
   - Ensure branch order matches sidebar order: [inventory, natural-classes, sound-rules]
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter analyze --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Natural Classes" lib/features/phonology/presentation/phonology_shell.dart
    - grep -q "natural-classes" lib/router/app_router.dart
    - grep -q "NaturalClassesPage" lib/features/phonology/presentation/inventory/natural_classes_page.dart
    - ! grep -q "_NaturalClassesSection" lib/features/phonology/presentation/inventory/inventory_page.dart
    - grep -q "RomanizationSection" lib/features/phonology/presentation/inventory/inventory_page.dart
  </acceptance_criteria>
  <done>Phonology has 3 sidebar tabs. Natural classes live on their own page. Romanization renders below vowel chart on inventory page.</done>
</task>

<task type="auto">
  <name>Task 2: Replace alt-key toggle with inline rom+IPA display (D-03, D-04)</name>
  <files>
    lib/features/phonology/presentation/inventory/inventory_page.dart
  </files>
  <read_first>
    lib/features/phonology/presentation/inventory/inventory_page.dart
    lib/features/phonology/data/romanization_providers.dart
  </read_first>
  <action>
1. Remove the alt-key handler entirely from `inventory_page.dart` (per D-04):
   - Delete the `_isAltHeld` state variable (line ~104)
   - Delete the `_handleKeyEvent` method that checks `HardwareKeyboard.instance.isAltPressed`
   - Remove `HardwareKeyboard.instance.addHandler(_handleKeyEvent)` from initState
   - Remove `HardwareKeyboard.instance.removeHandler(_handleKeyEvent)` from dispose
   - Remove all `isAltHeld` parameter passing throughout the widget tree (_AllPhonemesRow, _ConsonantSection, _VowelSection, _ConsonantGrid, _VowelChart, and all chip widgets)

2. Update phoneme chip display logic (per D-03):
   - In the phoneme chip widget (used in consonant grid cells and vowel chart positioned widgets), change display to:
     - Primary text: romanization (from romanization provider mapping for this phoneme's IPA symbol)
     - Secondary text: `/IPA/` in muted style (smaller font, lower opacity) to the RIGHT of romanization
     - Only show the secondary IPA text when romanization differs from IPA (e.g., skip for t->/t/, show for sh->/ʃ/)
   - The chip widgets currently receive phoneme data and the isAltHeld flag — replace the flag-based conditional with always-on logic:
     - Watch `romanizationMappingsProvider` to get the IPA->rom mapping
     - For each phoneme, look up `romMap[phoneme.ipaSymbol]`
     - If rom == ipaSymbol or rom is null: show just the IPA symbol
     - If rom != ipaSymbol: show `rom  /ipa/` where /ipa/ is Text with style `bodySmall, opacity 0.5`
   - Use a Row with MainAxisSize.min containing the rom Text and optionally the muted /ipa/ Text

3. Also update the `_AllPhonemesRow` chips to use the same rom-primary display logic (no more alt-key conditional).

4. Remove the `import 'package:flutter/services.dart'` if HardwareKeyboard was the only user of it.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter analyze --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ! grep -q "_isAltHeld" lib/features/phonology/presentation/inventory/inventory_page.dart
    - ! grep -q "HardwareKeyboard" lib/features/phonology/presentation/inventory/inventory_page.dart
    - ! grep -q "isAltHeld" lib/features/phonology/presentation/inventory/inventory_page.dart
    - grep -q "romanization" lib/features/phonology/presentation/inventory/inventory_page.dart
  </acceptance_criteria>
  <done>Alt-key behavior completely removed. Phoneme chips always show romanization as primary with muted /IPA/ to the right when they differ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan restructures existing UI with no new data flows.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | romanization display | accept | Read-only display of existing DB data; no new inputs |
</threat_model>

<verification>
1. `flutter analyze --no-pub` passes with no errors
2. Phonology sidebar shows exactly 3 entries
3. Navigating to /phonology/natural-classes renders the natural class editor
4. Inventory page shows consonant grid, vowel chart, then romanization section — no natural classes section
5. Phoneme chips show romanization primary with /IPA/ muted when different
6. No alt-key or HardwareKeyboard references remain in inventory_page.dart
</verification>

<success_criteria>
Phonology tab restructured with 3 sub-tabs, inline romanization, and always-visible rom+IPA display on phoneme chips. No functional regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-refactor/07-01-SUMMARY.md`
</output>
