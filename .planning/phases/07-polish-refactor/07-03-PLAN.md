---
phase: 07-polish-refactor
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - lib/db/app_database.dart
  - lib/features/grammar/data/grammar_dao.dart
  - lib/features/lexicon/data/lexeme_dao.dart
  - lib/features/morphology/data/morphology_dao.dart
  - lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart
  - lib/features/morphology/presentation/rules/rule_editor_dialog.dart
  - lib/features/morphology/presentation/rules/rule_editor/form_state_models.dart
  - lib/features/morphology/presentation/rules/rule_editor/condition_editor.dart
  - lib/features/morphology/presentation/rules/rule_editor/operation_editor.dart
  - lib/features/morphology/presentation/rules/rule_editor/branch_editor.dart
  - lib/features/morphology/presentation/rules/rule_editor/rule_editor_body.dart
autonomous: true
requirements: [REFAC-01]
must_haves:
  truths:
    - "No culture wiki imports, providers, or references exist in source code (except DB migration table definition which must stay)"
    - "flutter analyze --no-pub reports no issues"
    - "All getSingle() calls replaced with getSingleOrNull() + explicit null handling"
    - "rule_editor_dialog.dart is reduced to a thin shell (<200 lines) that delegates to child widgets in rule_editor/ subdirectory"
    - "Each file in rule_editor/ subdirectory is under 500 lines"
    - "All existing rule editor behavior preserved — pure refactor"
  artifacts:
    - path: "lib/features/morphology/presentation/rules/rule_editor/form_state_models.dart"
      provides: "_OpState, _CondState, _BranchState form state classes"
      min_lines: 50
    - path: "lib/features/morphology/presentation/rules/rule_editor/operation_editor.dart"
      provides: "Operation editor section widget"
      min_lines: 50
    - path: "lib/features/morphology/presentation/rules/rule_editor/condition_editor.dart"
      provides: "Condition editor section widget"
      min_lines: 50
    - path: "lib/features/morphology/presentation/rules/rule_editor/branch_editor.dart"
      provides: "Branch editor section widget"
      min_lines: 50
    - path: "lib/features/morphology/presentation/rules/rule_editor/rule_editor_body.dart"
      provides: "Main rule editor body assembling child widgets"
      min_lines: 50
  key_links:
    - from: "lib/features/morphology/presentation/rules/rule_editor_dialog.dart"
      to: "rule_editor/rule_editor_body.dart"
      via: "import and widget delegation"
      pattern: "RuleEditorBody"
    - from: "rule_editor/rule_editor_body.dart"
      to: "rule_editor/operation_editor.dart"
      via: "child widget composition"
      pattern: "OperationEditor"
---

<objective>
Clean up dead code, fix getSingle() safety issues, and refactor the 2486-line rule_editor_dialog.dart into smaller focused files.

Purpose: Address REFAC-01 — dead code from culture wiki removal, getSingle() crash risk (D-17/D-18), and rule_editor_dialog.dart maintainability (D-13 through D-16). This is Wave 2 because it depends on Plans 01 and 02 completing first (potential import changes from phonology restructure, and code touched by abbreviation normalization in rule_editor_dialog.dart).
Output: Clean codebase with no dead code, safe database queries, and a well-organized rule editor split into <500-line files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-refactor/07-01-SUMMARY.md
@.planning/phases/07-polish-refactor/07-02-SUMMARY.md
@lib/features/morphology/presentation/rules/rule_editor_dialog.dart
@lib/features/grammar/data/grammar_dao.dart
@lib/features/lexicon/data/lexeme_dao.dart
@lib/features/morphology/data/morphology_dao.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead code cleanup and getSingle() safety fixes (D-10, D-11, D-12, D-17, D-18)</name>
  <files>
    lib/features/grammar/data/grammar_dao.dart
    lib/features/lexicon/data/lexeme_dao.dart
    lib/features/morphology/data/morphology_dao.dart
    lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart
  </files>
  <read_first>
    lib/features/grammar/data/grammar_dao.dart
    lib/features/lexicon/data/lexeme_dao.dart
    lib/features/morphology/data/morphology_dao.dart
    lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart
    lib/db/app_database.dart
  </read_first>
  <action>
1. **Dead code cleanup (D-10, D-11, D-12):**
   - Run `grep -rn "culture\|Culture" lib/` to find any remaining culture wiki references
   - The DB migration table definition for `culture_pages` in `app_database.dart` MUST stay (it's part of the migration chain)
   - Remove any culture-related comments that reference the culture tab in non-migration code
   - Run `grep -rn "flutter_fancy_tree_view\|flutter_markdown_plus" .` to verify no wiki dependency remnants (already confirmed clean — just verify)
   - Run `dart fix --apply` to auto-fix any dart fix suggestions
   - Run `flutter analyze --no-pub` and fix any remaining issues

2. **getSingle() -> getSingleOrNull() safety fix (D-17, D-18):**

   In `grammar_dao.dart`:
   - Line ~59: `.getSingle()` -> `.getSingleOrNull()`. Add null check: if null, throw a descriptive StateError or return a sensible default. E.g.: `final row = await (...).getSingleOrNull(); if (row == null) throw StateError('Dimension not found for id=$dimId');`
   - Line ~316: Same pattern — `.getSingleOrNull()` + meaningful error message

   In `lexeme_dao.dart`:
   - Line ~102: `.getSingle()` -> `.getSingleOrNull()` + null handling with message like `'Lexeme not found for id=$lexemeId'`
   - Line ~114: Same pattern

   In `morphology_dao.dart`:
   - Line ~118: `.getSingleOrNull()` + `'Rule not found for id=$ruleId'`
   - Line ~121: Same pattern
   - Line ~165: Same pattern

   In `pos_dimensions_page.dart`:
   - Line ~39: `.getSingle()` -> `.getSingleOrNull()` + null handling. Since this is in a presentation widget, handle gracefully (e.g., show error text or return early) rather than throwing

   For each replacement, use this pattern:
   ```dart
   // Before:
   final row = await (select(table)..where((t) => t.id.equals(id))).getSingle();
   // After:
   final row = await (select(table)..where((t) => t.id.equals(id))).getSingleOrNull();
   if (row == null) {
     throw StateError('[ClassName.methodName] expected exactly one row for id=$id, found none');
   }
   ```

   For presentation layer code, use a softer pattern:
   ```dart
   final row = await (select(table)..where((t) => t.id.equals(id))).getSingleOrNull();
   if (row == null) return; // or show error state
   ```
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter analyze --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ! grep -rn "getSingle()" lib/features/grammar/data/grammar_dao.dart
    - ! grep -rn "getSingle()" lib/features/lexicon/data/lexeme_dao.dart
    - ! grep -rn "getSingle()" lib/features/morphology/data/morphology_dao.dart
    - grep -q "getSingleOrNull" lib/features/grammar/data/grammar_dao.dart
    - grep -q "getSingleOrNull" lib/features/lexicon/data/lexeme_dao.dart
    - grep -q "getSingleOrNull" lib/features/morphology/data/morphology_dao.dart
  </acceptance_criteria>
  <done>No dead code from culture wiki. All getSingle() replaced with getSingleOrNull() + meaningful error handling. flutter analyze passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor rule_editor_dialog.dart into rule_editor/ subdirectory (D-13, D-14, D-15, D-16)</name>
  <files>
    lib/features/morphology/presentation/rules/rule_editor_dialog.dart
    lib/features/morphology/presentation/rules/rule_editor/form_state_models.dart
    lib/features/morphology/presentation/rules/rule_editor/operation_editor.dart
    lib/features/morphology/presentation/rules/rule_editor/condition_editor.dart
    lib/features/morphology/presentation/rules/rule_editor/branch_editor.dart
    lib/features/morphology/presentation/rules/rule_editor/rule_editor_body.dart
  </files>
  <read_first>
    lib/features/morphology/presentation/rules/rule_editor_dialog.dart
  </read_first>
  <action>
**CRITICAL: This is a pure refactor. No behavior changes. All tests must continue to pass.**

Read the full 2486-line `rule_editor_dialog.dart` carefully before splitting. Identify the logical sections:

1. **Create `rule_editor/form_state_models.dart` (D-13):**
   - Extract `OpType` enum, `_OpState` class, `_CondState` class, `_BranchState` class
   - Make these classes public (remove underscore prefix): `OpState`, `CondState`, `BranchState` — since they'll be imported across files
   - Keep all their methods, dispose logic, and toOperation/toCondition/toBranch converters
   - Move the `AblautDirection` reference import if needed

2. **Create `rule_editor/operation_editor.dart` (D-14):**
   - Extract the operation editing section widget (the part that renders operation type dropdown, affix/ablaut/template/redup/suppletive fields)
   - This widget receives an `OpState` and callbacks for state changes
   - Should be a StatelessWidget or ConsumerWidget that takes the form state as parameters
   - Target: <500 lines

3. **Create `rule_editor/condition_editor.dart` (D-14):**
   - Extract the condition editing section (position dropdown, phonological class/literal fields, IPA text fields)
   - Receives `CondState` and callbacks
   - Target: <500 lines

4. **Create `rule_editor/branch_editor.dart` (D-14):**
   - Extract the branch editing section (default branch + conditional branches, branch add/remove)
   - Receives list of `BranchState` and callbacks
   - Composes OperationEditor and ConditionEditor for each branch
   - Target: <500 lines

5. **Create `rule_editor/rule_editor_body.dart` (D-14):**
   - Extract the main body of the dialog (name field, POS picker, binding picker, branch editor, preview panel, DSL text field, save/cancel buttons)
   - This is the primary composition widget that assembles all the sub-editors
   - Holds the ConsumerStatefulWidget state that currently lives in `_RuleEditorDialogState`
   - Target: <500 lines

6. **Slim down `rule_editor_dialog.dart`:**
   - Keep only the `RuleEditorDialog` StatelessWidget that shows the dialog (showDialog call pattern)
   - It should import and instantiate `RuleEditorBody` from the subdirectory
   - Target: <200 lines (just the dialog shell + parameter forwarding)

7. **Move DSL parsing helpers to domain layer (D-15):**
   - Any DSL-related helper functions that currently live in rule_editor_dialog.dart (e.g., parsing helpers, operation-to-DSL conversion utilities) should move to `lib/features/morphology/domain/morphology_dsl.dart` or a new file like `lib/features/morphology/domain/dsl_form_helpers.dart`
   - Only move pure functions that don't depend on Flutter/UI — keep UI-specific formatting in the presentation layer

8. **Verify no behavior change (D-16):**
   - Run `flutter analyze --no-pub` — must pass
   - Run `flutter test` — all existing tests must pass
   - The public API of `RuleEditorDialog` must remain identical (same constructor parameters, same showDialog usage pattern from callers)
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter analyze --no-pub 2>&1 | tail -5 && flutter test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test -d lib/features/morphology/presentation/rules/rule_editor
    - test -f lib/features/morphology/presentation/rules/rule_editor/form_state_models.dart
    - test -f lib/features/morphology/presentation/rules/rule_editor/operation_editor.dart
    - test -f lib/features/morphology/presentation/rules/rule_editor/condition_editor.dart
    - test -f lib/features/morphology/presentation/rules/rule_editor/branch_editor.dart
    - test -f lib/features/morphology/presentation/rules/rule_editor/rule_editor_body.dart
    - wc -l < lib/features/morphology/presentation/rules/rule_editor_dialog.dart | awk '{exit ($1 > 200)}'
    - wc -l < lib/features/morphology/presentation/rules/rule_editor/rule_editor_body.dart | awk '{exit ($1 > 500)}'
  </acceptance_criteria>
  <done>rule_editor_dialog.dart reduced to <200 line dialog shell. All extracted files in rule_editor/ subdirectory are <500 lines each. All tests pass. No behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB query → UI | getSingleOrNull changes affect how missing data is handled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | I (Info Disclosure) | getSingleOrNull error messages | mitigate | Error messages include row IDs but no user PII — acceptable for debug logging. Do not expose raw SQL in error messages. |
| T-07-04 | D (DoS) | Missing DB rows | mitigate | getSingleOrNull + explicit null check prevents unhandled StateError crashes that would freeze the UI |
</threat_model>

<verification>
1. `flutter analyze --no-pub` passes clean
2. `flutter test` — all existing tests pass
3. No `getSingle()` calls remain in grammar_dao, lexeme_dao, morphology_dao
4. rule_editor_dialog.dart is <200 lines
5. All rule_editor/ files are <500 lines each
6. Rule editor UI works identically (same dialog, same fields, same behavior)
</verification>

<success_criteria>
Codebase cleaned up: no dead code, safe DB queries, and rule_editor_dialog.dart decomposed into maintainable <500-line files with zero behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-refactor/07-03-SUMMARY.md`
</output>
