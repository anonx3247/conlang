---
phase: 07-polish-refactor
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/grammar/domain/dimension_level.dart
  - lib/features/lexicon/presentation/dictionary/word_list_panel.dart
  - lib/features/lexicon/presentation/dictionary/derivation_tree_widget.dart
  - lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
  - lib/features/morphology/presentation/rules/rules_page.dart
autonomous: true
gap_closure: true
requirements: [NIT-04]

must_haves:
  truths:
    - "Paradigm viewer headers and rule list UI show abbreviations in CAPITALS (e.g. NOM, SG)"
    - "Lexicon word list shows abbreviations in lowercase with trailing period (e.g. n., adj.)"
    - "Lexicon word list shows 'word (n.)' inline on one line, not 'Noun' on a separate line"
    - "Derived words in lexicon derivation tree show 'n.' without square brackets"
    - "Auto-derived words in lexicon list do NOT show the meaning text field"
  artifacts:
    - path: "lib/features/grammar/domain/dimension_level.dart"
      provides: "Context-aware abbreviation formatters"
    - path: "lib/features/lexicon/presentation/dictionary/word_list_panel.dart"
      provides: "Inline POS display + auto-derived meaning suppression"
  key_links:
    - from: "paradigm_table_widget.dart"
      to: "dimension_level.dart"
      via: "formatAbbrUpper()"
      pattern: "formatAbbrUpper"
    - from: "word_list_panel.dart"
      to: "dimension_level.dart"
      via: "formatAbbr()"
      pattern: "formatAbbr"
---

<objective>
Fix abbreviation display to be context-aware (CAPITALS in grammar UI, lowercase in lexicon) and restructure lexicon word list item layout to show inline POS and suppress meaning for auto-derived words.

Purpose: UAT items 1-5 from post-Phase-7 feedback batch.
Output: Context-correct abbreviation formatting across all surfaces, cleaner lexicon word list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/features/grammar/domain/dimension_level.dart
@lib/features/lexicon/presentation/dictionary/word_list_panel.dart
@lib/features/lexicon/presentation/dictionary/derivation_tree_widget.dart
@lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
@lib/features/morphology/presentation/rules/rules_page.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Context-aware abbreviation formatting</name>
  <files>
    lib/features/grammar/domain/dimension_level.dart
    lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
    lib/features/morphology/presentation/rules/rules_page.dart
    lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart
  </files>
  <read_first>
    lib/features/grammar/domain/dimension_level.dart
    lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
    lib/features/morphology/presentation/rules/rules_page.dart
    lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart
  </read_first>
  <action>
UAT feedback specifies two abbreviation display contexts:
- CAPITALS for rule-list UI and paradigm viewer titles (NOM, SG)
- lowercase with trailing period for lexicon word list (n., adj.)

In `dimension_level.dart`:
1. Keep existing `formatAbbr()` as-is (lowercase + trailing period) -- this is the lexicon style.
2. Add a NEW function `formatAbbrUpper(String? abbr)` that returns UPPERCASE without trailing period. Logic: if null/empty return '', else `abbr.toUpperCase().replaceAll('.', '')`. This is for paradigm headers and rule labels.

In `paradigm_table_widget.dart`:
- Replace ALL calls to `formatAbbr(...)` with `formatAbbrUpper(...)` (there are ~10 call sites at lines 479, 521, 537, 595, 1306, 1348, 1364, 1417). Import `formatAbbrUpper` from dimension_level.dart.

In `rules_page.dart`:
- Replace the `formatAbbr(inputAbbr)` and `formatAbbr(outputAbbr)` calls at line 331 with `formatAbbrUpper(inputAbbr)` and `formatAbbrUpper(outputAbbr)`. Also update the fallback from `'?.'` to `'?'` since upper format has no period.
- Replace `formatAbbr(lvl.abbr)` at line 573 with `formatAbbrUpper(lvl.abbr)`.

In `coverage_matrix_panel.dart`:
- Replace `formatAbbr(level?.abbr)` at line 131 with `formatAbbrUpper(level?.abbr)`.

Do NOT change calls in lexicon files (word_detail_panel.dart, word_creation_form.dart, derivation_tree_widget.dart) or POS dimension editor files -- those should keep using `formatAbbr()` (lowercase + period).
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && grep -n "formatAbbrUpper" lib/features/grammar/domain/dimension_level.dart lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart lib/features/morphology/presentation/rules/rules_page.dart lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `formatAbbrUpper()` exists in dimension_level.dart and returns uppercase without period
    - All paradigm_table_widget.dart and rules_page.dart abbr calls use `formatAbbrUpper`
    - Lexicon files still use `formatAbbr` (lowercase + period)
    - `flutter analyze --no-pub lib/` shows no new errors
  </acceptance_criteria>
  <done>Paradigm headers show NOM/SG (uppercase, no period), lexicon surfaces show n./adj. (lowercase with period)</done>
</task>

<task type="auto">
  <name>Task 2: Lexicon word list inline POS + derived word display fixes</name>
  <files>
    lib/features/lexicon/presentation/dictionary/word_list_panel.dart
    lib/features/lexicon/presentation/dictionary/derivation_tree_widget.dart
  </files>
  <read_first>
    lib/features/lexicon/presentation/dictionary/word_list_panel.dart
    lib/features/lexicon/presentation/dictionary/derivation_tree_widget.dart
    lib/features/grammar/domain/dimension_level.dart
  </read_first>
  <action>
Three UAT fixes in the word list and derivation tree:

**A) Inline POS in word list (UAT item 3):**
In `word_list_panel.dart` `_buildListView`, restructure each list item (lines ~448-523):
- The first line should show the word with POS inline: `"word (n.)"` format.
- Modify the first `Text(display.rom, ...)` widget (line 452-471) to include the POS abbreviation inline. Change the text from `display.rom` to `'${display.rom}${posAbbr.isNotEmpty ? " ($posAbbr)" : ""}'` where `posAbbr = formatAbbr(lexeme.partOfSpeech)`.
- Import `formatAbbr` from `dimension_level.dart` if not already imported.
- REMOVE the separate POS section at lines 511-523 (the `if (lexeme.partOfSpeech != null ...)` block that renders partOfSpeech on its own line).

**B) Suppress meaning for auto-derived words (UAT item 5):**
In the same list item layout, modify the meaning display condition (lines 501-510):
- Change from `if (lexeme.meaning != null && lexeme.meaning!.isNotEmpty)` to also exclude auto-derived words: `if (lexeme.meaning != null && lexeme.meaning!.isNotEmpty && lexeme.derivedViaRuleId == null)`.
- This ensures auto-derived words (those with a non-null `derivedViaRuleId`) do NOT show the meaning field. Manually entered words and promoted derivations (where derivedViaRuleId is nulled on edit) still show meaning.

**C) Remove brackets from derived word POS badge (UAT item 4):**
In `derivation_tree_widget.dart` at line 443, change:
```dart
'[${formatAbbr(widget.posAbbr)}]'
```
to:
```dart
formatAbbr(widget.posAbbr)
```
This removes the square brackets wrapping the abbreviation in derivation tree badges. The surrounding Container already provides visual distinction via background color and border radius.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && grep -n "derivedViaRuleId == null" lib/features/lexicon/presentation/dictionary/word_list_panel.dart && grep -n "formatAbbr(widget.posAbbr)" lib/features/lexicon/presentation/dictionary/derivation_tree_widget.dart | grep -v "\[" && flutter analyze --no-pub lib/features/lexicon/presentation/dictionary/word_list_panel.dart lib/features/lexicon/presentation/dictionary/derivation_tree_widget.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Word list items show "word (n.)" inline on the first line
    - Separate POS line removed from word list items
    - Auto-derived words (derivedViaRuleId != null) do not show meaning text
    - Derivation tree POS badge shows "n." not "[n.]"
    - No analyzer errors
  </acceptance_criteria>
  <done>Lexicon word list shows compact "word (n.)" format, auto-derived words hide meaning, derivation badges have no brackets</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — pure UI display changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | I | formatAbbrUpper | accept | Display-only change; no data mutation or user input processing |
</threat_model>

<verification>
- `flutter analyze --no-pub lib/` passes with no new errors
- `formatAbbrUpper` used in paradigm + rules contexts
- `formatAbbr` used in lexicon contexts
- Word list items show inline POS, no separate POS line
- Derived words show abbr without brackets
</verification>

<success_criteria>
All 5 abbreviation/display UAT items resolved: context-aware capitalization, inline POS in word list, no brackets on derived POS, no meaning for auto-derived words.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-refactor/07-04-SUMMARY.md`
</output>
