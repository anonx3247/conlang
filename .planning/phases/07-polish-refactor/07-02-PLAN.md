---
phase: 07-polish-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/grammar/data/grammar_dao.dart
  - lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart
  - lib/features/grammar/presentation/pos_dimensions/dimension_editor_panel.dart
  - lib/features/grammar/presentation/pos_dimensions/pos_crud_dialog.dart
  - lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
  - lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart
  - lib/features/grammar/domain/binding_translator.dart
  - lib/features/grammar/domain/dimension_level.dart
  - lib/features/morphology/presentation/rules/rules_page.dart
  - lib/features/morphology/presentation/rules/rule_editor_dialog.dart
  - lib/features/lexicon/presentation/dictionary/word_detail_panel.dart
  - lib/features/lexicon/presentation/dictionary/derivation_tree_widget.dart
  - lib/features/lexicon/presentation/dictionary/word_creation_form.dart
  - lib/shared/widgets/resizable_divider.dart
  - lib/features/phonology/presentation/phonology_shell.dart
  - lib/features/grammar/presentation/grammar_shell.dart
  - lib/features/lexicon/presentation/lexicon_shell.dart
  - lib/shared/widgets/app_shell.dart
autonomous: true
requirements: [NIT-04, NIT-05]
must_haves:
  truths:
    - "All abbreviation displays show trailing period (e.g. v., adj., nom., sg.)"
    - "Abbreviations are normalized to lowercase at save time in grammar_dao and pos_crud_dialog"
    - "Case-insensitive comparison when matching abbreviations"
    - "All panel sidebar/content dividers in shells are draggable to resize"
    - "Draggable handle is thin (4px), cursor changes to col-resize on hover, panels have min/max width constraints"
  artifacts:
    - path: "lib/shared/widgets/resizable_divider.dart"
      provides: "Reusable ResizableDivider widget"
      min_lines: 30
      exports: ["ResizableDivider"]
  key_links:
    - from: "lib/shared/widgets/resizable_divider.dart"
      to: "phonology_shell.dart, grammar_shell.dart, lexicon_shell.dart"
      via: "widget import and usage"
      pattern: "ResizableDivider"
---

<objective>
Normalize abbreviation formatting (lowercase + trailing period) across all display surfaces and replace fixed panel dividers with draggable resize handles throughout the app.

Purpose: Fix abbreviation inconsistencies (D-05 through D-07) and add panel resizing (D-08, D-09) for a more polished desktop experience.
Output: Consistent abbreviation display everywhere, and draggable panel separators in all shell layouts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/features/grammar/data/grammar_dao.dart
@lib/features/grammar/presentation/pos_dimensions/pos_crud_dialog.dart
@lib/features/grammar/presentation/pos_dimensions/dimension_editor_panel.dart
@lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
@lib/features/grammar/domain/binding_translator.dart
@lib/features/morphology/presentation/rules/rules_page.dart
@lib/features/phonology/presentation/phonology_shell.dart
@lib/features/grammar/presentation/grammar_shell.dart
@lib/features/lexicon/presentation/lexicon_shell.dart
@lib/shared/widgets/app_shell.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abbreviation normalization and trailing period display (D-05, D-06, D-07)</name>
  <files>
    lib/features/grammar/data/grammar_dao.dart
    lib/features/grammar/presentation/pos_dimensions/pos_crud_dialog.dart
    lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart
    lib/features/grammar/presentation/pos_dimensions/dimension_editor_panel.dart
    lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
    lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart
    lib/features/grammar/domain/binding_translator.dart
    lib/features/grammar/domain/dimension_level.dart
    lib/features/morphology/presentation/rules/rules_page.dart
    lib/features/morphology/presentation/rules/rule_editor_dialog.dart
    lib/features/lexicon/presentation/dictionary/word_detail_panel.dart
    lib/features/lexicon/presentation/dictionary/derivation_tree_widget.dart
    lib/features/lexicon/presentation/dictionary/word_creation_form.dart
  </files>
  <read_first>
    lib/features/grammar/data/grammar_dao.dart
    lib/features/grammar/presentation/pos_dimensions/pos_crud_dialog.dart
    lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart
    lib/features/grammar/presentation/pos_dimensions/dimension_editor_panel.dart
    lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
    lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart
    lib/features/grammar/domain/binding_translator.dart
    lib/features/grammar/domain/dimension_level.dart
    lib/features/morphology/presentation/rules/rules_page.dart
    lib/features/morphology/presentation/rules/rule_editor_dialog.dart
    lib/features/lexicon/presentation/dictionary/word_detail_panel.dart
    lib/features/lexicon/presentation/dictionary/derivation_tree_widget.dart
    lib/features/lexicon/presentation/dictionary/word_creation_form.dart
  </read_first>
  <action>
1. **Save-time normalization (D-05):**
   - In `pos_crud_dialog.dart`: when saving a POS, call `.toLowerCase()` on the abbreviation text before passing to DAO
   - In `grammar_dao.dart`: in any insert/update methods for POS and dimensions, normalize abbreviation to lowercase before writing to DB. Also normalize level abbreviations in the levelsJson
   - In `dimension_editor_panel.dart`: when saving dimension rename or level rename, normalize abbreviation to lowercase

2. **Create a shared helper function** — add to a small utility (can be top-level in `dimension_level.dart` or a new tiny file):
   ```dart
   /// Format abbreviation for display: lowercase with trailing period.
   /// Returns empty string if input is null or empty.
   String formatAbbr(String? abbr) {
     if (abbr == null || abbr.isEmpty) return '';
     final normalized = abbr.toLowerCase().replaceAll('.', '');
     return '$normalized.';
   }
   ```

3. **Display formatting (D-06) — search all files that display `.abbreviation` and wrap with `formatAbbr()`:**
   - `pos_dimensions_page.dart`: POS list items showing `(${pos.abbreviation})` -> `(${formatAbbr(pos.abbreviation)})`
   - `dimension_editor_panel.dart`: dimension chips and level chips that show abbreviations
   - `paradigm_table_widget.dart`: paradigm column/row headers that show dimension level abbreviations
   - `coverage_matrix_panel.dart`: matrix headers showing abbreviations
   - `rules_page.dart`: rule binding summary text showing abbreviations
   - `rule_editor_dialog.dart`: binding chip labels showing abbreviations
   - `word_detail_panel.dart`: POS pills and dimension badges showing abbreviations
   - `derivation_tree_widget.dart`: POS abbreviation badges
   - `word_creation_form.dart`: POS dropdown or badge display
   - `binding_translator.dart`: if it formats display text with abbreviations (check at read time)

4. **Case-insensitive comparison (D-07):**
   - In `grammar_dao.dart` line ~105 where `posRow.abbreviation.toLowerCase()` is already used for comparison — verify this pattern is consistent
   - In `pos_resolver.dart`: the `.toLowerCase()` comparison at line 37 already handles this — verify no other comparison sites exist
   - Search for any `== abbreviation` or `== abbr` comparisons and ensure they use `.toLowerCase()` on both sides
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter analyze --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "formatAbbr" lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart
    - grep -q "toLowerCase" lib/features/grammar/presentation/pos_dimensions/pos_crud_dialog.dart
    - grep -q "formatAbbr" lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
  </acceptance_criteria>
  <done>All abbreviation displays show trailing period. Abbreviations normalized to lowercase at save time. Case-insensitive comparison on all match sites.</done>
</task>

<task type="auto">
  <name>Task 2: Draggable panel separator widget and shell integration (D-08, D-09)</name>
  <files>
    lib/shared/widgets/resizable_divider.dart
    lib/features/phonology/presentation/phonology_shell.dart
    lib/features/grammar/presentation/grammar_shell.dart
    lib/features/lexicon/presentation/lexicon_shell.dart
    lib/shared/widgets/app_shell.dart
  </files>
  <read_first>
    lib/features/phonology/presentation/phonology_shell.dart
    lib/features/grammar/presentation/grammar_shell.dart
    lib/features/lexicon/presentation/lexicon_shell.dart
    lib/shared/widgets/app_shell.dart
  </read_first>
  <action>
1. **Create `lib/shared/widgets/resizable_divider.dart`:**
   ```dart
   /// A thin vertical draggable divider that resizes an adjacent panel.
   ///
   /// Place between two children in a Row. The [onDrag] callback receives
   /// the horizontal delta on each drag update. The handle is 4px wide,
   /// transparent by default, shows a subtle line on hover, and changes
   /// cursor to SystemMouseCursors.resizeColumn.
   class ResizableDivider extends StatefulWidget {
     const ResizableDivider({super.key, required this.onDrag});
     final void Function(double delta) onDrag;
     // ...
   }
   ```
   Implementation:
   - 4px wide MouseRegion with `cursor: SystemMouseCursors.resizeColumn`
   - GestureDetector with `onHorizontalDragUpdate: (d) => onDrag(d.delta.dx)`
   - On hover: show a 1px line in `colorScheme.outlineVariant` centered in the 4px zone
   - No visible bar when not hovered — just the cursor change

2. **Update each shell to use ResizableDivider for the sidebar divider:**

   For `phonology_shell.dart`:
   - Convert to ConsumerStatefulWidget to hold `_sidebarWidth` state (initial: 200)
   - Replace the fixed `SizedBox(width: 200, ...)` with `SizedBox(width: _sidebarWidth, ...)`
   - Replace the `VerticalDivider(width: 1, ...)` between sidebar and content with `ResizableDivider(onDrag: (d) => setState(() => _sidebarWidth = (_sidebarWidth + d).clamp(140, 320)))`
   - Keep the IPA chart panel divider as a second ResizableDivider if there's a panel there — store `_ipaChartWidth` state (use a similar pattern, but for the RIGHT panel, the delta is negative: `_ipaChartWidth = (_ipaChartWidth - d).clamp(200, 500)`)

   For `grammar_shell.dart`:
   - Same pattern: `_sidebarWidth` state, initial 200, clamp(140, 320)
   - Replace VerticalDivider with ResizableDivider

   For `lexicon_shell.dart`:
   - Same pattern: `_sidebarWidth` state, initial 200, clamp(140, 320)
   - Replace VerticalDivider with ResizableDivider

   For `app_shell.dart`:
   - If there is a glossary drawer divider, replace it with ResizableDivider — store `_glossaryWidth` state, clamp(240, 500)
   - Only apply if the app_shell has a VerticalDivider between content and glossary

3. Each shell widget that currently extends ConsumerWidget will need to become ConsumerStatefulWidget to hold the width state variable.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter analyze --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f lib/shared/widgets/resizable_divider.dart
    - grep -q "ResizableDivider" lib/features/phonology/presentation/phonology_shell.dart
    - grep -q "ResizableDivider" lib/features/grammar/presentation/grammar_shell.dart
    - grep -q "ResizableDivider" lib/features/lexicon/presentation/lexicon_shell.dart
    - grep -q "_sidebarWidth" lib/features/phonology/presentation/phonology_shell.dart
    - grep -q "resizeColumn" lib/shared/widgets/resizable_divider.dart
  </acceptance_criteria>
  <done>All shell panel dividers are draggable. Sidebar widths are resizable between min/max constraints. Handle is 4px with cursor change and subtle hover indicator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — UI-only changes with no new data inputs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | D (DoS) | ResizableDivider | accept | Drag is purely local UI state, cannot cause external harm |
</threat_model>

<verification>
1. `flutter analyze --no-pub` passes with no errors
2. All abbreviation displays show lowercase with trailing period
3. Panel dividers in all shells are draggable with cursor change
4. Sidebar widths stay within min/max bounds during drag
</verification>

<success_criteria>
Abbreviation formatting is consistent across all display surfaces. Panel dividers throughout the app are draggable with proper UX (thin handle, cursor change, min/max constraints).
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-refactor/07-02-SUMMARY.md`
</output>
