---
phase: 03.2
slug: phonology-enhancements-inserted
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-10
---

# Phase 03.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Per-task rows are to be filled in by the planner from RESEARCH.md §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `flutter_test` (bundled with Flutter SDK, already in `pubspec.yaml` dev_dependencies) |
| **Config file** | None — Flutter convention uses `test/` directory |
| **Quick run command** | `flutter test test/phonology/` |
| **Full suite command** | `flutter test` |
| **Estimated runtime** | ~5s quick / ~30s full |

---

## Sampling Rate

- **After every task commit:** Run `flutter test test/phonology/`
- **After every plan wave:** Run `flutter test` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green + manual UAT of both allophone surface points (phoneme dialog + Alt-held inline) via `flutter run`
- **Max feedback latency:** ~5 seconds (quick run)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _(to be filled by planner from RESEARCH.md §Validation Architecture rows)_ |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Create `test/phonology/` directory (does not exist yet)
- [ ] `test/phonology/default_natural_classes_test.dart` — PHON-09 catalog completeness + alias mapping stubs
- [ ] `test/phonology/build_inventory_test.dart` — `_buildInventory` defaults merge + user precedence (the F-1 fix)
- [ ] `test/phonology/resolve_class_test.dart` — `_resolveClass` alias-first lookup ordering (OR extend `test/morphology_engine_test.dart`)
- [ ] `test/phonology/dsl_with_defaults_test.dart` — integration test: parse `[stop]V` template, resolve against empty user inventory, expect non-empty output from default Stops
- [ ] `test/phonology/allophone_computer_test.dart` — stubs for 6 computation cases (literal match, class match, cluster skip, identity skip, dedup, empty state)
- [ ] `test/phonology/natural_class_editor_validator_test.dart` — widget test for extended validator (rejects `S`, `N`, `F`, `L`, `R`)
- [ ] `test/phonology/allophone_section_widget_test.dart` — widget test for dialog Allophones section (empty state + populated state)
- [ ] `test/phonology/phoneme_chip_alt_test.dart` — widget test for Alt-held inline annotation

**Framework install:** Not needed — `flutter_test` already in `pubspec.yaml` dev_dependencies.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Click a phoneme with 2+ rewrite rules → dialog shows "Allophones" section with correct realizations | PHON-11 | Full dialog interaction + visual rendering across themes | `flutter run`, open a project with rewrite rules, open phonology tab, tap a phoneme with known allophones |
| Hold Alt over inventory grid → chips show inline allophones; release Alt → annotations disappear | PHON-11 | HardwareKeyboard event + layout reflow behavior is hard to automate at widget-test level | `flutter run`, hold Alt while hovering inventory page, verify inline annotation appears/disappears |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
