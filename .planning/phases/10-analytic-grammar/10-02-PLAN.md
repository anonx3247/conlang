---
phase: 10-analytic-grammar
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/features/grammar/presentation/particles/particles_page.dart
  - lib/features/grammar/data/particle_providers.dart
  - lib/features/grammar/domain/leipzig_catalog.dart
  - lib/features/grammar/presentation/particles/particle_dialog.dart
  - test/widget/grammar/particles_page_test.dart
  - test/unit/grammar/closed_class_dao_test.dart
autonomous: true
requirements:
  - AGRAM-01

must_haves:
  truths:
    - "User can see closed-class words grouped by POS on the Particles page"
    - "User can add a new particle with IPA, POS, gloss tag, and meaning via a dialog"
    - "User can edit an existing particle by tapping its row"
    - "User can delete a particle with confirmation dialog"
    - "Gloss tag field offers autocomplete from ~45 Leipzig abbreviations but accepts custom tags"
    - "Particles automatically appear in unified lexicon search and Anki export"
  artifacts:
    - path: "lib/features/grammar/presentation/particles/particles_page.dart"
      provides: "Full Particles page with POS-grouped list, add/edit/delete"
      min_lines: 100
    - path: "lib/features/grammar/presentation/particles/particle_dialog.dart"
      provides: "Add/edit particle AlertDialog with IPA, POS, gloss tag autocomplete, meaning"
      exports: ["ParticleDialog"]
    - path: "lib/features/grammar/domain/leipzig_catalog.dart"
      provides: "Predefined list of ~45 common Leipzig glossing abbreviations"
      exports: ["leipzigGlossCatalog"]
    - path: "lib/features/grammar/data/particle_providers.dart"
      provides: "Riverpod providers for closed-class word list"
      exports: ["closedClassLexemeListProvider"]
  key_links:
    - from: "lib/features/grammar/presentation/particles/particles_page.dart"
      to: "lib/features/grammar/data/particle_providers.dart"
      via: "ref.watch(closedClassLexemeListProvider)"
      pattern: "closedClassLexemeListProvider"
    - from: "lib/features/grammar/data/particle_providers.dart"
      to: "lib/features/lexicon/data/lexeme_dao.dart"
      via: "watchClosedClassWords() stream"
      pattern: "watchClosedClassWords"
---

<objective>
Full Particles page implementation: POS-grouped closed-class word list, add/edit/delete dialogs with Leipzig gloss tag autocomplete, and Riverpod providers.

Purpose: Delivers AGRAM-01 — users can define closed-class words with grammatical gloss tags in a dedicated Grammar sub-tab. Because particles are stored in the Lexemes table (D-02), they automatically appear in unified search and Anki export.

Output: Working Particles page where users can CRUD closed-class words with gloss tags, grouped by POS with collapsible sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-analytic-grammar/10-CONTEXT.md
@.planning/phases/10-analytic-grammar/10-RESEARCH.md
@.planning/phases/10-analytic-grammar/10-UI-SPEC.md
@.planning/phases/10-analytic-grammar/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From lib/features/lexicon/data/lexeme_dao.dart (Plan 01 added):
```dart
Stream<List<Lexeme>> watchClosedClassWords() =>
    (select(lexemes)
          ..where((t) => t.isClosedClass.equals(true))
          ..orderBy([(t) => OrderingTerm.asc(t.partOfSpeech),
                     (t) => OrderingTerm.asc(t.ipa)]))
        .watch();
```

From lib/db/app_database.dart (Lexemes table after Plan 01):
```dart
// New columns added by Plan 01:
BoolColumn get isClosedClass => boolean().withDefault(const Constant(false))();
TextColumn get glossTag => text().nullable()();
```

From lib/features/lexicon/data/lexeme_providers.dart (existing pattern):
```dart
final lexemeDaoProvider = Provider<LexemeDao?>((ref) {
  final db = ref.watch(currentDatabaseProvider);
  if (db == null) return null;
  return db.lexemeDao;
});
```

From lib/features/grammar/data/grammar_providers.dart (POS list):
```dart
final posListProvider = StreamProvider<List<PartsOfSpeechData>>((ref) { ... });
```

From 10-UI-SPEC.md (Particles page components):
- Page: SingleChildScrollView(padding: EdgeInsets.all(24))
- Title: Text('Particles', style: theme.textTheme.headlineSmall) + SizedBox(height: 24)
- POS sections: ExpansionTile or Column, initially expanded, header w600
- List item: ListTile with leading gloss tag Chip, title = IPA, subtitle = meaning
- Add button: FilledButton.icon(icon: Icon(Icons.add), label: Text('Add Particle'))
- Delete: IconButton(Icons.delete_outline, color: colorScheme.error)
- Dialog: AlertDialog with TextField for IPA, DropdownButtonFormField for POS, Autocomplete for gloss tag
- Gloss tag chip: Chip with colorScheme.secondaryContainer background
- Empty state heading: "No particles yet"
- Empty state body: "Add closed-class words -- particles, auxiliaries, determiners, and conjunctions -- using the button above."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leipzig catalog + particle providers + Particles page + dialog</name>
  <files>
    lib/features/grammar/domain/leipzig_catalog.dart
    lib/features/grammar/data/particle_providers.dart
    lib/features/grammar/presentation/particles/particles_page.dart
    lib/features/grammar/presentation/particles/particle_dialog.dart
    test/unit/grammar/closed_class_dao_test.dart
  </files>
  <read_first>
    lib/features/grammar/presentation/particles/particles_page.dart
    lib/features/lexicon/data/lexeme_dao.dart
    lib/features/lexicon/data/lexeme_providers.dart
    lib/features/grammar/data/grammar_providers.dart
    lib/features/grammar/presentation/typology/typology_page.dart
    lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart
    .planning/phases/10-analytic-grammar/10-UI-SPEC.md
  </read_first>
  <action>
1. Create `lib/features/grammar/domain/leipzig_catalog.dart`:
   A `const List<LexicalGlossEntry>` with ~45 common Leipzig abbreviations. Each entry has `abbreviation` (String) and `meaning` (String) and `category` (String). Categories: Determination, Polarity, Aspect, Tense, Modality/TAM, Voice/Valence, Information, Pragmatics, Coordination, Adposition, Case function, Subordination, Evidentiality, Other. Include these abbreviations:
   DEF (Definite), INDF (Indefinite), NEG (Negation), PROG (Progressive), PERF (Perfective), IPFV (Imperfective), PST (Past), PRS (Present), FUT (Future), AUX (Auxiliary), MOD (Modal), CAUS (Causative), PASS (Passive), REFL (Reflexive), RECIP (Reciprocal), TOP (Topic), FOC (Focus), EMPH (Emphatic), Q (Question), CONJ (Conjunction), DISJ (Disjunction), PREP (Preposition), POST (Postposition), GEN (Genitive), ACC (Accusative), DAT (Dative), LOC (Locative), COMP (Complementizer), REL (Relativizer), SUB (Subordinator), PTCL (Particle), DET (Determiner), QUANT (Quantifier), INTJ (Interjection), COP (Copula), EXIST (Existential), CERT (Certitude), EV (Evidential), QUOT (Quotative), PRFV (Perfect).

   Also export a flat `const List<String> leipzigGlossAbbreviations` with just the abbreviation strings for the autocomplete.

2. Create `lib/features/grammar/data/particle_providers.dart`:
   ```dart
   final closedClassLexemeListProvider = StreamProvider<List<Lexeme>>((ref) {
     final dao = ref.watch(lexemeDaoProvider);
     if (dao == null) return Stream.value([]);
     return dao.watchClosedClassWords();
   });
   ```
   Follow the exact pattern from `lexeme_providers.dart`.

3. Replace the placeholder `lib/features/grammar/presentation/particles/particles_page.dart` with the full implementation per UI-SPEC:
   - `ConsumerWidget` (needs ref for providers)
   - Watch `closedClassLexemeListProvider` and `posListProvider`
   - Page structure: `SingleChildScrollView(padding: EdgeInsets.all(24))` with `headlineSmall` title "Particles", `SizedBox(height: 24)`
   - "Add Particle" button: `FilledButton.icon(icon: Icon(Icons.add), label: Text('Add Particle'))` — opens `ParticleDialog` in add mode
   - Group particles by `partOfSpeech` using POS names from `posListProvider`. For each POS that has particles, render a collapsible section (use a `Column` with a header row — the header has `fontWeight: FontWeight.w600`, the POS name). Default expanded.
   - Empty POS sections are hidden (not shown)
   - Each particle row: `ListTile` with:
     - leading: `Chip(label: Text(glossTag ?? ''), backgroundColor: colorScheme.secondaryContainer)` — max width via `ConstrainedBox(maxWidth: 60)`
     - title: `Text(lexeme.ipa)` (or romanization if available)
     - subtitle: `Text(lexeme.meaning ?? '')` 
     - trailing: `IconButton(icon: Icon(Icons.delete_outline), color: colorScheme.error)` — shows delete confirmation dialog
     - onTap: opens `ParticleDialog` in edit mode
   - Full empty state when no particles: heading "No particles yet", body "Add closed-class words -- particles, auxiliaries, determiners, and conjunctions -- using the button above."
   - Delete confirmation dialog: title "Delete particle?", body "'[ipa]' ([glossTag]) will be permanently deleted.", actions Cancel (TextButton) + Delete (FilledButton with error color)

4. Create `lib/features/grammar/presentation/particles/particle_dialog.dart`:
   - `ConsumerStatefulWidget` — `ParticleDialog`
   - Constructor params: optional `Lexeme? existingParticle` (null = add mode, non-null = edit mode)
   - Form fields in an `AlertDialog`:
     a. IPA text field — `TextField` with label "IPA", hint "Enter IPA form". Make the IPA keyboard accessible if the project has the IPA keyboard widget (check if there is a shared IPA keyboard trigger pattern — look at how morphology rule editor or lexicon word creation dialog does it).
     b. POS dropdown — `DropdownButtonFormField<String>` populated from `posListProvider`. Label "Part of speech". Uses `initialValue:` (not deprecated `value:`).
     c. Gloss tag — `Autocomplete<String>` widget with `optionsBuilder` that filters `leipzigGlossAbbreviations` by substring match (case-insensitive). Label "Gloss tag", hint "e.g. NEG, DEF, PROG". `maxLength: 20` on the underlying `TextField` (T-10-02 threat mitigation). Allows freeform entry per D-04.
     d. Meaning text field — `TextField` with label "Meaning".
   - Validation: IPA required (show "IPA form is required." error), gloss tag max 20 chars (show "Gloss tags must be 20 characters or fewer.")
   - Save action: calls `lexemeDao.insertLexeme(LexemesCompanion(..., isClosedClass: Value(true), glossTag: Value(tag)))` for add, or `lexemeDao.updateLexeme(...)` for edit. Dismiss dialog on success.
   - Cancel action: `Navigator.pop(context)`
   - Dialog title: "Add Particle" or "Edit Particle" based on mode

5. Create `test/unit/grammar/closed_class_dao_test.dart`:
   - Test that inserting a lexeme with `isClosedClass: true` makes it appear in `watchClosedClassWords()` but NOT in `watchRoots()`
   - Test that inserting a regular lexeme (isClosedClass: false) does NOT appear in `watchClosedClassWords()` but DOES appear in `watchRoots()`
   - Use `NativeDatabase.memory()` + `AppDatabase.forTesting(...)` pattern from existing tests
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter test test/unit/grammar/closed_class_dao_test.dart --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/grammar/domain/leipzig_catalog.dart contains `const leipzigGlossCatalog`
    - lib/features/grammar/domain/leipzig_catalog.dart contains at least 40 entries (DEF, NEG, PROG, FUT, PST, PRS, PASS, CAUS, etc.)
    - lib/features/grammar/domain/leipzig_catalog.dart contains `const leipzigGlossAbbreviations`
    - lib/features/grammar/data/particle_providers.dart contains `closedClassLexemeListProvider`
    - lib/features/grammar/data/particle_providers.dart contains `watchClosedClassWords`
    - lib/features/grammar/presentation/particles/particles_page.dart contains `ConsumerWidget` or `ConsumerStatefulWidget`
    - lib/features/grammar/presentation/particles/particles_page.dart contains `closedClassLexemeListProvider`
    - lib/features/grammar/presentation/particles/particles_page.dart contains `posListProvider`
    - lib/features/grammar/presentation/particles/particles_page.dart contains `No particles yet`
    - lib/features/grammar/presentation/particles/particles_page.dart contains `Add Particle`
    - lib/features/grammar/presentation/particles/particles_page.dart contains `Delete particle?`
    - lib/features/grammar/presentation/particles/particle_dialog.dart contains `class ParticleDialog`
    - lib/features/grammar/presentation/particles/particle_dialog.dart contains `Autocomplete`
    - lib/features/grammar/presentation/particles/particle_dialog.dart contains `leipzigGlossAbbreviations`
    - lib/features/grammar/presentation/particles/particle_dialog.dart contains `isClosedClass: Value(true)`
    - lib/features/grammar/presentation/particles/particle_dialog.dart contains `maxLength` or character limit enforcement for gloss tag
    - test/unit/grammar/closed_class_dao_test.dart exits 0
  </acceptance_criteria>
  <done>Particles page shows closed-class words grouped by POS. User can add/edit/delete particles with Leipzig gloss tag autocomplete. Particles stored in Lexemes table with isClosedClass=true, automatically visible in unified search.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text input -> Lexemes table | Gloss tag, IPA, meaning entered via dialog saved to SQLite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Gloss tag text field | mitigate | Drift parameterized queries — all values bound as parameters. maxLength: 20 on TextField. |
| T-10-02 | Denial of Service | Unbounded text field input | mitigate | maxLength: 20 on gloss tag, standard TextField limits on IPA/meaning |
</threat_model>

<verification>
1. `flutter test test/unit/grammar/closed_class_dao_test.dart --no-pub` — passes
2. App launches, navigate to Grammar > Particles, see empty state
3. Add a particle with IPA "la", POS "Determiner", gloss tag "DEF", meaning "the" — appears grouped under Determiner section
4. Edit the particle — dialog pre-fills with existing values
5. Delete the particle — confirmation dialog, then removed
6. The particle appears in Lexicon unified search
</verification>

<success_criteria>
- Particles page shows closed-class words grouped by POS with collapsible sections
- Add particle dialog has IPA field, POS dropdown, Leipzig gloss tag autocomplete, meaning field
- Gloss tag autocomplete shows ~45 Leipzig abbreviations, allows custom tags
- Edit and delete particle flows work correctly
- Particles appear in unified lexicon search automatically (D-02)
- Empty state shown when no particles exist
</success_criteria>

<output>
After completion, create `.planning/phases/10-analytic-grammar/10-02-SUMMARY.md`
</output>
