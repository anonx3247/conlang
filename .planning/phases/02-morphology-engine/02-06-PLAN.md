---
phase: 02-morphology-engine
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/morphology/presentation/rules/rule_editor_dialog.dart
  - lib/features/morphology/presentation/rules/preview_panel.dart
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Operation type dropdown shows plain-language labels (e.g. 'Replacement form' not 'Suppletive', 'Remove ending' not 'Remove suffix')"
    - "Affix, condition literal, and ablaut from/to fields in the rule editor use IpaTextField with IPA keyboard popup"
    - "Preview panel text uses monospace font for all root and derived forms"
    - "Preview panel has a regenerate button that forces new sample words"
  artifacts:
    - path: "lib/features/morphology/presentation/rules/rule_editor_dialog.dart"
      provides: "Renamed OpType labels, IpaTextField usage in morphology form fields"
      contains: "IpaTextField"
    - path: "lib/features/morphology/presentation/rules/preview_panel.dart"
      provides: "Monospace preview text, regenerate IconButton"
      contains: "Icons.refresh"
  key_links:
    - from: "lib/features/morphology/presentation/rules/rule_editor_dialog.dart"
      to: "lib/features/phonology/presentation/shared/ipa_keyboard/ipa_text_field.dart"
      via: "import and usage of IpaTextField"
      pattern: "IpaTextField"
---

<objective>
Apply UX polish to the rule editor and preview panel: clearer jargon, IPA keyboards, monospace preview, regenerate button.

Purpose: Four UAT gaps (items 1, 2, 6, 7) are independent polish fixes that don't alter data models or architecture. Grouping them avoids touching the same files across multiple plans.
Output: More accessible rule editor labels, IPA keyboard support in morphology fields, polished preview panel.
</objective>

<execution_context>
@/Users/neosapien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/neosapien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@lib/features/morphology/presentation/rules/rule_editor_dialog.dart
@lib/features/morphology/presentation/rules/preview_panel.dart
@lib/features/phonology/presentation/shared/ipa_keyboard/ipa_text_field.dart
@.planning/phases/02-morphology-engine/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jargon clarity + IPA keyboards in rule editor</name>
  <files>lib/features/morphology/presentation/rules/rule_editor_dialog.dart</files>
  <action>
**Jargon clarity (UAT gap 1):**
1. In the `OpType` enum, rename labels to be plain-language:
   - `prefix` → `'Prefix (add to start)'`
   - `suffix` → `'Suffix (add to end)'`
   - `infix` → `'Infix (insert inside)'`
   - `ablaut` → `'Vowel change'`
   - `template` → `'Root template'`
   - `reduplication` → `'Reduplication (copy)'`
   - `suppletive` → `'Replacement form'`

2. In `_buildOpFields`, update the hint texts:
   - For `OpType.suppletive`: change `'Suppletive form (literal)'` to `'Replacement form (e.g. went, mice)'`
   - For ablaut: hint text is fine as-is ('from (IPA)', 'to (IPA)')

**IPA keyboards (UAT gap 2):**
3. Add import for IpaTextField: `import '../../../phonology/presentation/shared/ipa_keyboard/ipa_text_field.dart';`
4. In `_buildOpFields`, replace `TextField` with `IpaTextField` for these fields:
   - `OpType.prefix || OpType.suffix` affix field (the `op.affixCtrl` TextField)
   - `OpType.infix` affix field (the `op.affixCtrl` TextField inside the Row — NOT the position number field)
   - `OpType.ablaut` from field (`op.ablautFromCtrl`) and to field (`op.ablautToCtrl`)
   - `OpType.suppletive` form field (`op.suppletiveCtrl`)
5. In `_buildConditionRow`, replace the condition value `TextField` with `IpaTextField` for the literal condition types. The field is the `SizedBox(width: 120)` TextField using `branch.condValueCtrl`. Only needs IPA keyboard when `condType` is `endsWithLiteral` or `startsWithLiteral` (not for class-based conditions which take class names like 'nasal', 'stop'). Approach: conditionally use `IpaTextField` when `branch.condType == CondType.endsWithLiteral || branch.condType == CondType.startsWithLiteral`, otherwise plain `TextField`.

Note: IpaTextField is a drop-in replacement for TextField — same constructor interface (controller, decoration, onChanged). The `showIpaKeyboard: true` default is fine. Keep the existing `isDense: true` and `border: OutlineInputBorder()` decoration.
  </action>
  <verify>Run `flutter analyze` — no errors. Verify IpaTextField import resolves.</verify>
  <done>OpType labels are plain-language. Affix, condition literal, ablaut, and suppletive fields use IpaTextField with IPA keyboard popup.</done>
</task>

<task type="auto">
  <name>Task 2: Preview panel monospace font + regenerate button</name>
  <files>lib/features/morphology/presentation/rules/preview_panel.dart</files>
  <action>
**Monospace font (UAT gap 6):**
The preview panel table rows already use monospace in `_buildRow` via `monoStyle`. Verify the "Preview" header label also looks consistent. No change needed in the table itself — it already uses `fontFamily: 'monospace'` with `fontFamilyFallback`. The gap may refer to the header or empty state text — those are fine as labels.

**Regenerate button (UAT gap 7):**
1. Add a regenerate button next to the "Preview" header label. In the `build` method, find the `Padding` widget that renders the `'Preview'` text (around line 152-159). Replace it with a `Row` containing:
   - The existing `Text('Preview', ...)` widget
   - `const Spacer()`
   - `IconButton(icon: const Icon(Icons.refresh, size: 18), tooltip: 'Regenerate preview', onPressed: _scheduleRefresh, padding: EdgeInsets.zero, constraints: const BoxConstraints(minWidth: 28, minHeight: 28))`

2. Apply the same Row pattern to the "Preview" label in the `_emptyState` method as well, so the regen button is always visible.

This mirrors the WordGeneratorPanel pattern from Phase 1 (Icons.refresh, size: 18, tooltip: 'Regenerate words').
  </action>
  <verify>Run `flutter analyze` — no errors.</verify>
  <done>Preview panel has a regenerate button in the header row. Monospace font is confirmed already applied to table text.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` — no new warnings or errors
2. OpType labels are human-readable in the dropdown
3. IPA keyboard popup appears when focusing affix/condition/ablaut/suppletive fields in the rule editor
4. Preview panel shows a refresh icon button next to "Preview" heading
</verification>

<success_criteria>
Rule editor labels are jargon-free, IPA fields have keyboard popups, preview has monospace text and a regenerate button.
</success_criteria>

<output>
After completion, create `.planning/phases/02-morphology-engine/02-06-SUMMARY.md`
</output>
