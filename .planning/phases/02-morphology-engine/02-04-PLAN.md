---
phase: 02-morphology-engine
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - lib/features/morphology/presentation/rules/rules_page.dart
  - lib/features/morphology/presentation/rules/rule_editor_dialog.dart
autonomous: false

must_haves:
  truths:
    - "User has visually confirmed the Morphology tab and rule editor work end-to-end"
    - "All four typological strategies produce correct output in the preview"
    - "Branching conditions correctly route to different operations based on word shape"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete morphology engine flow end-to-end with the user, then apply any UI fixes discovered during verification.

Purpose: The rule editor is the centrepiece of the application. Visual and interaction quality must be confirmed by a human before marking the phase complete.

Output: Verified, working morphology engine with rule editor UI.
</objective>

<execution_context>
@/Users/neosapien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/neosapien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-morphology-engine/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify morphology engine end-to-end</name>
  <files>lib/features/morphology/presentation/rules/rules_page.dart, lib/features/morphology/presentation/rules/rule_editor_dialog.dart</files>
  <action>
Launch the app with `flutter run -d macos` and present the verification steps to the user. No code changes -- this is a human verification gate.
  </action>
  <verify>
Launch the app with `flutter run -d macos` and open a project that has phonemes and phonotactic templates defined.

**Test 1 -- Suffix rule (concatenative):**
1. Click the Morphology tab
2. Click "Add Rule", name it "Plural"
3. Add a Suffix operation with affix "in"
4. Verify the DSL expression shows something like `+in`
5. Verify the preview panel shows sample words with "-in" appended

**Test 2 -- Branching rule (environment-sensitive):**
1. Create a new rule "Suffix -in (branching)"
2. Add a branch with condition "Ends with literal" = "o", operations: remove "o" + suffix "in"
3. Add a default branch with suffix "in"
4. Verify DSL shows both branches separated by `|`
5. Verify preview: consonant-final words get "+in", words ending in "o" lose the "o" and get "+in"

**Test 3 -- Template (Semitic root-and-pattern):**
1. Create a rule "Template Test"
2. Add a Template operation with pattern `1a23aa`
3. Verify preview shows consonants extracted from sample words and slotted into the pattern

**Test 4 -- Ablaut:**
1. Create a rule "Ablaut Test"
2. Add an Ablaut operation: from "a" to "e"
3. Verify preview shows all "a" vowels replaced with "e"

**Test 5 -- Edit and Delete:**
1. Edit one of the created rules, change a parameter, verify preview updates
2. Delete a rule, confirm it disappears from the list

**Test 6 -- Error handling:**
1. Create a rule with only a conditional branch (no default), verify that sample words not matching the condition show an error in the preview
  </verify>
  <done>User types "approved" or describes issues to fix.</done>
</task>

<task type="auto">
  <name>Task 2: Apply UI fixes from verification feedback</name>
  <files>lib/features/morphology/presentation/rules/rules_page.dart, lib/features/morphology/presentation/rules/rule_editor_dialog.dart</files>
  <action>
Apply any fixes reported by the user during the checkpoint verification. Common issues to watch for:
- Layout overflow on smaller windows
- Text truncation in the DSL display
- Preview panel not updating correctly
- Operation fields not clearing when switching type
- Branch reordering issues
- Color/contrast issues in the dark theme

If the user reports no issues, skip this task (mark as done with "No fixes needed").
  </action>
  <verify>Relaunch app, confirm the reported issues are fixed.</verify>
  <done>All user-reported issues from checkpoint are resolved, or no issues were reported.</done>
</task>

</tasks>

<verification>
1. User has confirmed the morphology engine works end-to-end
2. All four typological strategies (suffix, template, ablaut, suppletive/passthrough) produce correct output
3. Branching conditions correctly route based on word environment
4. Rule CRUD (create, read, update, delete) works
5. Any reported UI issues are fixed
</verification>

<success_criteria>
- User approves the checkpoint
- All four success criteria from ROADMAP.md are met or testable:
  1. Pattern mini-language rule can be applied to produce correct derived forms
  2. Four typological strategies work: concatenative, template, ablaut, analytic
  3. Word derivation rules chain with root definitions (tested via multi-operation branches)
  4. Per-word exception infrastructure exists (exceptions table + stale detection, UI for entering exceptions is Phase 3 lexicon work since "exceptions entered from the word")
</success_criteria>

<output>
After completion, create `.planning/phases/02-morphology-engine/02-04-SUMMARY.md`
</output>
