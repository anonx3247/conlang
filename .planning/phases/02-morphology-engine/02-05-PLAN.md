---
phase: 02-morphology-engine
plan: 05
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/features/morphology/domain/morphology_dsl.dart
  - test/morphology_engine_test.dart
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "InfixOp rules saved to DB can be reloaded into the editor with correct affix and position values"
    - "DSL round-trip for InfixOp is lossless: serialize then parse produces identical domain object"
    - "ROADMAP.md success criterion 4 explicitly notes Phase 3 delivery for exception UI"
  artifacts:
    - path: "lib/features/morphology/domain/morphology_dsl.dart"
      provides: "infix: parser case in _buildOperationParser()"
      contains: "string('infix:')"
    - path: "test/morphology_engine_test.dart"
      provides: "InfixOp DSL round-trip test"
      contains: "InfixOp"
  key_links:
    - from: "lib/features/morphology/domain/morphology_dsl.dart"
      to: "lib/features/morphology/domain/morphology_dsl.dart"
      via: "_buildOperationParser infix case mirrors _serializeOp InfixOp case"
      pattern: "string\\('infix:'"
---

<objective>
Fix the InfixOp DSL parser gap (blocker) and formally defer exception UI to Phase 3.

Purpose: InfixOp rules cannot survive a save/reload cycle — the serializer emits `infix:um:1` but the parser has no `infix:` case, silently dropping the operation on reload. This is a data-loss blocker. Additionally, ROADMAP.md success criterion 4 (exception UI) needs formal deferral to Phase 3 where word detail pages exist.
Output: Working InfixOp DSL round-trip with TDD test, updated ROADMAP.md
</objective>

<execution_context>
@/Users/neosapien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/neosapien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/features/morphology/domain/morphology_dsl.dart
@test/morphology_engine_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — InfixOp DSL round-trip fix</name>
  <files>lib/features/morphology/domain/morphology_dsl.dart, test/morphology_engine_test.dart</files>
  <action>
RED phase:
1. Add a test to `test/morphology_engine_test.dart` in a new group `'InfixOp DSL round-trip'`:
   - Create a rule with `InfixOp(affix: 'um', position: 1)` as the sole operation
   - Serialize it with `serializeMorphRule()`
   - Verify the serialized string is `'infix:um:1'`
   - Parse the serialized string with `parseMorphDsl()`
   - Verify `parsed.isValid` is true
   - Verify the first branch's first operation is an `InfixOp` with affix `'um'` and position `1`
2. Run `flutter test test/morphology_engine_test.dart` — the parse step MUST FAIL (proving the bug exists)

GREEN phase:
3. In `_buildOperationParser()` in `morphology_dsl.dart`, add an `infix:` parser case BEFORE the `suffix` case (since `infix:` starts with a letter and would be consumed by `prefix` pattern otherwise). The parser should:
   - Match `string('infix:')` followed by `pattern('^ |:').plus().flatten()` (affix) followed by `char(':')` followed by `pattern('0-9').plus().flatten()` (position)
   - Map to `InfixOp(affix: affix, position: int.parse(posStr))`
4. Insert the `infix` parser into the choice list in `_buildOperationParser()`. Place it after `redup` and before `supplete` (both use colon-prefixed tokens). The full order should be: `ablaut | redup | infix | supplete | removeSuffixQuoted | removeSuffixBare | suffix | template | prefix`
5. Run `flutter test test/morphology_engine_test.dart` — ALL tests must pass including the new one

REFACTOR: No refactoring needed — the change is minimal and follows the existing redup pattern exactly.
  </action>
  <verify>Run `flutter test test/morphology_engine_test.dart` — all tests pass (15 existing + 1 new = 16 total)</verify>
  <done>InfixOp serializes to `infix:um:1` and parses back to InfixOp with correct affix and position. Round-trip is lossless.</done>
</task>

<task type="auto">
  <name>Task 2: Defer exception UI to Phase 3 in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
In `.planning/ROADMAP.md`, update Phase 2 success criterion 4:

Change:
```
4. User can mark any individual word as an exception to any morphological rule and supply the irregular form directly
```
To:
```
4. ~~User can mark any individual word as an exception to any morphological rule and supply the irregular form directly~~ — **Infrastructure complete (schema + DAO); UI deferred to Phase 3** where word detail pages provide the natural entry point for per-word exception management (per CONTEXT.md: "Exceptions entered from the word")
```

This formally documents the deferral rather than leaving it as an implicit gap. The schema (MorphologicalRuleExceptions table) and DAO (insertException, deleteException, watchExceptionsForRule, findStaleExceptions) are fully built and tested — only the UI entry point is missing, and it belongs in the word detail page (Phase 3 Lexicon).
  </action>
  <verify>Read `.planning/ROADMAP.md` and confirm criterion 4 shows the deferral note</verify>
  <done>ROADMAP.md Phase 2 criterion 4 explicitly documents that exception UI infrastructure is complete but UI is deferred to Phase 3</done>
</task>

</tasks>

<verification>
1. `flutter test test/morphology_engine_test.dart` — 16 tests pass (15 existing + 1 new InfixOp round-trip)
2. ROADMAP.md Phase 2 criterion 4 shows explicit deferral with rationale
3. `flutter analyze` — no new warnings or errors
</verification>

<success_criteria>
InfixOp DSL round-trip works: serialize -> parse -> identical domain object. ROADMAP.md formally documents the exception UI deferral to Phase 3.
</success_criteria>

<output>
After completion, create `.planning/phases/02-morphology-engine/02-05-SUMMARY.md`
</output>
