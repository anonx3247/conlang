---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/app.dart
  - lib/router/app_router.dart
  - lib/shared/widgets/app_shell.dart
  - lib/features/phonology/presentation/phonology_shell.dart
  - lib/features/phonology/presentation/inventory/inventory_page.dart
  - lib/features/phonology/presentation/sound_rules/sound_rules_page.dart
  - analysis_options.yaml
autonomous: true

must_haves:
  truths:
    - "Flutter desktop app launches with a 1280x800 window, minimum 900x600"
    - "Top tab bar shows Phonology tab (active) and grayed-out placeholders for Lexicon, Grammar, Culture"
    - "Phonology sidebar shows two items: Inventory and Sound Rules"
    - "Clicking sidebar items navigates between Inventory and Sound Rules pages without losing scroll/form state"
    - "Empty state pages render with placeholder content when no project is open"
  artifacts:
    - path: "pubspec.yaml"
      provides: "All Phase 1 dependencies"
      contains: "go_router"
    - path: "lib/main.dart"
      provides: "App entry point with window_manager and ProviderScope"
      contains: "windowManager"
    - path: "lib/router/app_router.dart"
      provides: "go_router with nested StatefulShellRoute"
      contains: "StatefulShellRoute"
    - path: "lib/shared/widgets/app_shell.dart"
      provides: "Top-level tab shell with NavigationRail or TabBar"
      min_lines: 30
    - path: "lib/features/phonology/presentation/phonology_shell.dart"
      provides: "Phonology sub-shell with sidebar navigation"
      min_lines: 20
  key_links:
    - from: "lib/main.dart"
      to: "lib/app.dart"
      via: "runApp(ProviderScope(child: ConlangApp()))"
      pattern: "ProviderScope.*ConlangApp"
    - from: "lib/app.dart"
      to: "lib/router/app_router.dart"
      via: "MaterialApp.router with routerConfig"
      pattern: "MaterialApp\\.router"
    - from: "lib/router/app_router.dart"
      to: "lib/shared/widgets/app_shell.dart"
      via: "StatefulShellRoute builder"
      pattern: "AppShell"
---

<objective>
Create the Flutter desktop application shell with all Phase 1 dependencies, window management, go_router navigation with nested StatefulShellRoute, top-level tab bar, and Phonology sidebar with Inventory and Sound Rules sub-pages.

Purpose: Establishes the foundational app structure that all other plans build into. Navigation architecture (tabs + sidebar) is the hardest to retrofit later.
Output: Running Flutter desktop app with tab+sidebar navigation, empty state pages, and all dependencies installed.
</objective>

<execution_context>
@/Users/neosapien/.claude/get-shit-done/workflows/execute-plan.md
@/Users/neosapien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Flutter project with all dependencies and window setup</name>
  <files>pubspec.yaml, lib/main.dart, analysis_options.yaml</files>
  <action>
Create a new Flutter desktop project (if not already initialized) in the repo root. If `pubspec.yaml` does not exist, run `flutter create . --project-name conlang_workbench --platforms=macos,windows,linux`.

Add all Phase 1 dependencies to pubspec.yaml:
- Core: go_router, drift, drift_flutter, flutter_riverpod, riverpod_annotation, riverpod_generator, petitparser, just_audio, just_audio_media_kit, window_manager, path_provider, path, media_kit_libs_linux, media_kit_libs_windows_audio
- Dev: build_runner, drift_dev, riverpod_lint, riverpod_generator (if not already in core)

Run `flutter pub get` to install.

Create `lib/main.dart`:
- Call `WidgetsFlutterBinding.ensureInitialized()`
- Call `await windowManager.ensureInitialized()`
- Call `JustAudioMediaKit.ensureInitialized()` for Windows/Linux audio (per research Pitfall 4)
- Configure `WindowOptions`: size 1280x800, minimumSize 900x600, center: true, titleBarStyle: normal
- Use `windowManager.waitUntilReadyToShow()` then show+focus
- Call `runApp(const ProviderScope(child: ConlangApp()))`

Update `analysis_options.yaml` to include riverpod_lint custom_lint rules.
  </action>
  <verify>Run `flutter pub get` — exits 0. Run `dart analyze lib/main.dart` — no errors.</verify>
  <done>pubspec.yaml has all dependencies, main.dart configures window and launches ProviderScope-wrapped app, analysis_options includes riverpod_lint.</done>
</task>

<task type="auto">
  <name>Task 2: Create go_router navigation shell with tabs and sidebar</name>
  <files>lib/app.dart, lib/router/app_router.dart, lib/shared/widgets/app_shell.dart, lib/features/phonology/presentation/phonology_shell.dart, lib/features/phonology/presentation/inventory/inventory_page.dart, lib/features/phonology/presentation/sound_rules/sound_rules_page.dart</files>
  <action>
Create `lib/app.dart`:
- `ConlangApp` StatelessWidget returning `MaterialApp.router` with `routerConfig` from the router provider
- Use a professional desktop-appropriate Material 3 theme (dark or light, Claude's discretion)

Create `lib/router/app_router.dart`:
- Define a `GoRouter` with `initialLocation: '/phonology/inventory'`
- Outer `StatefulShellRoute.indexedStack` with builder `AppShell` — this is the top-level tab bar
- Inner `StatefulShellRoute.indexedStack` for Phonology with builder `PhonologyShell` — this is the sidebar
- Two branches inside Phonology: `/phonology/inventory` -> InventoryPage, `/phonology/sound-rules` -> SoundRulesPage
- Add placeholder branches for future tabs (Lexicon, Grammar, Culture) that route to simple "Coming soon" pages — these tabs will be disabled/grayed
- Expose router as a Riverpod provider

Create `lib/shared/widgets/app_shell.dart`:
- `AppShell` widget that renders a horizontal tab bar at the top for major sections (Phonology, Lexicon, Grammar, Culture)
- Only Phonology tab is interactive; others are visually disabled (grayed out, non-clickable) with tooltip "Coming in Phase N"
- Renders the `navigationShell.currentChild` below the tab bar
- This should feel like a professional desktop tool (per user decision), not a mobile app — use appropriate sizing, spacing, and density

Create `lib/features/phonology/presentation/phonology_shell.dart`:
- Left sidebar with two items: "Inventory" and "Sound Rules"
- Use NavigationRail or a custom sidebar (width ~200px) — Claude's discretion on exact widget
- IPA reference chart panel placeholder on the right side (fixed width ~280px, empty for now — Plan 01-03 fills it)
- Main content area in the center renders the current sub-page

Create empty state pages:
- `InventoryPage`: placeholder text "No project open" or "Create a project to start defining phonemes" (per Claude's discretion for empty state design)
- `SoundRulesPage`: similar placeholder

Per user decision: navigation should feel like a professional desktop tool with tabs + sidebar, not a mobile app. The IPA reference chart is a persistent side panel (always visible), not a separate page.
  </action>
  <verify>Run `flutter build macos --debug 2>&1 | tail -5` (or appropriate desktop target) — compiles without error. Run `dart analyze lib/` — no errors.</verify>
  <done>App launches showing Phonology tab active, sidebar with Inventory and Sound Rules, clicking between them navigates correctly, disabled tabs for future phases are visible but non-interactive, layout has professional desktop density.</done>
</task>

</tasks>

<verification>
- `flutter pub get` succeeds
- `dart analyze lib/` reports no errors
- `flutter build macos --debug` (or host platform) succeeds
- App launches to a window with correct dimensions
- Tab bar visible with Phonology active, other tabs grayed out
- Sidebar switches between Inventory and Sound Rules without full page rebuild
</verification>

<success_criteria>
Flutter desktop app compiles and launches. Navigation structure (tabs + sidebar + future IPA panel slot) is in place. All Phase 1 dependencies are installed. Empty state pages render correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
