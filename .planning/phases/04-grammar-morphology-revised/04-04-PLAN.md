---
phase: 04-grammar-morphology-revised
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - lib/features/grammar/presentation/grammar_shell.dart
  - lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart
  - lib/features/grammar/presentation/pos_dimensions/dimension_template_picker.dart
  - lib/features/grammar/presentation/pos_dimensions/dimension_editor_panel.dart
  - lib/features/grammar/presentation/inflectional_rules/inflectional_rules_page.dart
  - lib/features/grammar/presentation/paradigm_viewer/paradigm_viewer_page.dart
  - lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart
  - lib/features/grammar/presentation/paradigm_viewer/cell_override_dialog.dart
  - lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart
  - lib/features/grammar/presentation/paradigm_viewer/axis_config_bar.dart
  - lib/features/grammar/presentation/typology/typology_page.dart
  - lib/features/grammar/presentation/shared/migration_banner.dart
  - lib/features/grammar/data/paradigm_cell_override_dao.dart
  - lib/features/grammar/data/paradigm_coverage_provider.dart
  - lib/features/morphology/presentation/rules/rule_editor_dialog.dart
  - lib/features/morphology/presentation/rules/rules_page.dart
  - lib/router/app_router.dart
  - lib/shared/widgets/app_shell.dart
  - test/widget/grammar/grammar_router_test.dart
  - test/widget/grammar/rule_editor_dialog_kind_test.dart
  - test/widget/grammar/pos_dimensions_page_test.dart
  - test/widget/grammar/paradigm_table_widget_test.dart
  - test/widget/grammar/typology_page_test.dart
  - test/unit/grammar/paradigm_cell_override_test.dart
autonomous: true
requirements: [GRAM-01, GRAM-02, GRAM-03, GRAM-04, GRAM-05, GRAM-06]
must_haves:
  truths:
    - "Navigating to /grammar/pos renders the POS & Dimensions master-detail page; navigating to /grammar/inflectional renders the Inflectional Rules page; /grammar/paradigm renders the Paradigm Viewer; /grammar/typology renders the Typology form"
    - "The Morphology top-tab is gone from app_shell.dart; the Grammar tab is enabled"
    - "No /morphology/* routes exist in app_router.dart (they are replaced by /grammar/*)"
    - "Opening the 'Add Dimension' modal shows ≥20 templates grouped by 9 categories with tooltips"
    - "Clicking a template card inserts a dimension instance bound to the current POS"
    - "The rule editor dialog renders FilterChip rows for inflectional mode (one row per dimension with one chip per level), and input/output POS dropdowns for derivational mode"
    - "An inflectional rule with zero chips selected shows a validation error and prevents save"
    - "Creating a second rule with identical feature bindings to an existing one shows a live tiebreak conflict banner with the other rule's name"
    - "The paradigm table widget renders one cell per Cartesian feature combination for the selected word, with romanization on top line and [IPA] on bottom line"
    - "Clicking any paradigm cell opens the Cell Override Dialog; saving persists via ParadigmCellOverrides table keyed on (lexemeId, featureSetJson)"
    - "Overridden cells render amber with a warning icon; clearing the override reverts to the computed form"
    - "Uncovered cells render em-dash + 'Create a rule for this cell' button in the override dialog"
    - "The coverage matrix panel lists every {dim, level} pair for the selected POS with green dot (has matching rule) or red dot (no matching rule)"
    - "Typology form auto-saves alignment/word_order/modality to project_settings on every dropdown change"
    - "Axis config bar persists (rows, cols, tabs) per POS to project_settings"
    - "The migration banner renders on first open of /grammar/inflectional and dismisses per project_settings key"
  artifacts:
    - path: "lib/features/grammar/presentation/grammar_shell.dart"
      provides: "Grammar tab shell with 4-sub-tab sidebar (mirror of lexicon_shell.dart)"
      contains: "class GrammarShell"
    - path: "lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart"
      provides: "Master-detail POS + dimension editor"
      contains: "class PosDimensionsPage"
    - path: "lib/features/grammar/presentation/pos_dimensions/dimension_template_picker.dart"
      provides: "Two-step template picker modal (D-05)"
      contains: "showDimensionTemplatePicker"
    - path: "lib/features/grammar/presentation/inflectional_rules/inflectional_rules_page.dart"
      provides: "Rules list filtered to kind='inflectional' using the shared rule editor"
      contains: "class InflectionalRulesPage"
    - path: "lib/features/grammar/presentation/paradigm_viewer/paradigm_viewer_page.dart"
      provides: "Paradigm viewer sub-tab with POS picker, axis config, table, coverage panel"
      contains: "class ParadigmViewerPage"
    - path: "lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart"
      provides: "Shared paradigm table widget (Grammar + Lexicon consumers)"
      contains: "class ParadigmTableWidget"
    - path: "lib/features/grammar/presentation/paradigm_viewer/cell_override_dialog.dart"
      provides: "Per-cell override dialog with rom/IPA auto-derive"
      contains: "class CellOverrideDialog"
    - path: "lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart"
      provides: "POS coverage matrix panel with green/red indicators"
      contains: "class CoverageMatrixPanel"
    - path: "lib/features/grammar/presentation/typology/typology_page.dart"
      provides: "Typology form with alignment/word_order/modality dropdowns"
      contains: "class TypologyPage"
    - path: "lib/features/morphology/presentation/rules/rule_editor_dialog.dart"
      provides: "Extended RuleEditorDialog with RuleKind constructor parameter + chip picker"
      contains: "RuleKind kind"
    - path: "lib/features/grammar/data/paradigm_cell_override_dao.dart"
      provides: "ParadigmCellOverrides CRUD + featureSetKey helper"
      contains: "ParadigmCellOverrideDao"
    - path: "lib/features/grammar/data/paradigm_coverage_provider.dart"
      provides: "paradigmCoverageMatrixProvider(posId)"
      contains: "paradigmCoverageMatrixProvider"
  key_links:
    - from: "lib/router/app_router.dart"
      to: "lib/features/grammar/presentation/grammar_shell.dart"
      via: "StatefulShellBranch with 4 sub-routes replaces Morphology branch"
      pattern: "GrammarShell"
    - from: "lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart"
      to: "lib/features/grammar/data/typology_providers.dart::computedInflectedParadigmProvider"
      via: "ref.watch(computedInflectedParadigmProvider(lexemeId))"
      pattern: "computedInflectedParadigmProvider"
    - from: "lib/features/morphology/presentation/rules/rule_editor_dialog.dart"
      to: "lib/features/grammar/domain/tiebreak_detector.dart::findDuplicateSpecificityConflicts"
      via: "live chip toggle recomputes conflicts and renders banner"
      pattern: "findDuplicateSpecificityConflicts"
---

<objective>
Build the Grammar tab UI end-to-end — router surgery to delete the Morphology tab and create the Grammar tab with 4 sub-routes, POS & Dimensions master-detail page with template picker, Inflectional Rules page reusing the shared rule editor, Paradigm Viewer with axis config + table + coverage matrix + cell override dialog, and Typology form. Also extend `RuleEditorDialog` to be kind-aware with feature-binding chips and live tiebreak detection, and introduce the ParadigmCellOverrides DAO.

Purpose: Satisfy D-05 (template picker), D-06 (no hard limits), D-12 (inline tiebreak banner), D-14 (uncovered cell UX), D-15 (coverage visibility), D-23 (POS page relocation), D-24 (delete Morphology tab), D-25 (2-axis layout), D-26 (axis config persisted), D-27 (paradigm viewer in Grammar), D-28 (per-cell override dialog), D-29 (rom/IPA stacked display), D-30 (violation highlighting per cell), D-31 (4-sub-tab Grammar IA), D-32 (master-detail POS+dim), D-33 (inflectional rules page), D-34 (paradigm viewer layout), D-35 (typology form), D-40 (kind-aware RuleEditorDialog), D-42 (chip picker).

Output: Complete Grammar tab UI with all 4 sub-tabs, extended rule editor dialog, cell override DAO, coverage matrix provider, comprehensive widget tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-grammar-morphology-revised/04-CONTEXT.md
@.planning/phases/04-grammar-morphology-revised/04-RESEARCH.md
@.planning/phases/04-grammar-morphology-revised/04-UI-SPEC.md
@.planning/phases/04-grammar-morphology-revised/04-01-PLAN.md
@.planning/phases/04-grammar-morphology-revised/04-02-PLAN.md
@.planning/phases/04-grammar-morphology-revised/04-03-PLAN.md

<interfaces>
From lib/features/lexicon/presentation/lexicon_shell.dart (existing — mirror this exactly for GrammarShell):
- 200px left sidebar with `surfaceContainerLow` background
- VerticalDivider (`outlineVariant`)
- Expanded content area with navigationShell
- Section header "LEXICON" in labelSmall with `letterSpacing: 1.2`, padding `fromLTRB(16, 16, 16, 8)`
- `_SidebarItem` tiles: height 40, `primaryContainer` when selected, `onPrimaryContainer` icon/label when selected

From lib/router/app_router.dart (to be modified):
- Existing Branch 1: Morphology — DELETE
- Existing Branch 3: Grammar placeholder (Coming Soon page) — REPLACE with full Grammar StatefulShellRoute
- The Grammar branch should have 4 sub-routes under /grammar/pos, /grammar/inflectional, /grammar/paradigm, /grammar/typology
- Strategy: REPLACE Morphology branch index (branch 1) with the new full Grammar branch (moving Grammar from branch 3). This keeps Lexicon at branch 2 stable. Delete the old Branch 3 Grammar placeholder. Final order: 0=Phonology, 1=Grammar, 2=Lexicon, 3=Culture.

From lib/shared/widgets/app_shell.dart lines 23-29 (current _tabs list):
```dart
static const _tabs = [
  _TabItem(label: 'Phonology', icon: Icons.music_note, enabled: true, phase: null),
  _TabItem(label: 'Morphology', icon: Icons.auto_fix_high, enabled: true, phase: null),
  _TabItem(label: 'Lexicon', icon: Icons.menu_book, enabled: true, phase: null),
  _TabItem(label: 'Grammar', icon: Icons.account_tree, enabled: false, phase: 'Phase 4'),
  _TabItem(label: 'Culture', icon: Icons.language, enabled: false, phase: 'Phase 5'),
];
```
Target post-change:
```dart
static const _tabs = [
  _TabItem(label: 'Phonology', icon: Icons.music_note, enabled: true, phase: null),
  _TabItem(label: 'Grammar', icon: Icons.account_tree, enabled: true, phase: null),
  _TabItem(label: 'Lexicon', icon: Icons.menu_book, enabled: true, phase: null),
  _TabItem(label: 'Culture', icon: Icons.language, enabled: false, phase: 'Phase 5'),
];
```

From lib/features/morphology/presentation/rules/rule_editor_dialog.dart (existing, to be extended):
- Constructor currently: `const RuleEditorDialog({super.key, this.existing})` (verify by reading lines 1-50)
- Target: `const RuleEditorDialog({super.key, required this.kind, this.existing})`
- Internal state: `_OpState`, `_BranchState`, `_CondState` — unchanged
- The dialog currently shows POS chip filter for posIds (multi-select). For inflectional kind → hide the POS chip filter, add "Applies to" feature-binding chip rows. For derivational kind → keep POS filter as "Input POS" + add "Output POS" dropdown.

From 04-02:
- `InflectionalRule` view-model (id, name, source, isActive, bindings)
- `FeatureBindings(pos, dims)` 
- `RuleKind` enum
- `grammarDaoProvider`, `dimensionsForPosProvider`
- `inflectionalRulesForPosProvider`, `rulesByKindProvider`
- `dimensionTemplates` const catalog

From 04-03:
- `computedInflectedParadigmProvider(lexemeId)` → `Map<FeatureSet, ParadigmCell>`
- `typologySettingsProvider`, `writeTypologyKey`
- `paradigmAxesProvider(posId)`, `setParadigmAxes`
- `findDuplicateSpecificityConflicts` from tiebreak_detector

From lib/shared/widgets/violation_text.dart (Phase 3):
- `ViolationText(text, violations)` widget — drop-in for phonotactic highlighting per D-30

From lib/features/phonology/data/romanization_providers.dart (Phase 1):
- `romanizeProvider` — `String Function(String ipa)` for rendering rom on top line per D-29
- `deromanizeProvider` — `String Function(String romanization)` for reverse-derive in cell override dialog per D-28

UI-SPEC key specs:
- Sidebar: 200px width, surfaceContainerLow bg
- Touch target: 48px min
- Paradigm cell: min 48×64, 4px internal vertical gap
- Override cell: amber background `Colors.amber.withValues(alpha: 0.15)`, border `Colors.amber.withValues(alpha: 0.4)`, icon `Icons.warning_amber_outlined`
- Uncovered cell: em-dash `—` + `Icons.add_circle_outline`
- Coverage dots: green = `Colors.green.shade600` 8px, red = `colorScheme.error` 8px
- Tiebreaker banner: `colorScheme.errorContainer` bg, 12px padding, rounded 4px
- Migration banner: `colorScheme.surfaceContainerHigh` bg, primary left-border 3px, dismissible
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router surgery + Grammar shell — delete Morphology branch, add Grammar branch with 4 sub-routes, update app_shell tabs. Widget test covers all routes.</name>
  <files>
    lib/router/app_router.dart,
    lib/shared/widgets/app_shell.dart,
    lib/features/grammar/presentation/grammar_shell.dart,
    lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart,
    lib/features/grammar/presentation/inflectional_rules/inflectional_rules_page.dart,
    lib/features/grammar/presentation/paradigm_viewer/paradigm_viewer_page.dart,
    lib/features/grammar/presentation/typology/typology_page.dart,
    test/widget/grammar/grammar_router_test.dart
  </files>
  <read_first>
    - lib/router/app_router.dart (full file — 203 lines — understand the nested StatefulShellRoute architecture)
    - lib/shared/widgets/app_shell.dart (full file — _tabs list, _TabButton)
    - lib/features/lexicon/presentation/lexicon_shell.dart (pattern for sidebar mirror)
    - lib/features/morphology/presentation/morphology_shell.dart (the shell being deleted — study its 152-line StatefulNavigationShell structure so the Grammar one mirrors it)
    - .planning/phases/04-grammar-morphology-revised/04-UI-SPEC.md (GrammarShell spec)
    - .planning/phases/04-grammar-morphology-revised/04-RESEARCH.md §Pitfall 4 (branch index drift)
  </read_first>
  <behavior>
    - Test 1: A pumped `AppShell` with a test GoRouter initially at `/grammar/pos` displays `PosDimensionsPage` (find by type). Tapping the "Inflectional Rules" sidebar item navigates to `/grammar/inflectional` and `InflectionalRulesPage` is visible.
    - Test 2: Navigating programmatically via `router.go('/grammar/paradigm')` renders `ParadigmViewerPage`.
    - Test 3: Navigating via `router.go('/grammar/typology')` renders `TypologyPage`.
    - Test 4: The top tab bar contains a "Grammar" tab (find via `find.text('Grammar')`) and does NOT contain "Morphology".
    - Test 5: No GoRoute path in `app_router.dart` starts with `/morphology` (grep after modification).
    - Test 6: `/morphology` and `/morphology/pos` are not reachable — attempting `router.go('/morphology')` does not crash (go_router handles gracefully) but displays nothing matching a Morphology widget.
  </behavior>
  <action>
**Step 1: Create skeleton Grammar pages** (content will be filled in subsequent tasks). For this task only, create minimal stub widgets so the router compiles and the widget test can assert route resolution:

`lib/features/grammar/presentation/grammar_shell.dart` — mirror `lexicon_shell.dart` exactly. Copy the file structure, rename class to `GrammarShell`, change the section header string to `'GRAMMAR'`, and replace the `_sidebarItems` list with:

```dart
static const _sidebarItems = [
  _SidebarItem(label: 'POS & Dimensions', icon: Icons.category_outlined, route: '/grammar/pos'),
  _SidebarItem(label: 'Inflectional Rules', icon: Icons.auto_fix_high_outlined, route: '/grammar/inflectional'),
  _SidebarItem(label: 'Paradigm Viewer', icon: Icons.table_chart_outlined, route: '/grammar/paradigm'),
  _SidebarItem(label: 'Typology', icon: Icons.language_outlined, route: '/grammar/typology'),
];
```

Keep the `StatefulNavigationShell` construction identical to `lexicon_shell.dart`. Expected full file ~160 lines. The `_SidebarItem` class + builder method must match the lexicon_shell exactly — same styling, same `primaryContainer` selected state, same 200px width.

Create minimal stub widgets for the 4 sub-pages (each a `StatelessWidget` with a single `Center(Text('POS & Dimensions'))` style body — real content comes in tasks 2–5):

```dart
// pos_dimensions_page.dart
class PosDimensionsPage extends StatelessWidget {
  const PosDimensionsPage({super.key});
  @override Widget build(BuildContext context) =>
      const Center(child: Text('POS & Dimensions'));
}
```

Do the same for `InflectionalRulesPage`, `ParadigmViewerPage`, `TypologyPage`. Each in its own file at the paths listed in `files_modified`.

**Step 2: Edit `lib/router/app_router.dart`:**

Imports at top — remove:
```dart
import '../features/morphology/presentation/morphology_shell.dart';
import '../features/morphology/presentation/pos/pos_page.dart';
import '../features/morphology/presentation/rules/rules_page.dart';
```

Add:
```dart
import '../features/grammar/presentation/grammar_shell.dart';
import '../features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart';
import '../features/grammar/presentation/inflectional_rules/inflectional_rules_page.dart';
import '../features/grammar/presentation/paradigm_viewer/paradigm_viewer_page.dart';
import '../features/grammar/presentation/typology/typology_page.dart';
```

Replace Branch 1 (Morphology, lines ~104-133) and Branch 3 (Grammar placeholder, lines ~180-188) with a SINGLE Grammar branch at index 1 (replacing Morphology's slot). Delete the old Branch 3 placeholder entirely. Final branch order: 0=Phonology, 1=Grammar, 2=Lexicon, 3=Culture.

```dart
// Branch 1: Grammar (Phase 4)
StatefulShellBranch(
  routes: [
    GoRoute(
      path: '/grammar',
      redirect: (_, _) => '/grammar/pos',
    ),
    StatefulShellRoute.indexedStack(
      builder: (context, state, navigationShell) =>
          GrammarShell(navigationShell: navigationShell),
      branches: [
        StatefulShellBranch(
          routes: [
            GoRoute(
              path: '/grammar/pos',
              builder: (_, _) => const PosDimensionsPage(),
            ),
          ],
        ),
        StatefulShellBranch(
          routes: [
            GoRoute(
              path: '/grammar/inflectional',
              builder: (_, _) => const InflectionalRulesPage(),
            ),
          ],
        ),
        StatefulShellBranch(
          routes: [
            GoRoute(
              path: '/grammar/paradigm',
              builder: (_, _) => const ParadigmViewerPage(),
            ),
          ],
        ),
        StatefulShellBranch(
          routes: [
            GoRoute(
              path: '/grammar/typology',
              builder: (_, _) => const TypologyPage(),
            ),
          ],
        ),
      ],
    ),
  ],
),
```

**Step 3: Edit `lib/shared/widgets/app_shell.dart`:**

Replace the `_tabs` list (lines 23-29) with:

```dart
static const _tabs = [
  _TabItem(label: 'Phonology', icon: Icons.music_note, enabled: true, phase: null),
  _TabItem(label: 'Grammar', icon: Icons.account_tree, enabled: true, phase: null),
  _TabItem(label: 'Lexicon', icon: Icons.menu_book, enabled: true, phase: null),
  _TabItem(label: 'Culture', icon: Icons.language, enabled: false, phase: 'Phase 5'),
];
```

Note: Morphology removed, Grammar promoted to index 1 and enabled, Culture shifts from index 4 to index 3. The branch indices in `app_router.dart` now match the tab indices exactly.

**Step 4: Leave the morphology shell file alone for now** — do NOT delete `lib/features/morphology/presentation/morphology_shell.dart` yet. The rule editor dialog and rules_page widgets will still be imported by the Grammar inflectional rules page and Lexicon derivations page. Only the shell and router wiring are deprecated. The shell file can be physically deleted in a later cleanup if desired, but for this phase we leave it dormant to minimize delete-churn (nothing imports it after router surgery).

**Step 5: Write `test/widget/grammar/grammar_router_test.dart`:**

```dart
import 'package:conlang_workbench/features/grammar/presentation/grammar_shell.dart';
import 'package:conlang_workbench/features/grammar/presentation/inflectional_rules/inflectional_rules_page.dart';
import 'package:conlang_workbench/features/grammar/presentation/paradigm_viewer/paradigm_viewer_page.dart';
import 'package:conlang_workbench/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart';
import 'package:conlang_workbench/features/grammar/presentation/typology/typology_page.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:go_router/go_router.dart';

void main() {
  // Build a minimal router mirroring the Phase 4 Grammar branch structure.
  // We can't instantiate the full app_router because it depends on project
  // providers — instead, construct an isolated router with just the Grammar branch.

  Widget _buildApp(GoRouter router) => ProviderScope(
        child: MaterialApp.router(routerConfig: router),
      );

  GoRouter _grammarOnlyRouter(String initial) => GoRouter(
        initialLocation: initial,
        routes: [
          StatefulShellRoute.indexedStack(
            builder: (context, state, shell) => GrammarShell(navigationShell: shell),
            branches: [
              StatefulShellBranch(routes: [GoRoute(path: '/grammar/pos', builder: (_, _) => const PosDimensionsPage())]),
              StatefulShellBranch(routes: [GoRoute(path: '/grammar/inflectional', builder: (_, _) => const InflectionalRulesPage())]),
              StatefulShellBranch(routes: [GoRoute(path: '/grammar/paradigm', builder: (_, _) => const ParadigmViewerPage())]),
              StatefulShellBranch(routes: [GoRoute(path: '/grammar/typology', builder: (_, _) => const TypologyPage())]),
            ],
          ),
        ],
      );

  testWidgets('/grammar/pos renders PosDimensionsPage', (tester) async {
    final router = _grammarOnlyRouter('/grammar/pos');
    await tester.pumpWidget(_buildApp(router));
    await tester.pumpAndSettle();
    expect(find.byType(PosDimensionsPage), findsOneWidget);
  });

  testWidgets('/grammar/inflectional renders InflectionalRulesPage', (tester) async {
    final router = _grammarOnlyRouter('/grammar/inflectional');
    await tester.pumpWidget(_buildApp(router));
    await tester.pumpAndSettle();
    expect(find.byType(InflectionalRulesPage), findsOneWidget);
  });

  testWidgets('/grammar/paradigm renders ParadigmViewerPage', (tester) async {
    final router = _grammarOnlyRouter('/grammar/paradigm');
    await tester.pumpWidget(_buildApp(router));
    await tester.pumpAndSettle();
    expect(find.byType(ParadigmViewerPage), findsOneWidget);
  });

  testWidgets('/grammar/typology renders TypologyPage', (tester) async {
    final router = _grammarOnlyRouter('/grammar/typology');
    await tester.pumpWidget(_buildApp(router));
    await tester.pumpAndSettle();
    expect(find.byType(TypologyPage), findsOneWidget);
  });

  testWidgets('app_router.dart declares no /morphology routes', (tester) async {
    // Source-level assertion via file read — avoids needing to bootstrap the full app.
    final file = await File('lib/router/app_router.dart').readAsString();
    expect(file.contains('/morphology'), isFalse, reason: 'Morphology routes should be removed.');
    expect(file.contains('/grammar/pos'), isTrue);
    expect(file.contains('/grammar/inflectional'), isTrue);
    expect(file.contains('/grammar/paradigm'), isTrue);
    expect(file.contains('/grammar/typology'), isTrue);
  });
}
```

Add `import 'dart:io';` at the top for the source-level assertion.
  </action>
  <verify>
    <automated>flutter test --no-pub test/widget/grammar/grammar_router_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class GrammarShell' lib/features/grammar/presentation/grammar_shell.dart`
    - `grep -q 'class PosDimensionsPage' lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart`
    - `grep -q 'class InflectionalRulesPage' lib/features/grammar/presentation/inflectional_rules/inflectional_rules_page.dart`
    - `grep -q 'class ParadigmViewerPage' lib/features/grammar/presentation/paradigm_viewer/paradigm_viewer_page.dart`
    - `grep -q 'class TypologyPage' lib/features/grammar/presentation/typology/typology_page.dart`
    - `grep -q '/grammar/pos' lib/router/app_router.dart`
    - `grep -q '/grammar/inflectional' lib/router/app_router.dart`
    - `grep -q '/grammar/paradigm' lib/router/app_router.dart`
    - `grep -q '/grammar/typology' lib/router/app_router.dart`
    - `grep -v '//' lib/router/app_router.dart | grep -q "'/morphology'" || true; if grep -v '//' lib/router/app_router.dart | grep -q "'/morphology"; then exit 1; fi` — confirms no active /morphology route (comments OK, live routes not)
    - `grep -q "_TabItem(label: 'Grammar', icon: Icons.account_tree, enabled: true" lib/shared/widgets/app_shell.dart`
    - `! grep -q "_TabItem(label: 'Morphology'" lib/shared/widgets/app_shell.dart`
    - `flutter test --no-pub test/widget/grammar/grammar_router_test.dart` exits 0 with all 5 tests passing
    - `flutter analyze --no-fatal-warnings lib/router/app_router.dart lib/shared/widgets/app_shell.dart lib/features/grammar/presentation/grammar_shell.dart` passes
  </acceptance_criteria>
  <done>Grammar tab is live in the top tab bar, all 4 sub-routes resolve to stub pages, Morphology tab and routes are gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PosDimensionsPage master-detail with POS list panel + dimension editor + template picker modal</name>
  <files>
    lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart,
    lib/features/grammar/presentation/pos_dimensions/dimension_editor_panel.dart,
    lib/features/grammar/presentation/pos_dimensions/dimension_template_picker.dart,
    test/widget/grammar/pos_dimensions_page_test.dart
  </files>
  <read_first>
    - lib/features/morphology/presentation/pos/pos_page.dart (existing POS editor — reuse for name/abbreviation CRUD per D-32; relocate the dialog or inline it)
    - lib/features/lexicon/presentation/dictionary/dictionary_page.dart (master-detail layout reference for the 260px left + Expanded right pattern)
    - lib/features/grammar/data/dimension_templates.dart (04-02 — the const catalog)
    - lib/features/grammar/data/grammar_providers.dart (grammarDaoProvider, dimensionsForPosProvider, dimensionTemplatesProvider)
    - lib/features/grammar/data/grammar_dao.dart (insertDimension, updateDimensionLevels, deleteDimension, nextDimensionOrdering, nextLevelId)
    - lib/features/grammar/domain/dimension_level.dart (DimensionLevel + encode/decode)
    - .planning/phases/04-grammar-morphology-revised/04-UI-SPEC.md §POS & Dimensions sub-tab, §Dimension Template Picker modal
    - .planning/phases/04-grammar-morphology-revised/04-CONTEXT.md D-05, D-06, D-32
  </read_first>
  <behavior>
    - Test 1: With an in-memory DB seeded with 2 POS rows (Noun, Verb), pumping `PosDimensionsPage` inside a `ProviderScope` (with `currentDatabaseProvider` overridden) shows both POS names in the left panel.
    - Test 2: Tapping the "Noun" tile selects it (the tile's background becomes `primaryContainer` — assert via `find.byType(Container)` color check OR by verifying the right panel updates).
    - Test 3: With a POS selected but no dimensions, the right panel shows empty state "No dimensions yet" + an "Add Dimension" button.
    - Test 4: Tapping "Add Dimension" opens the template picker modal. `find.text('Gender')` and `find.text('Number')` (group headers) are visible. `find.text('Masculine / Feminine')` is visible (one of the template names).
    - Test 5: The template picker has a search field at the top; typing "case" filters the visible templates to only show the Case group.
    - Test 6: Tapping the "Masculine / Feminine" template card inserts a dimension into the selected POS and closes the modal. After a pump, `find.text('Gender')` appears in the dimension editor panel (as the dimension name) with level chips showing "M" and "F".
    - Test 7: The template picker includes a "Custom (blank)" entry in each group (per UI-SPEC).
    - Test 8: Typology limit soft warning: creating a 5th dimension on one POS shows a yellow `SnackBar` or warning banner mentioning ">4 dimensions" — optional if time allows; otherwise assert D-06 no hard limit by creating 5+ dimensions without error.
  </behavior>
  <action>
**Step 1:** Implement `lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart` as a `ConsumerStatefulWidget`. Layout:

```dart
class PosDimensionsPage extends ConsumerStatefulWidget {
  const PosDimensionsPage({super.key});
  @override ConsumerState createState() => _PosDimensionsPageState();
}

class _PosDimensionsPageState extends ConsumerState<PosDimensionsPage> {
  int? _selectedPosId;

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final cs = theme.colorScheme;
    final posAsync = ref.watch(posListProvider);
    final posList = posAsync.asData?.value ?? const [];

    return Row(
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: [
        // Left: POS list panel (260px)
        SizedBox(
          width: 260,
          child: Column(
            children: [
              // Header with "Parts of Speech" label + Add (+) button
              Padding(
                padding: const EdgeInsets.fromLTRB(16, 12, 8, 8),
                child: Row(
                  children: [
                    Expanded(child: Text('Parts of Speech', style: theme.textTheme.titleMedium)),
                    IconButton(
                      icon: const Icon(Icons.add),
                      tooltip: 'Add POS',
                      onPressed: () => _showAddPosDialog(context),
                    ),
                  ],
                ),
              ),
              Expanded(
                child: ListView.builder(
                  itemCount: posList.length,
                  itemBuilder: (ctx, i) {
                    final pos = posList[i];
                    final selected = pos.id == _selectedPosId;
                    return ListTile(
                      title: Text('${pos.name} (${pos.abbreviation})'),
                      selected: selected,
                      selectedTileColor: cs.primaryContainer,
                      onTap: () => setState(() => _selectedPosId = pos.id),
                      trailing: IconButton(
                        icon: Icon(Icons.delete_outline, color: cs.error),
                        tooltip: 'Delete POS',
                        onPressed: () => _confirmDeletePos(pos),
                      ),
                    );
                  },
                ),
              ),
            ],
          ),
        ),
        VerticalDivider(width: 1, color: cs.outlineVariant),
        // Right: Dimension editor
        Expanded(
          child: _selectedPosId == null
              ? _emptyState(theme, cs)
              : DimensionEditorPanel(posId: _selectedPosId!),
        ),
      ],
    );
  }
  // ... _showAddPosDialog, _confirmDeletePos, _emptyState ...
}
```

For `_showAddPosDialog`: reuse logic from `lib/features/morphology/presentation/pos/pos_page.dart` — copy the dialog code into a local function or extract into `lib/features/grammar/presentation/pos_dimensions/pos_crud_dialog.dart` if cleaner. The dialog should accept a POS name + abbreviation and insert via `ref.read(currentDatabaseProvider)!.into(partsOfSpeech).insert(...)`.

For `_confirmDeletePos`: show AlertDialog with copy from UI-SPEC: "This will also delete all its dimensions and remove dimension bindings from inflectional rules. This cannot be undone."

**Step 2:** Implement `lib/features/grammar/presentation/pos_dimensions/dimension_editor_panel.dart`:

```dart
class DimensionEditorPanel extends ConsumerWidget {
  const DimensionEditorPanel({super.key, required this.posId});
  final int posId;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final theme = Theme.of(context);
    final dimsAsync = ref.watch(dimensionsForPosProvider(posId));
    return dimsAsync.when(
      data: (dims) => _buildContent(context, ref, theme, dims),
      loading: () => const Center(child: CircularProgressIndicator()),
      error: (e, _) => Center(child: Text('Error: $e')),
    );
  }

  Widget _buildContent(BuildContext ctx, WidgetRef ref, ThemeData theme, List<Dimension> dims) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Padding(
          padding: const EdgeInsets.all(16),
          child: Row(
            children: [
              Expanded(
                child: Text('Dimensions', style: theme.textTheme.titleMedium),
              ),
              TextButton.icon(
                onPressed: () => _onAddDimension(ctx, ref),
                icon: const Icon(Icons.add),
                label: const Text('Add Dimension'),
              ),
            ],
          ),
        ),
        if (dims.isEmpty)
          Expanded(
            child: Center(
              child: Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  Icon(Icons.layers_outlined, size: 64, color: theme.colorScheme.onSurface.withValues(alpha: 0.3)),
                  const SizedBox(height: 16),
                  Text('No dimensions yet', style: theme.textTheme.titleMedium),
                  const SizedBox(height: 8),
                  Text('Click "Add Dimension" to begin.', style: theme.textTheme.bodyMedium),
                ],
              ),
            ),
          )
        else
          Expanded(
            child: ListView.builder(
              padding: const EdgeInsets.symmetric(horizontal: 16),
              itemCount: dims.length,
              itemBuilder: (ctx, i) => _dimensionCard(ctx, ref, theme, dims[i]),
            ),
          ),
      ],
    );
  }

  Future<void> _onAddDimension(BuildContext ctx, WidgetRef ref) async {
    final template = await showDimensionTemplatePicker(ctx);
    if (template == null) return;
    final dao = ref.read(grammarDaoProvider);
    if (dao == null) return;
    final ordering = await dao.nextDimensionOrdering(posId);
    // Insert the dimension with template's levels (or empty for "Custom (blank)").
    await dao.insertDimension(DimensionsCompanion.insert(
      posId: posId,
      name: template.name,
      ordering: Value(ordering),
      levelsJson: encodeLevelsJson(template.levels),
      templateId: Value(template.id),
    ));
  }

  Widget _dimensionCard(BuildContext ctx, WidgetRef ref, ThemeData theme, Dimension dim) {
    final levels = decodeLevelsJson(dim.levelsJson);
    return Card(
      margin: const EdgeInsets.only(bottom: 12),
      child: Padding(
        padding: const EdgeInsets.all(8),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Row(
              children: [
                Icon(Icons.drag_handle, color: theme.colorScheme.onSurface.withValues(alpha: 0.4)),
                const SizedBox(width: 8),
                Expanded(child: Text(dim.name, style: theme.textTheme.titleMedium)),
                IconButton(
                  icon: const Icon(Icons.delete_outline),
                  onPressed: () => _confirmDeleteDimension(ctx, ref, dim),
                ),
              ],
            ),
            const SizedBox(height: 8),
            Wrap(
              spacing: 8,
              children: levels.map((l) => Chip(
                label: Text('${l.name} (${l.abbr})'),
                onDeleted: () => _removeLevel(ref, dim, l),
              )).toList(),
            ),
            TextButton.icon(
              icon: const Icon(Icons.add, size: 16),
              label: const Text('Add level'),
              onPressed: () => _addLevel(ctx, ref, dim),
            ),
          ],
        ),
      ),
    );
  }

  Future<void> _addLevel(BuildContext ctx, WidgetRef ref, Dimension dim) async {
    // Simple prompt dialog: name + abbreviation TextField → new DimensionLevel with nextLevelId
    // ... implementation left to executor following Phase 2 pos_page.dart dialog conventions ...
  }

  Future<void> _removeLevel(WidgetRef ref, Dimension dim, DimensionLevel level) async {
    final dao = ref.read(grammarDaoProvider);
    if (dao == null) return;
    final updated = decodeLevelsJson(dim.levelsJson).where((l) => l.id != level.id).toList();
    await dao.updateDimensionLevels(dim.id, updated);
  }

  Future<void> _confirmDeleteDimension(BuildContext ctx, WidgetRef ref, Dimension dim) async {
    // AlertDialog with copy from UI-SPEC: "All level bindings on inflectional rules for this dimension will be cleared."
    // On confirm: dao.deleteDimension(dim.id)
    // ... executor to implement ...
  }
}
```

**Step 3:** Implement `lib/features/grammar/presentation/pos_dimensions/dimension_template_picker.dart`:

```dart
import 'package:flutter/material.dart';

import '../../data/dimension_templates.dart';
import '../../domain/dimension_level.dart';

/// Shows the two-step template picker modal (D-05).
/// Returns the selected template, or null if cancelled.
/// A "Custom (blank)" pseudo-template is appended to each group.
Future<DimensionTemplate?> showDimensionTemplatePicker(BuildContext context) {
  return showDialog<DimensionTemplate>(
    context: context,
    builder: (_) => const _DimensionTemplatePickerDialog(),
  );
}

class _DimensionTemplatePickerDialog extends StatefulWidget {
  const _DimensionTemplatePickerDialog();
  @override State createState() => _DimensionTemplatePickerDialogState();
}

class _DimensionTemplatePickerDialogState extends State<_DimensionTemplatePickerDialog> {
  String _search = '';

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final cs = theme.colorScheme;

    final lowered = _search.toLowerCase();
    // Group templates by `group`. Always include one "Custom (blank)" per group.
    final groups = <String, List<DimensionTemplate>>{};
    for (final t in dimensionTemplates) {
      if (lowered.isNotEmpty &&
          !t.name.toLowerCase().contains(lowered) &&
          !t.group.toLowerCase().contains(lowered) &&
          !t.description.toLowerCase().contains(lowered)) {
        continue;
      }
      groups.putIfAbsent(t.group, () => []).add(t);
    }
    // Append "Custom (blank)" to each group that has any entries visible.
    for (final g in groups.keys) {
      groups[g]!.add(DimensionTemplate(
        id: 'custom.${g.toLowerCase()}',
        group: g,
        name: 'Custom',
        levels: const [],
        description: 'Start from scratch — no preset levels.',
      ));
    }

    return Dialog(
      child: ConstrainedBox(
        constraints: const BoxConstraints(maxWidth: 560, maxHeight: 640),
        child: Column(
          mainAxisSize: MainAxisSize.min,
          children: [
            Padding(
              padding: const EdgeInsets.all(16),
              child: Text('Add Dimension', style: theme.textTheme.titleLarge),
            ),
            Padding(
              padding: const EdgeInsets.fromLTRB(16, 0, 16, 8),
              child: TextField(
                onChanged: (v) => setState(() => _search = v),
                decoration: const InputDecoration(
                  hintText: 'Search templates…',
                  prefixIcon: Icon(Icons.search),
                  border: OutlineInputBorder(),
                ),
              ),
            ),
            Expanded(
              child: ListView(
                padding: const EdgeInsets.symmetric(horizontal: 16),
                children: [
                  for (final entry in groups.entries) ...[
                    Padding(
                      padding: const EdgeInsets.only(top: 12, bottom: 4),
                      child: Text(
                        entry.key.toUpperCase(),
                        style: theme.textTheme.labelSmall?.copyWith(
                          letterSpacing: 1.2,
                          color: cs.onSurface.withValues(alpha: 0.5),
                        ),
                      ),
                    ),
                    for (final t in entry.value)
                      _templateCard(t, theme, cs),
                  ],
                ],
              ),
            ),
            Padding(
              padding: const EdgeInsets.all(16),
              child: Row(
                mainAxisAlignment: MainAxisAlignment.end,
                children: [
                  TextButton(onPressed: () => Navigator.of(context).pop(), child: const Text('Cancel')),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }

  Widget _templateCard(DimensionTemplate t, ThemeData theme, ColorScheme cs) {
    return Tooltip(
      message: t.description,
      waitDuration: const Duration(milliseconds: 500),
      child: Card(
        child: InkWell(
          onTap: () => Navigator.of(context).pop(t),
          child: Padding(
            padding: const EdgeInsets.all(8),
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text(t.name, style: theme.textTheme.bodyMedium?.copyWith(fontWeight: FontWeight.w600)),
                if (t.levels.isNotEmpty) ...[
                  const SizedBox(height: 4),
                  Text(
                    t.levels.map((l) => l.abbr).join(' · '),
                    style: theme.textTheme.bodySmall?.copyWith(
                      color: cs.onSurface.withValues(alpha: 0.6),
                    ),
                  ),
                ],
              ],
            ),
          ),
        ),
      ),
    );
  }
}
```

**Step 4:** Write widget test `test/widget/grammar/pos_dimensions_page_test.dart`. Use the ProviderScope + in-memory AppDatabase override pattern. Pump the PosDimensionsPage, seed POS rows via the DAO, then assert the behaviors from the &lt;behavior&gt; list above. For the template picker test, use `showDialog` indirectly by tapping the "Add Dimension" button after selecting a POS.
  </action>
  <verify>
    <automated>flutter test --no-pub test/widget/grammar/pos_dimensions_page_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class PosDimensionsPage' lib/features/grammar/presentation/pos_dimensions/pos_dimensions_page.dart`
    - `grep -q 'class DimensionEditorPanel' lib/features/grammar/presentation/pos_dimensions/dimension_editor_panel.dart`
    - `grep -q 'showDimensionTemplatePicker' lib/features/grammar/presentation/pos_dimensions/dimension_template_picker.dart`
    - `grep -q 'Custom' lib/features/grammar/presentation/pos_dimensions/dimension_template_picker.dart`
    - `grep -q "ref.watch(dimensionsForPosProvider" lib/features/grammar/presentation/pos_dimensions/dimension_editor_panel.dart`
    - `flutter test --no-pub test/widget/grammar/pos_dimensions_page_test.dart` exits 0 with the behaviors from the list passing
    - `flutter analyze --no-fatal-warnings lib/features/grammar/presentation/pos_dimensions/` passes
  </acceptance_criteria>
  <done>Users can manage POS, add dimensions from the template catalog (or blank), edit levels, and delete dimensions. D-23 / D-32 / D-05 / D-06 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend RuleEditorDialog with kind parameter, feature-binding chip picker, and live tiebreak banner. Wire Inflectional Rules sub-tab to reuse it with kind filter.</name>
  <files>
    lib/features/morphology/presentation/rules/rule_editor_dialog.dart,
    lib/features/morphology/presentation/rules/rules_page.dart,
    lib/features/grammar/presentation/inflectional_rules/inflectional_rules_page.dart,
    lib/features/grammar/presentation/shared/migration_banner.dart,
    test/widget/grammar/rule_editor_dialog_kind_test.dart
  </files>
  <read_first>
    - lib/features/morphology/presentation/rules/rule_editor_dialog.dart (the full file — 429+ lines — understand the _OpState / _BranchState / _CondState flow; identify the POS chip filter section to replace with chip picker)
    - lib/features/morphology/presentation/rules/rules_page.dart (existing — study the POS filter + Drift stream consumption; you'll parameterize this with a kind filter)
    - lib/features/grammar/domain/feature_bindings.dart
    - lib/features/grammar/domain/rule_kind.dart
    - lib/features/grammar/domain/tiebreak_detector.dart
    - lib/features/grammar/data/grammar_providers.dart (dimensionsForPosProvider)
    - lib/features/morphology/data/morphology_providers.dart (rulesByKindProvider, inflectionalRulesForPosProvider)
    - .planning/phases/04-grammar-morphology-revised/04-UI-SPEC.md §Rule Editor Dialog, §Tiebreaker Error, §Inflectional Rules sub-tab, §One-time migration banner
    - .planning/phases/04-grammar-morphology-revised/04-CONTEXT.md D-12, D-33, D-40, D-42
  </read_first>
  <behavior>
    - Test 1: `showDialog(RuleEditorDialog(kind: RuleKind.inflectional))` renders no input/output POS dropdowns, and when a POS is selected (via internal state), shows FilterChip rows one per dimension of that POS.
    - Test 2: `showDialog(RuleEditorDialog(kind: RuleKind.derivational))` renders an "Input POS" DropdownButton and an "Output POS" DropdownButton at the top of the dialog. No feature-binding chip rows.
    - Test 3: In inflectional mode with no chips selected, pressing Save shows an inline validation error "Inflectional rules must bind at least one feature." and does NOT insert the rule.
    - Test 4: In inflectional mode with at least one chip selected, Save inserts via `insertRuleWithKind(..., RuleKind.inflectional)` and the featureBindings column contains the selected dim→level map.
    - Test 5: When two rules have identical feature bindings live in the DB, opening the rule editor to edit one shows the tiebreak banner with the other rule's name. (Requires seeding 2 identical-binding rules, then opening editor with the second → banner renders with text containing the first rule's name.)
    - Test 6: `InflectionalRulesPage` renders a rules list filtered to `kind='inflectional'` using `rulesByKindProvider`. Seeding 2 inflectional + 1 derivational rule shows only 2 items.
    - Test 7: The migration banner appears on `InflectionalRulesPage` when `project_settings['ui.migration_v8_banner_dismissed.inflectional']` is not 'true', and is hidden once dismissed.
  </behavior>
  <action>
**Step 1: Edit `rule_editor_dialog.dart`.** This is a non-trivial extension of a 400+ line existing file. Approach:

1. Add imports:
```dart
import '../../../grammar/data/grammar_providers.dart';
import '../../../grammar/domain/dimension_level.dart';
import '../../../grammar/domain/feature_bindings.dart';
import '../../../grammar/domain/inflectional_rule.dart';
import '../../../grammar/domain/rule_kind.dart';
import '../../../grammar/domain/tiebreak_detector.dart';
```

2. Change the constructor:
```dart
class RuleEditorDialog extends ConsumerStatefulWidget {
  const RuleEditorDialog({
    super.key,
    required this.kind,
    this.existing,
  });
  final RuleKind kind;
  final db.MorphologicalRule? existing;
  // ...
}
```

3. Add new state fields in `_RuleEditorDialogState`:
```dart
// Inflectional mode: the selected POS whose dimensions drive the chip rows.
int? _selectedPosIdForChips;
// The currently-bound feature set for inflectional rules. Maps dimensionId → levelId.
final Map<int, int> _featureBindings = {};
// Derivational mode: input/output POS.
int? _inputPosId;
int? _outputPosId;
// Live tiebreak conflict (inflectional only).
TiebreakConflict? _tiebreakConflict;
```

4. In `initState`, if editing an existing rule:
```dart
if (widget.existing != null) {
  final bindings = widget.existing!.featureBindings;
  if (widget.kind == RuleKind.inflectional) {
    _featureBindings.addAll(bindings.dims);
    _selectedPosIdForChips = bindings.pos.isNotEmpty ? bindings.pos.first : null;
  } else {
    _inputPosId = widget.existing!.inputPosId ?? (bindings.pos.isNotEmpty ? bindings.pos.first : null);
    _outputPosId = widget.existing!.outputPosId ?? _inputPosId;
  }
}
```

5. Build the conditional UI sections in `build`. Find the existing POS chip filter section (grep for `posIds` references around line 200-300 of rule_editor_dialog.dart) and replace with:

```dart
if (widget.kind == RuleKind.inflectional) ...[
  // "Applies to" section header
  Text('Applies to', style: theme.textTheme.titleMedium),
  const SizedBox(height: 8),
  // POS dropdown (which POS's dimensions to show chips for)
  _PosDropdown(
    selectedId: _selectedPosIdForChips,
    onChanged: (id) => setState(() {
      _selectedPosIdForChips = id;
      _featureBindings.clear();
      _recomputeTiebreak();
    }),
  ),
  const SizedBox(height: 8),
  // Chip rows — one per dimension of the selected POS
  if (_selectedPosIdForChips != null)
    Consumer(builder: (ctx, ref, _) {
      final dimsAsync = ref.watch(dimensionsForPosProvider(_selectedPosIdForChips!));
      final dims = dimsAsync.asData?.value ?? const [];
      return Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          for (final dim in dims) _buildDimensionChipRow(dim, theme),
        ],
      );
    }),
  // Validation error
  if (_featureBindings.isEmpty) ...[
    const SizedBox(height: 8),
    Text(
      'Inflectional rules must bind at least one feature. Select at least one level chip above.',
      style: theme.textTheme.bodySmall?.copyWith(color: cs.error),
    ),
  ],
  // Tiebreak conflict banner
  if (_tiebreakConflict != null) _buildTiebreakBanner(_tiebreakConflict!, theme, cs),
  const SizedBox(height: 16),
],
if (widget.kind == RuleKind.derivational) ...[
  Row(
    children: [
      Expanded(
        child: _PosDropdown(
          label: 'Input POS',
          selectedId: _inputPosId,
          onChanged: (id) => setState(() {
            _inputPosId = id;
            _outputPosId ??= id; // default output = input (D-38)
          }),
        ),
      ),
      const Padding(
        padding: EdgeInsets.symmetric(horizontal: 8),
        child: Icon(Icons.arrow_forward),
      ),
      Expanded(
        child: _PosDropdown(
          label: 'Output POS',
          selectedId: _outputPosId,
          onChanged: (id) => setState(() => _outputPosId = id),
        ),
      ),
    ],
  ),
  const SizedBox(height: 16),
],
```

Where `_buildDimensionChipRow` renders a Row of `FilterChip` widgets for each level of a dimension:

```dart
Widget _buildDimensionChipRow(Dimension dim, ThemeData theme) {
  final levels = decodeLevelsJson(dim.levelsJson);
  return Padding(
    padding: const EdgeInsets.only(bottom: 8),
    child: Row(
      children: [
        SizedBox(width: 100, child: Text(dim.name, style: theme.textTheme.bodyMedium)),
        Expanded(
          child: Wrap(
            spacing: 6,
            children: levels.map((l) {
              final selected = _featureBindings[dim.id] == l.id;
              return FilterChip(
                label: Text(l.abbr),
                selected: selected,
                onSelected: (sel) {
                  setState(() {
                    if (sel) {
                      _featureBindings[dim.id] = l.id;
                    } else {
                      _featureBindings.remove(dim.id);
                    }
                    _recomputeTiebreak();
                  });
                },
              );
            }).toList(),
          ),
        ),
      ],
    ),
  );
}
```

And `_recomputeTiebreak`:

```dart
void _recomputeTiebreak() {
  if (widget.kind != RuleKind.inflectional || _featureBindings.isEmpty) {
    _tiebreakConflict = null;
    return;
  }
  // Build a synthetic InflectionalRule representing THIS rule (including its current id).
  final selfBindings = FeatureBindings(
    pos: _selectedPosIdForChips == null ? const [] : [_selectedPosIdForChips!],
    dims: Map<int, int>.from(_featureBindings),
  );
  // Fetch all inflectional rules and check if any existing rule has identical bindings.
  final allInflectionalAsync = ref.read(rulesByKindProvider(RuleKind.inflectional));
  final all = allInflectionalAsync.asData?.value ?? const [];
  final others = all.where((r) => r.id != widget.existing?.id).map((r) {
    return InflectionalRule.fromDbRow(r);
  }).toList();
  final selfRule = InflectionalRule(
    id: widget.existing?.id ?? -1,
    name: widget.existing?.name ?? '(this rule)',
    source: '',
    isActive: true,
    bindings: selfBindings,
  );
  final conflicts = findDuplicateSpecificityConflicts([selfRule, ...others]);
  // Find the conflict group that contains self.
  final myConflict = conflicts.where((c) => c.rules.any((r) => r.id == selfRule.id)).firstOrNull;
  _tiebreakConflict = myConflict;
}
```

And `_buildTiebreakBanner`:

```dart
Widget _buildTiebreakBanner(TiebreakConflict conflict, ThemeData theme, ColorScheme cs) {
  final otherNames = conflict.rules
      .where((r) => r.id != widget.existing?.id && r.id != -1)
      .map((r) => r.name)
      .join(', ');
  return Container(
    padding: const EdgeInsets.all(12),
    margin: const EdgeInsets.symmetric(vertical: 8),
    decoration: BoxDecoration(
      color: cs.errorContainer,
      border: Border.all(color: cs.error),
      borderRadius: BorderRadius.circular(4),
    ),
    child: Row(
      children: [
        Icon(Icons.warning_outlined, color: cs.error, size: 16),
        const SizedBox(width: 8),
        Expanded(
          child: Text(
            "Conflict: This rule has the same specificity as '$otherNames' and both match overlapping cells. "
            'Add a distinguishing dimension binding to resolve.',
            style: theme.textTheme.bodySmall?.copyWith(color: cs.onErrorContainer),
          ),
        ),
      ],
    ),
  );
}
```

6. In the Save handler, switch on kind:

```dart
Future<void> _save() async {
  if (widget.kind == RuleKind.inflectional && _featureBindings.isEmpty) {
    setState(() {}); // show the validation error
    return;
  }
  final bindings = FeatureBindings(
    pos: widget.kind == RuleKind.inflectional
        ? (_selectedPosIdForChips == null ? const [] : [_selectedPosIdForChips!])
        : (_inputPosId == null ? const [] : [_inputPosId!]),
    dims: widget.kind == RuleKind.inflectional ? Map<int, int>.from(_featureBindings) : const {},
  );
  final dao = ref.read(morphologyDaoProvider);
  if (dao == null) return;
  final companion = MorphologicalRulesCompanion(
    name: Value(_nameController.text),
    source: Value(_dslController.text),
    isActive: Value(_isActive),
    featureBindings: Value(bindings),
    inputPosId: Value(widget.kind == RuleKind.derivational ? _inputPosId : null),
    outputPosId: Value(widget.kind == RuleKind.derivational ? _outputPosId : null),
  );
  if (widget.existing == null) {
    await dao.insertRuleWithKind(companion, widget.kind);
  } else {
    await dao.updateRule(/* existing row with new field values */);
  }
  if (mounted) Navigator.of(context).pop();
}
```

**Step 2: Parameterize `rules_page.dart`** to accept a kind filter:

```dart
class RulesPage extends ConsumerStatefulWidget {
  const RulesPage({super.key, this.kind});
  /// When non-null, only rules of this kind are shown AND newly-created rules
  /// are created with this kind. When null (legacy), all rules are shown and
  /// created as derivational (backward-compat with pre-Phase-4 callers).
  final RuleKind? kind;
  // ...
}
```

In the state class, use `rulesByKindProvider(widget.kind ?? RuleKind.derivational)` instead of `morphologicalRuleListProvider` when `widget.kind != null`. When opening the editor:

```dart
showDialog(context: context, builder: (_) => RuleEditorDialog(
  kind: widget.kind ?? RuleKind.derivational,
  existing: existingRule,
));
```

**Step 3: Implement `InflectionalRulesPage`** — the wrapper that renders the migration banner + the reused rules page:

```dart
import 'package:flutter/material.dart';

import '../../../grammar/domain/rule_kind.dart';
import '../../../morphology/presentation/rules/rules_page.dart';
import '../shared/migration_banner.dart';

class InflectionalRulesPage extends StatelessWidget {
  const InflectionalRulesPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const MigrationBanner(settingsKey: 'ui.migration_v8_banner_dismissed.inflectional'),
        Expanded(child: const RulesPage(kind: RuleKind.inflectional)),
      ],
    );
  }
}
```

**Step 4: Implement `lib/features/grammar/presentation/shared/migration_banner.dart`:**

```dart
import 'package:drift/drift.dart' as drift;
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../../db/app_database.dart';
import '../../../project/data/project_providers.dart';
// Import the same project_settings stream provider the rest of the codebase uses

class MigrationBanner extends ConsumerWidget {
  const MigrationBanner({super.key, required this.settingsKey});
  final String settingsKey;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final theme = Theme.of(context);
    final cs = theme.colorScheme;

    final settingsAsync = ref.watch(projectSettingsProvider); // adapt to actual name
    final rows = settingsAsync.asData?.value ?? const <ProjectSetting>[];
    final dismissed = rows.any((r) => r.key == settingsKey && r.value == 'true');
    if (dismissed) return const SizedBox.shrink();

    return Container(
      padding: const EdgeInsets.all(12),
      decoration: BoxDecoration(
        color: cs.surfaceContainerHigh,
        border: Border(left: BorderSide(color: cs.primary, width: 3)),
      ),
      child: Row(
        children: [
          Icon(Icons.info_outline, color: cs.primary, size: 16),
          const SizedBox(width: 8),
          Expanded(
            child: Text(
              'Morphology rules have been moved here. Existing rules are now Derivational rules in Lexicon → Derivations. '
              'Create Inflectional rules here to drive paradigm tables.',
              style: theme.textTheme.bodySmall,
            ),
          ),
          IconButton(
            icon: const Icon(Icons.close),
            tooltip: 'Dismiss',
            onPressed: () async {
              final db = ref.read(currentDatabaseProvider);
              if (db == null) return;
              await db.into(db.projectSettings).insertOnConflictUpdate(
                ProjectSettingsCompanion.insert(key: settingsKey, value: 'true'),
              );
            },
          ),
        ],
      ),
    );
  }
}
```

**Step 5: Widget test `test/widget/grammar/rule_editor_dialog_kind_test.dart`** covering tests 1-5 from &lt;behavior&gt;. Use an in-memory AppDatabase via ProviderScope overrides. Test 5 (tiebreak banner) requires seeding 2 rules with identical bindings; it's the most complex — if blocked by Drift companion construction specifics, implement it as a lower-level unit test of `findDuplicateSpecificityConflicts` with synthetic data (which is already covered in Task 1 of 04-03) and mark the UI test as a TODO if time-boxed.
  </action>
  <verify>
    <automated>flutter test --no-pub test/widget/grammar/rule_editor_dialog_kind_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'required this.kind' lib/features/morphology/presentation/rules/rule_editor_dialog.dart`
    - `grep -q 'RuleKind' lib/features/morphology/presentation/rules/rule_editor_dialog.dart`
    - `grep -q 'FilterChip' lib/features/morphology/presentation/rules/rule_editor_dialog.dart`
    - `grep -q 'findDuplicateSpecificityConflicts' lib/features/morphology/presentation/rules/rule_editor_dialog.dart`
    - `grep -q 'insertRuleWithKind' lib/features/morphology/presentation/rules/rule_editor_dialog.dart`
    - `grep -q 'this.kind' lib/features/morphology/presentation/rules/rules_page.dart`
    - `grep -q 'rulesByKindProvider' lib/features/morphology/presentation/rules/rules_page.dart`
    - `grep -q 'class InflectionalRulesPage' lib/features/grammar/presentation/inflectional_rules/inflectional_rules_page.dart`
    - `grep -q 'class MigrationBanner' lib/features/grammar/presentation/shared/migration_banner.dart`
    - `grep -q "ui.migration_v8_banner_dismissed" lib/features/grammar/presentation/shared/migration_banner.dart`
    - `flutter test --no-pub test/widget/grammar/rule_editor_dialog_kind_test.dart` exits 0
    - `flutter analyze --no-fatal-warnings lib/features/morphology/presentation/rules/ lib/features/grammar/presentation/inflectional_rules/ lib/features/grammar/presentation/shared/` passes
  </acceptance_criteria>
  <done>RuleEditorDialog is kind-aware with chip picker for inflectional and POS dropdowns for derivational. Live tiebreak detection renders an inline banner. Inflectional Rules sub-tab reuses the shared rules page with the kind filter, behind a dismissible migration banner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Paradigm viewer page + shared paradigm table widget + cell override dialog + coverage matrix panel + per-cell override DAO</name>
  <files>
    lib/features/grammar/data/paradigm_cell_override_dao.dart,
    lib/features/grammar/data/paradigm_coverage_provider.dart,
    lib/features/grammar/presentation/paradigm_viewer/paradigm_viewer_page.dart,
    lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart,
    lib/features/grammar/presentation/paradigm_viewer/cell_override_dialog.dart,
    lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart,
    lib/features/grammar/presentation/paradigm_viewer/axis_config_bar.dart,
    lib/db/app_database.dart,
    lib/db/app_database.g.dart,
    test/unit/grammar/paradigm_cell_override_test.dart,
    test/widget/grammar/paradigm_table_widget_test.dart
  </files>
  <read_first>
    - lib/db/app_database.dart @DriftDatabase annotation (add ParadigmCellOverrideDao to the daos list)
    - lib/features/grammar/data/grammar_dao.dart (pattern for thin DAO wrapping one table)
    - lib/features/grammar/data/typology_providers.dart (04-03 — computedInflectedParadigmProvider, paradigmAxesProvider, setParadigmAxes)
    - lib/features/grammar/domain/paradigm_cell.dart (sealed ParadigmCell hierarchy)
    - lib/shared/widgets/violation_text.dart (ViolationText widget for D-30)
    - lib/features/phonology/data/romanization_providers.dart (romanizeProvider, deromanizeProvider per D-28 and D-29)
    - .planning/phases/04-grammar-morphology-revised/04-UI-SPEC.md §Paradigm Viewer, §Paradigm Table widget, §Coverage Matrix panel, §Cell Override Dialog
    - .planning/phases/04-grammar-morphology-revised/04-CONTEXT.md D-14, D-15, D-25, D-26, D-27, D-28, D-29, D-30, D-34
  </read_first>
  <behavior>
    - Test 1: `ParadigmCellOverrideDao.upsertOverride(lexemeId: 1, featureSet: {5: 2}, overrideIpa: 'mīs', overrideRomanization: 'mīs')` inserts a new row. A second call with the same (lexemeId, featureSet) updates the existing row rather than creating a duplicate.
    - Test 2: `ParadigmCellOverrideDao.watchOverridesForLexeme(1)` returns a map of featureSetKey → ParadigmCellOverride.
    - Test 3: `ParadigmCellOverrideDao.clearOverride(lexemeId: 1, featureSet: {5: 2})` removes the row.
    - Test 4: `featureSetKey({5: 2, 7: 4})` returns a canonical string `"5:2,7:4"` (dims sorted by id). `featureSetKey({7: 4, 5: 2})` returns the SAME string (order-independent).
    - Test 5 (widget): ParadigmTableWidget given a 2-dim POS (rows × cols) and a paradigm of 4 cells renders a 2×2 grid with column headers, row headers, and one cell per feature combination.
    - Test 6 (widget): A cell with a ParadigmFilled(form:'kata') renders 'kata' on the top line via ViolationText and the IPA on the bottom line (when rom enabled).
    - Test 7 (widget): A cell with ParadigmUncovered renders em-dash `—`.
    - Test 8 (widget): A cell with an override in ParadigmCellOverrides renders amber background + warning icon (at least the Icons.warning_amber_outlined icon is found).
    - Test 9 (widget): Tapping any cell opens the CellOverrideDialog; saving persists a new override row.
    - Test 10: `paradigmCoverageMatrixProvider(posId)` returns `Map<(int dimId, int levelId), bool>` where the value is true iff at least one inflectional rule for posId has a binding matching that (dim, level) pair.
  </behavior>
  <action>
**Step 1: Create `lib/features/grammar/data/paradigm_cell_override_dao.dart`:**

```dart
import 'dart:convert';

import 'package:drift/drift.dart';

import '../../../db/app_database.dart';

part 'paradigm_cell_override_dao.g.dart';

/// Canonical string key for a feature set: "dimId:levelId,dimId:levelId,..."
/// sorted by dimId ascending. Used as the unique key into paradigm_cell_overrides.
String featureSetKey(Map<int, int> featureSet) {
  final entries = featureSet.entries.toList()..sort((a, b) => a.key.compareTo(b.key));
  return entries.map((e) => '${e.key}:${e.value}').join(',');
}

/// Feature set stored as JSON in the DB (matches the shape used elsewhere in Phase 4).
String featureSetToJson(Map<int, int> featureSet) {
  final sorted = <String, int>{};
  final keys = featureSet.keys.toList()..sort();
  for (final k in keys) {
    sorted[k.toString()] = featureSet[k]!;
  }
  return jsonEncode(sorted);
}

Map<int, int> featureSetFromJson(String raw) {
  if (raw.isEmpty) return const {};
  final decoded = jsonDecode(raw);
  if (decoded is! Map) return const {};
  final out = <int, int>{};
  for (final e in decoded.entries) {
    final k = int.tryParse(e.key.toString());
    final v = e.value;
    if (k != null && v is int) out[k] = v;
  }
  return out;
}

@DriftAccessor(tables: [ParadigmCellOverrides])
class ParadigmCellOverrideDao extends DatabaseAccessor<AppDatabase>
    with _$ParadigmCellOverrideDaoMixin {
  ParadigmCellOverrideDao(super.db);

  /// Watches all overrides for a lexeme, keyed by canonical featureSetKey.
  Stream<Map<String, ParadigmCellOverride>> watchOverridesForLexeme(int lexemeId) {
    return (select(paradigmCellOverrides)..where((t) => t.lexemeId.equals(lexemeId)))
        .watch()
        .map((rows) {
      final out = <String, ParadigmCellOverride>{};
      for (final r in rows) {
        final key = featureSetKey(featureSetFromJson(r.featureSetJson));
        out[key] = r;
      }
      return out;
    });
  }

  /// Inserts or updates an override for (lexemeId, featureSet).
  Future<void> upsertOverride({
    required int lexemeId,
    required Map<int, int> featureSet,
    required String overrideIpa,
    String? overrideRomanization,
    String? notes,
  }) async {
    final json = featureSetToJson(featureSet);
    final existing = await (select(paradigmCellOverrides)
          ..where((t) => t.lexemeId.equals(lexemeId) & t.featureSetJson.equals(json)))
        .getSingleOrNull();
    if (existing == null) {
      await into(paradigmCellOverrides).insert(ParadigmCellOverridesCompanion.insert(
        lexemeId: lexemeId,
        featureSetJson: json,
        overrideIpa: overrideIpa,
        overrideRomanization: Value(overrideRomanization),
        notes: Value(notes),
      ));
    } else {
      await (update(paradigmCellOverrides)..where((t) => t.id.equals(existing.id))).write(
        ParadigmCellOverridesCompanion(
          overrideIpa: Value(overrideIpa),
          overrideRomanization: Value(overrideRomanization),
          notes: Value(notes),
        ),
      );
    }
  }

  Future<int> clearOverride({required int lexemeId, required Map<int, int> featureSet}) {
    final json = featureSetToJson(featureSet);
    return (delete(paradigmCellOverrides)
          ..where((t) => t.lexemeId.equals(lexemeId) & t.featureSetJson.equals(json)))
        .go();
  }
}
```

Register in `app_database.dart` `@DriftDatabase(daos:...)` list and add the import. Run `dart run build_runner build --delete-conflicting-outputs`.

**Step 2: Create `lib/features/grammar/data/paradigm_coverage_provider.dart`:**

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../morphology/data/morphology_providers.dart';
import '../domain/rule_kind.dart';
import 'grammar_providers.dart';

/// Key = (dimensionId, levelId). Value = true iff ≥1 active inflectional rule
/// for this POS binds exactly this (dim, level) as part of its feature_bindings.
/// Used by the coverage matrix panel (D-15) and uncovered-cell rendering (D-14).
final paradigmCoverageMatrixProvider =
    Provider.family<Map<(int, int), bool>, int>((ref, posId) {
  final rulesAsync = ref.watch(inflectionalRulesForPosProvider(posId));
  final rules = rulesAsync.asData?.value ?? const [];

  final dimsAsync = ref.watch(dimensionsForPosProvider(posId));
  final dims = dimsAsync.asData?.value ?? const [];

  // Every (dim, level) pair starts false.
  final coverage = <(int, int), bool>{};
  for (final d in dims) {
    // Need to decode levelsJson to get the level ids.
    // Delegate decoding to a helper imported from dimension_level.dart.
    // (Import and call decodeLevelsJson here.)
  }
  // Mark pairs covered by at least one rule's bindings.
  for (final r in rules) {
    for (final e in r.bindings.dims.entries) {
      coverage[(e.key, e.value)] = true;
    }
  }
  return coverage;
});
```

(Add missing import for `decodeLevelsJson` and populate the coverage map with false for every level of every dim.)

**Step 3: Create `lib/features/grammar/presentation/paradigm_viewer/paradigm_viewer_page.dart`** — the Grammar Paradigm Viewer sub-tab:

Layout (top-down):
1. `Row` with POS dropdown + word picker (searchable TextField; scoped to selected POS via posForLexeme match)
2. `AxisConfigBar(posId)` — three dropdowns for rows/cols/tabs
3. `Row` containing:
   - Expanded `ParadigmTableWidget(lexemeId, posId)`
   - VerticalDivider
   - `CoverageMatrixPanel(posId)` (240px fixed width)

The word picker filters lexemes where `posForLexeme(lexeme, posList)?.id == selectedPosId`. When no word selected, pass a synthetic lexemeId = -1 to the table widget and have it render a template using the root `'XYZ-'`.

**Step 4: Create `lib/features/grammar/presentation/paradigm_viewer/axis_config_bar.dart`:**

```dart
class AxisConfigBar extends ConsumerWidget {
  const AxisConfigBar({super.key, required this.posId});
  final int posId;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final dimsAsync = ref.watch(dimensionsForPosProvider(posId));
    final dims = dimsAsync.asData?.value ?? const [];
    final axes = ref.watch(paradigmAxesProvider(posId));
    if (dims.length < 2) return const SizedBox.shrink();

    return Padding(
      padding: const EdgeInsets.all(8),
      child: Row(
        children: [
          const Text('Rows:'),
          const SizedBox(width: 8),
          DropdownButton<int?>(
            value: axes.rows,
            items: dims.map((d) => DropdownMenuItem(value: d.id, child: Text(d.name))).toList(),
            onChanged: (v) async {
              if (v == null) return;
              final newAxes = ParadigmAxes(
                rows: v,
                cols: axes.cols == v ? null : axes.cols,
                tabs: axes.tabs.where((t) => t != v).toList(),
              );
              await setParadigmAxes(ref, posId, newAxes);
            },
          ),
          const SizedBox(width: 16),
          const Text('Columns:'),
          const SizedBox(width: 8),
          DropdownButton<int?>(
            value: axes.cols,
            items: dims.where((d) => d.id != axes.rows).map((d) => DropdownMenuItem(value: d.id, child: Text(d.name))).toList(),
            onChanged: (v) async {
              if (v == null) return;
              final newAxes = ParadigmAxes(
                rows: axes.rows,
                cols: v,
                tabs: axes.tabs.where((t) => t != v).toList(),
              );
              await setParadigmAxes(ref, posId, newAxes);
            },
          ),
        ],
      ),
    );
  }
}
```

**Step 5: Create `lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart`:**

```dart
class ParadigmTableWidget extends ConsumerWidget {
  const ParadigmTableWidget({super.key, required this.lexemeId, required this.posId});
  final int lexemeId;
  final int posId;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final theme = Theme.of(context);
    final cs = theme.colorScheme;

    final paradigm = ref.watch(computedInflectedParadigmProvider(lexemeId));
    final dimsAsync = ref.watch(dimensionsForPosProvider(posId));
    final dims = dimsAsync.asData?.value ?? const [];
    final axes = ref.watch(paradigmAxesProvider(posId));

    final rowDim = dims.firstWhereOrNull((d) => d.id == axes.rows);
    final colDim = dims.firstWhereOrNull((d) => d.id == axes.cols);
    if (rowDim == null || colDim == null) {
      return const Center(child: Text('Configure row and column dimensions above.'));
    }
    final rowLevels = decodeLevelsJson(rowDim.levelsJson);
    final colLevels = decodeLevelsJson(colDim.levelsJson);
    // For now, ignore tabs dimension — compute cells for all-first-level of tabs dim.
    // In a full impl, wrap in TabBar/DropdownButton per D-25.

    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        // Header row
        Row(
          children: [
            const SizedBox(width: 80), // corner
            for (final col in colLevels)
              Container(
                width: 80, height: 32,
                alignment: Alignment.center,
                color: cs.surfaceContainerLow,
                child: Text(col.abbr, style: theme.textTheme.labelSmall?.copyWith(fontWeight: FontWeight.w600)),
              ),
          ],
        ),
        // Body rows
        for (final row in rowLevels)
          Row(
            children: [
              Container(
                width: 80, height: 56,
                alignment: Alignment.center,
                color: cs.surfaceContainerLow,
                child: Text(row.abbr, style: theme.textTheme.labelSmall?.copyWith(fontWeight: FontWeight.w600)),
              ),
              for (final col in colLevels)
                _buildCell(context, ref, paradigm, rowDim, row, colDim, col, theme, cs),
            ],
          ),
      ],
    );
  }

  Widget _buildCell(BuildContext ctx, WidgetRef ref, Map<FeatureSet, ParadigmCell> paradigm,
      Dimension rowDim, DimensionLevel rowLevel, Dimension colDim, DimensionLevel colLevel,
      ThemeData theme, ColorScheme cs) {
    final key = {rowDim.id: rowLevel.id, colDim.id: colLevel.id};
    final cell = paradigm[key];

    return InkWell(
      onTap: () => _openOverrideDialog(ctx, ref, key, cell),
      child: Container(
        width: 80, height: 56, alignment: Alignment.center,
        decoration: BoxDecoration(
          border: Border.all(color: cs.outlineVariant, width: 0.5),
        ),
        child: switch (cell) {
          null || ParadigmUncovered() => Text('—', style: TextStyle(color: cs.onSurface.withValues(alpha: 0.3))),
          ParadigmAmbiguous() => Icon(Icons.error_outline, color: cs.error, size: 16),
          ParadigmFilled(:final form) => _FilledCell(form: form),
        },
      ),
    );
  }

  void _openOverrideDialog(BuildContext ctx, WidgetRef ref, Map<int, int> featureSet, ParadigmCell? cell) {
    showDialog(
      context: ctx,
      builder: (_) => CellOverrideDialog(lexemeId: lexemeId, featureSet: featureSet, currentCell: cell),
    );
  }
}

class _FilledCell extends ConsumerWidget {
  const _FilledCell({required this.form});
  final String form;
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final romanize = ref.watch(romanizeProvider);
    final rom = romanize(form);
    final theme = Theme.of(context);
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Text(rom, style: theme.textTheme.bodyMedium),
        Text('[$form]', style: theme.textTheme.bodySmall?.copyWith(
          color: theme.colorScheme.onSurface.withValues(alpha: 0.6),
        )),
      ],
    );
  }
}
```

**Note:** This is a simplified table — it does NOT yet wrap ViolationText around `rom`. Phase 4 mandate D-30 requires `ViolationText(rom, validateWord(form, inventory, constraints).violations)`. Add that wrapper in the `_FilledCell` using the existing Phase 3 validation path. The full wiring is left to the executor; reference `lib/shared/widgets/violation_text.dart` and the per-word exception toggle from Phase 3 plan 03-06.

Override-cell rendering: read the per-lexeme overrides via `ParadigmCellOverrideDao.watchOverridesForLexeme(lexemeId)`, look up by `featureSetKey(key)`, and if present, render amber background + `Icons.warning_amber_outlined` overlay.

**Step 6: Create `lib/features/grammar/presentation/paradigm_viewer/cell_override_dialog.dart`:**

```dart
class CellOverrideDialog extends ConsumerStatefulWidget {
  const CellOverrideDialog({
    super.key,
    required this.lexemeId,
    required this.featureSet,
    required this.currentCell,
  });
  final int lexemeId;
  final Map<int, int> featureSet;
  final ParadigmCell? currentCell;
  @override
  ConsumerState createState() => _CellOverrideDialogState();
}

class _CellOverrideDialogState extends ConsumerState<CellOverrideDialog> {
  final _romController = TextEditingController();
  final _ipaController = TextEditingController();
  bool _ipaDerivedFromRom = true;

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final cs = theme.colorScheme;
    final deromanize = ref.watch(deromanizeProvider);
    return AlertDialog(
      title: Text('Edit cell'),
      content: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          TextField(
            controller: _romController,
            decoration: const InputDecoration(labelText: 'Romanization'),
            onChanged: (v) {
              if (_ipaDerivedFromRom) {
                _ipaController.text = deromanize(v);
              }
            },
          ),
          TextField(
            controller: _ipaController,
            decoration: const InputDecoration(labelText: 'IPA'),
            onChanged: (_) => _ipaDerivedFromRom = false,
          ),
          // If uncovered, show Create-a-rule button
          if (widget.currentCell is ParadigmUncovered || widget.currentCell == null)
            TextButton(
              onPressed: () {
                Navigator.of(context).pop();
                // Navigate to /grammar/inflectional with prefilled chips — pass featureSet via GoRouter extra
                GoRouter.of(context).go('/grammar/inflectional', extra: widget.featureSet);
              },
              child: const Text('Create a rule for this cell'),
            ),
        ],
      ),
      actions: [
        TextButton(onPressed: () => Navigator.of(context).pop(), child: const Text('Cancel')),
        FilledButton(
          onPressed: () async {
            final dao = ParadigmCellOverrideDao(ref.read(currentDatabaseProvider)!);
            await dao.upsertOverride(
              lexemeId: widget.lexemeId,
              featureSet: widget.featureSet,
              overrideIpa: _ipaController.text,
              overrideRomanization: _romController.text.isEmpty ? null : _romController.text,
            );
            if (mounted) Navigator.of(context).pop();
          },
          child: const Text('Save Override'),
        ),
      ],
    );
  }
}
```

**Step 7: Create `lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart`:**

```dart
class CoverageMatrixPanel extends ConsumerWidget {
  const CoverageMatrixPanel({super.key, required this.posId});
  final int posId;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final theme = Theme.of(context);
    final cs = theme.colorScheme;
    final dimsAsync = ref.watch(dimensionsForPosProvider(posId));
    final dims = dimsAsync.asData?.value ?? const [];
    final coverage = ref.watch(paradigmCoverageMatrixProvider(posId));

    return SizedBox(
      width: 240,
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Padding(
            padding: const EdgeInsets.all(12),
            child: Text('COVERAGE',
                style: theme.textTheme.labelSmall?.copyWith(letterSpacing: 1.2)),
          ),
          Expanded(
            child: ListView(
              children: [
                for (final dim in dims) ...[
                  Padding(
                    padding: const EdgeInsets.fromLTRB(12, 8, 12, 4),
                    child: Text(dim.name, style: theme.textTheme.bodyMedium),
                  ),
                  for (final l in decodeLevelsJson(dim.levelsJson))
                    Padding(
                      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 2),
                      child: Row(
                        children: [
                          Container(
                            width: 8, height: 8,
                            decoration: BoxDecoration(
                              shape: BoxShape.circle,
                              color: (coverage[(dim.id, l.id)] ?? false) ? Colors.green.shade600 : cs.error,
                            ),
                          ),
                          const SizedBox(width: 8),
                          Text(l.abbr, style: theme.textTheme.labelSmall),
                        ],
                      ),
                    ),
                ],
              ],
            ),
          ),
        ],
      ),
    );
  }
}
```

**Step 8: Write `test/unit/grammar/paradigm_cell_override_test.dart`** with tests 1-4 from &lt;behavior&gt; using an in-memory AppDatabase.

**Step 9: Write `test/widget/grammar/paradigm_table_widget_test.dart`** with tests 5-9. Use a ProviderContainer with `computedInflectedParadigmProvider` overridden to return a fixture Map&lt;FeatureSet, ParadigmCell&gt; for deterministic testing, plus `dimensionsForPosProvider(...)` overridden to return fixture dimensions.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs &amp;&amp; flutter test --no-pub test/unit/grammar/paradigm_cell_override_test.dart test/widget/grammar/paradigm_table_widget_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class ParadigmCellOverrideDao' lib/features/grammar/data/paradigm_cell_override_dao.dart`
    - `grep -q 'featureSetKey' lib/features/grammar/data/paradigm_cell_override_dao.dart`
    - `grep -q 'upsertOverride' lib/features/grammar/data/paradigm_cell_override_dao.dart`
    - `grep -q 'paradigmCoverageMatrixProvider' lib/features/grammar/data/paradigm_coverage_provider.dart`
    - `grep -q 'class ParadigmViewerPage' lib/features/grammar/presentation/paradigm_viewer/paradigm_viewer_page.dart`
    - `grep -q 'class ParadigmTableWidget' lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart`
    - `grep -q 'class CellOverrideDialog' lib/features/grammar/presentation/paradigm_viewer/cell_override_dialog.dart`
    - `grep -q 'class CoverageMatrixPanel' lib/features/grammar/presentation/paradigm_viewer/coverage_matrix_panel.dart`
    - `grep -q 'class AxisConfigBar' lib/features/grammar/presentation/paradigm_viewer/axis_config_bar.dart`
    - `grep -q 'ParadigmCellOverrideDao' lib/db/app_database.dart`
    - `grep -q 'ViolationText' lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart`
    - `grep -q 'Icons.warning_amber_outlined' lib/features/grammar/presentation/paradigm_viewer/paradigm_table_widget.dart`
    - `flutter test --no-pub test/unit/grammar/paradigm_cell_override_test.dart` exits 0 with all 4 tests passing
    - `flutter test --no-pub test/widget/grammar/paradigm_table_widget_test.dart` exits 0
    - `flutter analyze --no-fatal-warnings lib/features/grammar/presentation/paradigm_viewer/ lib/features/grammar/data/paradigm_cell_override_dao.dart lib/features/grammar/data/paradigm_coverage_provider.dart` passes
  </acceptance_criteria>
  <done>Paradigm Viewer sub-tab is functional: pick a POS + word, see the computed paradigm with rom/IPA, amber overrides, em-dash for uncovered cells, coverage matrix panel, and click-to-override dialog. Axis config persists to project_settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Typology form page with alignment/word_order/modality dropdowns and auto-save to project_settings</name>
  <files>
    lib/features/grammar/presentation/typology/typology_page.dart,
    test/widget/grammar/typology_page_test.dart
  </files>
  <read_first>
    - lib/features/grammar/data/typology_providers.dart (04-03 — typologySettingsProvider, writeTypologyKey)
    - .planning/phases/04-grammar-morphology-revised/04-UI-SPEC.md §Typology sub-tab and copywriting tooltips
    - .planning/phases/04-grammar-morphology-revised/04-CONTEXT.md D-35
    - lib/features/phonology/presentation/sound_rules/sound_rules_page.dart (for Phase 1 form-style page pattern if applicable)
  </read_first>
  <behavior>
    - Test 1: TypologyPage shows a page title "Typology" and three labeled sections: "Alignment", "Basic Word Order", "Modality Strategy".
    - Test 2: Each section has an info tooltip icon (`Icons.info_outline`).
    - Test 3: Changing the alignment dropdown to "Ergative-Absolutive" triggers a project_settings write with key=`typology.alignment` value=`erg_abs`.
    - Test 4: Changing the word order dropdown to "SOV" writes key=`typology.word_order` value=`SOV`.
    - Test 5: Changing modality to "Analytic" writes key=`typology.modality` value=`analytic`.
    - Test 6: No save button — changes persist on selection change (auto-save).
  </behavior>
  <action>
Implement `lib/features/grammar/presentation/typology/typology_page.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../data/typology_providers.dart';

class TypologyPage extends ConsumerWidget {
  const TypologyPage({super.key});

  static const _alignmentOptions = [
    ('nom_acc', 'Nominative-Accusative'),
    ('erg_abs', 'Ergative-Absolutive'),
    ('split', 'Split'),
  ];
  static const _wordOrderOptions = [
    ('SVO', 'SVO'), ('SOV', 'SOV'), ('VSO', 'VSO'),
    ('VOS', 'VOS'), ('OVS', 'OVS'), ('OSV', 'OSV'),
    ('free', 'Free / Topic-prominent'),
  ];
  static const _modalityOptions = [
    ('synthetic', 'Synthetic'),
    ('analytic', 'Analytic'),
    ('mixed', 'Mixed'),
  ];

  static const _alignmentTooltip =
      'Nominative-accusative: subjects of intransitive and transitive verbs '
      'share marking. Ergative-absolutive: intransitive subjects and direct '
      'objects share marking. Split: varies by tense, aspect, or animacy.';
  static const _wordOrderTooltip =
      'The default order of Subject (S), Verb (V), and Object (O) in '
      'declarative sentences.';
  static const _modalityTooltip =
      'Synthetic: modality expressed by inflectional morphology (e.g. mood '
      'suffixes). Analytic: modality expressed by auxiliary verbs or '
      'particles. Mixed: both strategies used.';

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final theme = Theme.of(context);
    final settings = ref.watch(typologySettingsProvider);

    return SingleChildScrollView(
      padding: const EdgeInsets.all(24),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text('Typology', style: theme.textTheme.headlineSmall),
          const SizedBox(height: 24),
          _section(
            context, ref,
            label: 'Alignment',
            tooltip: _alignmentTooltip,
            current: settings.alignment,
            options: _alignmentOptions,
            onChanged: (v) => writeTypologyKey(ref, 'typology.alignment', v),
          ),
          const SizedBox(height: 24),
          _section(
            context, ref,
            label: 'Basic Word Order',
            tooltip: _wordOrderTooltip,
            current: settings.wordOrder,
            options: _wordOrderOptions,
            onChanged: (v) => writeTypologyKey(ref, 'typology.word_order', v),
          ),
          const SizedBox(height: 24),
          _section(
            context, ref,
            label: 'Modality Strategy',
            tooltip: _modalityTooltip,
            current: settings.modality,
            options: _modalityOptions,
            onChanged: (v) => writeTypologyKey(ref, 'typology.modality', v),
          ),
        ],
      ),
    );
  }

  Widget _section(BuildContext context, WidgetRef ref, {
    required String label,
    required String tooltip,
    required String? current,
    required List<(String, String)> options,
    required Future<void> Function(String) onChanged,
  }) {
    final theme = Theme.of(context);
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Row(
          children: [
            Text(label, style: theme.textTheme.bodyMedium?.copyWith(fontWeight: FontWeight.w600)),
            const SizedBox(width: 8),
            Tooltip(
              message: tooltip,
              waitDuration: const Duration(milliseconds: 500),
              child: Icon(Icons.info_outline, size: 14,
                  color: theme.colorScheme.onSurface.withValues(alpha: 0.5)),
            ),
          ],
        ),
        const SizedBox(height: 8),
        DropdownButtonFormField<String>(
          value: current,
          items: options
              .map((o) => DropdownMenuItem(value: o.$1, child: Text(o.$2)))
              .toList(),
          onChanged: (v) async {
            if (v != null) await onChanged(v);
          },
          decoration: const InputDecoration(border: OutlineInputBorder()),
        ),
      ],
    );
  }
}
```

**Note on `writeTypologyKey` signature from 04-03:** If the function signature was declared as `Future<void> writeTypologyKey(WidgetRef ref, String key, String value)`, pass `ref` directly. Adjust if the 04-03 implementation chose a different shape.

Write `test/widget/grammar/typology_page_test.dart` covering tests 1-6. Use a ProviderScope with an in-memory AppDatabase and assert that after dropdown selection the `project_settings` table contains the expected key/value pair.
  </action>
  <verify>
    <automated>flutter test --no-pub test/widget/grammar/typology_page_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class TypologyPage' lib/features/grammar/presentation/typology/typology_page.dart`
    - `grep -q 'typology.alignment' lib/features/grammar/presentation/typology/typology_page.dart`
    - `grep -q 'typology.word_order' lib/features/grammar/presentation/typology/typology_page.dart`
    - `grep -q 'typology.modality' lib/features/grammar/presentation/typology/typology_page.dart`
    - `grep -q 'Icons.info_outline' lib/features/grammar/presentation/typology/typology_page.dart`
    - `flutter test --no-pub test/widget/grammar/typology_page_test.dart` exits 0
    - `flutter analyze --no-fatal-warnings lib/features/grammar/presentation/typology/` passes
  </acceptance_criteria>
  <done>Typology form ships with all three fields, info tooltips, and auto-save to project_settings. D-35 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-typed DSL in rule editor | Parsed via petitparser (Phase 2); errors surface in preview panel. |
| User-typed romanization in cell override dialog | Auto-derived IPA via deromanizeProvider; both editable. No execution surface. |
| go_router state from deep-linking (`/grammar/...?create=true`) | Phase 3 pattern used for lexicon; Phase 4 uses GoRouter.extra for featureSet — non-URL-exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | Migration banner dismissal bypass (user deletes the settings row) | accept | Banner re-appears; low-stakes UX feature. |
| T-04-15 | Information disclosure | Paradigm viewer showing forms across projects | mitigate | All providers are scoped to `currentDatabaseProvider`; each project DB is isolated (Phase 1 architecture). |
| T-04-16 | Denial of service | Paradigm viewer rebuilding an enormous paradigm on every keystroke | mitigate | `computedInflectedParadigmProvider` is a Riverpod family provider cached per lexemeId. Only rebuilds when rules/inventory/constraints change. |
| T-04-17 | Tampering | User creating two identical-binding rules silently | mitigate | D-12 enforced via live tiebreak banner in rule editor (Task 3) and ParadigmAmbiguous cell rendering (Task 4). |
| T-04-18 | Information disclosure | Override dialog showing stale form from a different word after picker change | mitigate | Dialog constructor receives lexemeId explicitly; no shared static state. |
</threat_model>

<verification>
```bash
flutter test --no-pub test/widget/grammar/ test/unit/grammar/paradigm_cell_override_test.dart
flutter analyze --no-fatal-warnings lib/features/grammar/presentation/ lib/features/morphology/presentation/rules/ lib/router/app_router.dart lib/shared/widgets/app_shell.dart
```

All Phase 4 widget tests in grammar folder must pass; analyzer clean.

**Manual smoke-test (checkpoint candidate, but this plan stays autonomous):** After all tasks complete, open the app, click through each of the 4 Grammar sub-tabs, add a POS + Number dimension, create an inflectional rule bound to `Number=PL`, add a second identical rule and verify the tiebreak banner appears, then visit Paradigm Viewer and see the coverage matrix.
</verification>

<success_criteria>
- Morphology top tab is gone; Grammar tab is live and routes to 4 sub-pages.
- POS & Dimensions master-detail works with template picker + dimension editor.
- Template picker shows ≥20 templates grouped by 9 categories, each with tooltips, plus Custom-from-scratch per group.
- RuleEditorDialog is kind-aware with chip picker (inflectional) and input/output POS dropdowns (derivational), live tiebreak banner, and unbound-rule validation.
- InflectionalRulesPage shows the migration banner + filtered rules list.
- Paradigm Viewer renders rom+IPA stacked cells, amber overrides, em-dash uncovered cells, coverage matrix panel.
- Cell Override Dialog persists to ParadigmCellOverrides keyed by (lexemeId, featureSetKey).
- Axis config persists per-POS to project_settings via setParadigmAxes.
- Typology form auto-saves all three fields to project_settings.
- All widget tests pass; analyzer clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-grammar-morphology-revised/04-04-SUMMARY.md` listing:
- Router surgery: which branches were deleted/added/renumbered
- app_shell tab list changes
- Files created under lib/features/grammar/presentation/ (inventory)
- Rule editor dialog extension points
- ParadigmCellOverrideDao registration
- Widget test pass counts per file
- Any deferred items or executor deviations
</output>
