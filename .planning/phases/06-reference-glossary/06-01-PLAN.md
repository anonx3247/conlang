---
phase: 06-reference-glossary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/glossary.json
  - lib/features/glossary/domain/glossary_entry.dart
  - lib/features/glossary/data/glossary_providers.dart
  - pubspec.yaml
  - test/features/glossary/glossary_entry_test.dart
  - test/features/glossary/glossary_providers_test.dart
autonomous: true
requirements: [REF-01]

must_haves:
  truths:
    - "Glossary JSON asset loads and parses into typed GlossaryEntry list"
    - "Search filter matches on both term name and definition text"
    - "Category filter restricts results to a single category"
    - "150-200 linguistic terms span all five categories"
  artifacts:
    - path: "assets/glossary.json"
      provides: "Bundled linguistic glossary dataset"
      contains: "allophone"
    - path: "lib/features/glossary/domain/glossary_entry.dart"
      provides: "GlossaryEntry data class with fromJson"
      exports: ["GlossaryEntry"]
    - path: "lib/features/glossary/data/glossary_providers.dart"
      provides: "Riverpod providers for glossary loading, filtering, open state"
      exports: ["glossaryProvider", "filteredGlossaryProvider", "glossaryOpenProvider", "glossarySearchProvider", "glossaryCategoryFilterProvider"]
    - path: "test/features/glossary/glossary_entry_test.dart"
      provides: "Unit tests for GlossaryEntry.fromJson"
    - path: "test/features/glossary/glossary_providers_test.dart"
      provides: "Unit tests for filter logic"
  key_links:
    - from: "lib/features/glossary/data/glossary_providers.dart"
      to: "assets/glossary.json"
      via: "rootBundle.loadString"
      pattern: "rootBundle\\.loadString.*glossary\\.json"
    - from: "lib/features/glossary/data/glossary_providers.dart"
      to: "lib/features/glossary/domain/glossary_entry.dart"
      via: "import"
      pattern: "import.*glossary_entry"
---

<objective>
Create the glossary data layer: a bundled JSON asset with 150-200 linguistic terms, a typed GlossaryEntry domain class, and Riverpod providers for loading and real-time filtering by search text and category.

Purpose: Establishes the data foundation that Plan 02's UI will consume. Follows the proven swadesh_list/thesaurus JSON asset pattern.
Output: glossary.json asset, GlossaryEntry class, 5 providers, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reference-glossary/06-CONTEXT.md
@.planning/phases/06-reference-glossary/06-RESEARCH.md

@lib/features/lexicon/data/semantic_providers.dart
@pubspec.yaml

<interfaces>
<!-- Existing patterns the executor needs -->

From lib/features/lexicon/data/semantic_providers.dart:
```dart
// Proven JSON asset loading pattern:
final swadeshListProvider = FutureProvider<List<SwadeshItem>>((ref) async {
  final jsonStr = await rootBundle.loadString('assets/swadesh_list.json');
  final jsonList = json.decode(jsonStr) as List<dynamic>;
  return jsonList.map((e) => SwadeshItem.fromJson(e as Map<String, dynamic>)).toList();
});
```

From pubspec.yaml:
```yaml
flutter:
  assets:
    - assets/ipa_audio/
    - assets/AUDIO_CREDITS.md
    - assets/swadesh_list.json
    - assets/conlangers_thesaurus.json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Glossary JSON asset and domain model</name>
  <files>assets/glossary.json, lib/features/glossary/domain/glossary_entry.dart, pubspec.yaml, test/features/glossary/glossary_entry_test.dart</files>
  <read_first>
    - lib/features/lexicon/data/semantic_providers.dart
    - pubspec.yaml
    - assets/swadesh_list.json (first 20 lines, for JSON structure reference)
  </read_first>
  <behavior>
    - GlossaryEntry.fromJson correctly parses term, category, definition, seeAlso fields
    - GlossaryEntry.fromJson handles missing seeAlso (defaults to empty list)
    - glossary.json is valid JSON array with at least 150 entries
    - Every entry has non-empty term, category, and definition
    - Categories are exactly: "Phonology", "Morphology", "Syntax", "Semantics", "Typology"
  </behavior>
  <action>
    1. Create `lib/features/glossary/domain/glossary_entry.dart` with GlossaryEntry class (per D-01):
       - Fields: `String term`, `String category`, `String definition`, `List<String> seeAlso`
       - Constructor with required term/category/definition, seeAlso defaults to const []
       - `factory GlossaryEntry.fromJson(Map<String, dynamic> json)` parser
       - Pattern: identical to SwadeshItem.fromJson in semantic_providers.dart

    2. Create `test/features/glossary/glossary_entry_test.dart`:
       - Test fromJson with all fields present
       - Test fromJson with missing seeAlso key (should default to [])
       - Test fromJson with empty seeAlso list

    3. Create `assets/glossary.json` with 150-200 linguistics terms (per D-02):
       - JSON array of objects: {"term", "category", "definition", "seeAlso"}
       - Categories per D-10: "Phonology", "Morphology", "Syntax", "Semantics", "Typology"
       - Include cross-references per D-03 (e.g. "allophone" seeAlso: ["phoneme", "phonological rule"])
       - Static definitions only per D-04 — no dynamic data references
       - Cover core terms a conlanger encounters: phoneme, allophone, morpheme, affix, infix, ablaut, umlaut, agglutinative, fusional, isolating, polysynthetic, ergative, accusative, nominative, paradigm, declension, conjugation, etc.
       - Aim for roughly equal distribution across 5 categories (~30-40 each)

    4. Register asset in `pubspec.yaml` — add `- assets/glossary.json` under the flutter.assets list, after `conlangers_thesaurus.json`
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter test test/features/glossary/glossary_entry_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class GlossaryEntry" lib/features/glossary/domain/glossary_entry.dart
    - grep -q "fromJson" lib/features/glossary/domain/glossary_entry.dart
    - grep -q "seeAlso" lib/features/glossary/domain/glossary_entry.dart
    - grep -q "glossary.json" pubspec.yaml
    - python3 -c "import json; d=json.load(open('assets/glossary.json')); assert len(d)>=150, f'Only {len(d)} entries'; cats=set(e['category'] for e in d); assert cats=={'Phonology','Morphology','Syntax','Semantics','Typology'}, f'Bad cats: {cats}'"
  </acceptance_criteria>
  <done>GlossaryEntry class with fromJson, glossary.json with 150+ terms across 5 categories with seeAlso cross-refs, asset registered in pubspec.yaml, unit tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Glossary Riverpod providers with filter logic</name>
  <files>lib/features/glossary/data/glossary_providers.dart, test/features/glossary/glossary_providers_test.dart</files>
  <read_first>
    - lib/features/glossary/domain/glossary_entry.dart
    - lib/features/lexicon/data/semantic_providers.dart
  </read_first>
  <behavior>
    - glossaryProvider loads and parses glossary.json via rootBundle
    - filteredGlossaryProvider returns all entries when search is empty and category is null
    - filteredGlossaryProvider filters by term name (case-insensitive substring match per D-07)
    - filteredGlossaryProvider filters by definition text (case-insensitive substring match per D-07)
    - filteredGlossaryProvider filters by category when glossaryCategoryFilterProvider is non-null (per D-10)
    - filteredGlossaryProvider combines search + category filters (AND logic)
    - glossaryOpenProvider defaults to false
    - glossarySearchProvider defaults to empty string
    - glossaryCategoryFilterProvider defaults to null
  </behavior>
  <action>
    1. Create `lib/features/glossary/data/glossary_providers.dart` with 5 providers:
       - `glossaryProvider` = FutureProvider<List<GlossaryEntry>> — loads `assets/glossary.json` via `rootBundle.loadString` + `json.decode`, maps through `GlossaryEntry.fromJson`. Identical pattern to `swadeshListProvider`.
       - `glossaryOpenProvider` = StateProvider<bool>((ref) => false) — controls drawer visibility (per D-05)
       - `glossarySearchProvider` = StateProvider<String>((ref) => '') — search query text (per D-07)
       - `glossaryCategoryFilterProvider` = StateProvider<String?>((ref) => null) — active category filter (per D-06/D-10)
       - `filteredGlossaryProvider` = Provider<List<GlossaryEntry>> — watches glossaryProvider (.asData?.value ?? []), glossarySearchProvider (lowercased), glossaryCategoryFilterProvider. Returns entries matching both search (term OR definition contains query) AND category (if non-null). Uses `.where()` filter, no debounce needed for 150-200 items.

    2. Create `test/features/glossary/glossary_providers_test.dart`:
       - Test filteredGlossaryProvider search by term name match
       - Test filteredGlossaryProvider search by definition text match
       - Test filteredGlossaryProvider category filter
       - Test filteredGlossaryProvider combined search + category
       - Test empty search + null category returns all
       - Use ProviderContainer with manual overrides for glossaryProvider (override with AsyncValue.data of test entries) to avoid rootBundle dependency in tests
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter test test/features/glossary/glossary_providers_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "glossaryProvider" lib/features/glossary/data/glossary_providers.dart
    - grep -q "filteredGlossaryProvider" lib/features/glossary/data/glossary_providers.dart
    - grep -q "glossaryOpenProvider" lib/features/glossary/data/glossary_providers.dart
    - grep -q "glossarySearchProvider" lib/features/glossary/data/glossary_providers.dart
    - grep -q "glossaryCategoryFilterProvider" lib/features/glossary/data/glossary_providers.dart
    - grep -q "rootBundle.loadString" lib/features/glossary/data/glossary_providers.dart
    - grep -q "asData?.value" lib/features/glossary/data/glossary_providers.dart
  </acceptance_criteria>
  <done>5 Riverpod providers created (glossary loader, open state, search, category filter, filtered list). Provider tests pass with mock data overrides. Real-time filter covers term name, definition text, and category.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static bundled JSON asset, no user input persisted, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I (Info Disclosure) | glossary.json | accept | Public linguistic definitions, no sensitive data |
| T-06-02 | T (Tampering) | assets/glossary.json | accept | Bundled at build time, read-only at runtime via rootBundle |
</threat_model>

<verification>
- `flutter test test/features/glossary/` passes all tests
- `assets/glossary.json` contains 150+ entries with valid structure
- `pubspec.yaml` registers the glossary asset
</verification>

<success_criteria>
- GlossaryEntry.fromJson correctly round-trips all fields
- Provider filter logic tested for search (term + definition), category, and combined
- 150+ glossary terms authored across 5 categories with seeAlso cross-references
</success_criteria>

<output>
After completion, create `.planning/phases/06-reference-glossary/06-01-SUMMARY.md`
</output>
