---
phase: 09-platform-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - macos/Runner/Configs/AppInfo.xcconfig
  - macos/Runner/Info.plist
  - macos/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json
  - macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_16.png
  - macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_32.png
  - macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_64.png
  - macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_128.png
  - macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_256.png
  - macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_512.png
  - macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_1024.png
  - assets/logo.png
autonomous: true
requirements:
  - PLAT-03

must_haves:
  truths:
    - "macOS menu bar shows 'Conlang Workbench' as app name (no underscores)"
    - "App window title reads 'Conlang Workbench'"
    - "macOS app icon uses the custom logo from SVG source"
    - "A logo PNG asset exists at assets/logo.png for use in welcome screen and about dialog"
  artifacts:
    - path: "macos/Runner/Configs/AppInfo.xcconfig"
      provides: "PRODUCT_NAME set to Conlang Workbench"
      contains: "PRODUCT_NAME = Conlang Workbench"
    - path: "macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_1024.png"
      provides: "Custom app icon at highest resolution"
    - path: "assets/logo.png"
      provides: "Logo PNG for in-app use (welcome screen, about dialog)"
  key_links:
    - from: "macos/Runner/Configs/AppInfo.xcconfig"
      to: "macos/Runner/Info.plist"
      via: "$(PRODUCT_NAME) variable reference"
      pattern: "PRODUCT_NAME"
---

<objective>
Set app identity to "Conlang Workbench" everywhere and generate custom app icons from the SVG source.

Purpose: The app currently shows "conlang_workbench" with underscores in macOS menu bar and title bar, and uses the default Flutter icon. This plan fixes both per D-13, D-14, D-15.
Output: Updated macOS config with proper app name, custom app icon at all required sizes, and a logo PNG asset for in-app use.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-platform-polish/09-CONTEXT.md
@macos/Runner/Configs/AppInfo.xcconfig
@macos/Runner/Info.plist
@macos/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update macOS app name and generate icon assets from SVG</name>
  <files>
    macos/Runner/Configs/AppInfo.xcconfig,
    macos/Runner/Info.plist,
    macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_16.png,
    macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_32.png,
    macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_64.png,
    macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_128.png,
    macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_256.png,
    macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_512.png,
    macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_1024.png,
    assets/logo.png
  </files>
  <read_first>
    macos/Runner/Configs/AppInfo.xcconfig,
    macos/Runner/Info.plist,
    macos/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json
  </read_first>
  <action>
    Per D-13: In `macos/Runner/Configs/AppInfo.xcconfig`, change `PRODUCT_NAME = conlang_workbench` to `PRODUCT_NAME = Conlang Workbench`. This propagates to Info.plist via $(PRODUCT_NAME) references, fixing the macOS menu bar app name and window title.

    Per D-14: Convert `~/Downloads/conlang_workbench.svg` to PNG at all required macOS icon sizes. Use `sips` (macOS built-in) or `rsvg-convert` (if available). Generate these files and overwrite existing Flutter default icons:
    - app_icon_16.png (16x16)
    - app_icon_32.png (32x32)
    - app_icon_64.png (64x64)
    - app_icon_128.png (128x128)
    - app_icon_256.png (256x256)
    - app_icon_512.png (512x512)
    - app_icon_1024.png (1024x1024)

    Conversion approach: First render SVG to a high-res PNG (1024px) using `rsvg-convert` (check `which rsvg-convert`). If not available, try `qlmanage -t -s 1024 -o /tmp ~/Downloads/conlang_workbench.svg` or `python3` with cairosvg. Then use `sips -z {size} {size} /tmp/logo_1024.png --out {target}` to resize down for each size.

    Per D-15: Copy the 512px version (or generate a separate clean render) to `assets/logo.png` for use in the welcome screen and about dialog. Add `assets/logo.png` to pubspec.yaml assets list.

    Also in Info.plist, add .conlang file type association for D-06 (preparation for Plan 02):
    ```xml
    <key>CFBundleDocumentTypes</key>
    <array>
      <dict>
        <key>CFBundleTypeName</key>
        <string>Conlang Project</string>
        <key>CFBundleTypeExtensions</key>
        <array>
          <string>conlang</string>
        </array>
        <key>CFBundleTypeRole</key>
        <string>Editor</string>
        <key>LSHandlerRank</key>
        <string>Owner</string>
        <key>LSItemContentTypes</key>
        <array>
          <string>com.conlangworkbench.project</string>
        </array>
      </dict>
    </array>
    <key>UTExportedTypeDeclarations</key>
    <array>
      <dict>
        <key>UTTypeConformsTo</key>
        <array>
          <string>public.data</string>
          <string>public.database</string>
        </array>
        <key>UTTypeDescription</key>
        <string>Conlang Project</string>
        <key>UTTypeIdentifier</key>
        <string>com.conlangworkbench.project</string>
        <key>UTTypeTagSpecification</key>
        <dict>
          <key>public.filename-extension</key>
          <array>
            <string>conlang</string>
          </array>
        </dict>
      </dict>
    </array>
    ```

    This registers .conlang as a known file type so double-clicking opens the app (per CONTEXT.md specifics).
  </action>
  <verify>
    <automated>grep "PRODUCT_NAME = Conlang Workbench" macos/Runner/Configs/AppInfo.xcconfig && test -f macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_1024.png && test -f assets/logo.png && grep "conlang" macos/Runner/Info.plist && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - AppInfo.xcconfig contains `PRODUCT_NAME = Conlang Workbench` (no underscores)
    - All 7 icon PNG files exist in AppIcon.appiconset and are NOT the Flutter default (file size differs from original)
    - assets/logo.png exists
    - Info.plist contains CFBundleDocumentTypes with .conlang extension
    - Info.plist contains UTExportedTypeDeclarations for com.conlangworkbench.project
  </acceptance_criteria>
  <done>macOS app name is "Conlang Workbench" everywhere, custom icon assets generated at all sizes, .conlang file type registered, logo.png available for in-app use</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG file input | Reading user-provided SVG from ~/Downloads — trusted local file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T (Tampering) | Icon assets | accept | Build-time only; no runtime security impact. Icons are static PNGs bundled into app binary |
</threat_model>

<verification>
- `flutter build macos` compiles without errors
- macOS menu bar shows "Conlang Workbench" (not "conlang_workbench")
- App icon in Dock is the custom logo
</verification>

<success_criteria>
- PRODUCT_NAME is "Conlang Workbench" in xcconfig
- All 7 macOS icon sizes generated from SVG source
- assets/logo.png exists for in-app use
- .conlang file type association registered in Info.plist
</success_criteria>

<output>
After completion, create `.planning/phases/09-platform-polish/09-01-SUMMARY.md`
</output>
