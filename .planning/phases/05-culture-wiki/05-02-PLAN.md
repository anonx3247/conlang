---
phase: 05-culture-wiki
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/shared/widgets/app_shell.dart
  - lib/router/app_router.dart
  - lib/features/culture/presentation/culture_shell.dart
  - lib/features/culture/presentation/page_tree_sidebar.dart
  - lib/features/culture/presentation/culture_page_view.dart
  - lib/features/culture/presentation/back_forward_bar.dart
  - pubspec.yaml
  - pubspec.lock
autonomous: true
requirements:
  - CULT-01
must_haves:
  truths:
    - "Culture tab in AppShell is enabled and navigates to the culture wiki"
    - "Culture shell displays a 240px tree sidebar on the left and content area on the right"
    - "Sidebar shows pages in a tree hierarchy with expand/collapse, first 2 levels expanded by default"
    - "User can create, rename, set icon, and delete pages via context menu on tree nodes"
    - "User can drag-and-drop tree nodes to reorder siblings and reparent pages"
    - "Back/forward navigation arrows work at the top of the content area"
    - "Empty state shown when no pages exist with 'New page' CTA"
    - "Selecting a tree node shows the page content in the right pane"
  artifacts:
    - path: "lib/shared/widgets/app_shell.dart"
      provides: "Culture tab enabled"
      contains: "enabled: true"
    - path: "lib/features/culture/presentation/culture_shell.dart"
      provides: "Culture shell with sidebar + content layout"
      exports: ["CultureShell"]
    - path: "lib/features/culture/presentation/page_tree_sidebar.dart"
      provides: "Tree sidebar with DnD and context menu"
      exports: ["PageTreeSidebar"]
    - path: "lib/features/culture/presentation/culture_page_view.dart"
      provides: "Page display with header, timestamps, content"
      exports: ["CulturePageView"]
    - path: "lib/features/culture/presentation/back_forward_bar.dart"
      provides: "Back/forward navigation bar"
      exports: ["BackForwardBar"]
  key_links:
    - from: "lib/router/app_router.dart"
      to: "lib/features/culture/presentation/culture_shell.dart"
      via: "GoRoute builder"
      pattern: "CultureShell"
    - from: "lib/features/culture/presentation/page_tree_sidebar.dart"
      to: "lib/features/culture/data/culture_providers.dart"
      via: "ref.watch(cultureRootPagesProvider)"
      pattern: "cultureRootPagesProvider"
    - from: "lib/features/culture/presentation/culture_shell.dart"
      to: "lib/features/culture/domain/page_history.dart"
      via: "PageHistory instance"
      pattern: "PageHistory"
---

<objective>
Build the Culture Wiki shell UI: enable the Culture tab, wire GoRouter, create the tree sidebar with drag-and-drop and CRUD context menu, page view skeleton, and back/forward navigation.

Purpose: Make the Culture tab functional with page organization, tree navigation, and CRUD operations so users can create and organize wiki pages.
Output: Working Culture shell with tree sidebar, page creation/management, and navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-culture-wiki/05-CONTEXT.md
@.planning/phases/05-culture-wiki/05-RESEARCH.md
@.planning/phases/05-culture-wiki/05-UI-SPEC.md
@.planning/phases/05-culture-wiki/05-01-SUMMARY.md

@lib/shared/widgets/app_shell.dart
@lib/router/app_router.dart
@lib/features/grammar/presentation/grammar_shell.dart
@lib/features/culture/data/culture_dao.dart
@lib/features/culture/data/culture_providers.dart
@lib/features/culture/domain/page_history.dart

<interfaces>
From lib/features/culture/data/culture_providers.dart (created in Plan 01):
```dart
final culturePageListProvider = StreamProvider.autoDispose<List<CulturePage>>(...);
final pageTitleIndexProvider = Provider.autoDispose<Map<String, int>>(...);
final cultureRootPagesProvider = StreamProvider.autoDispose<List<CulturePage>>(...);
final cultureChildPagesProvider = StreamProvider.autoDispose.family<List<CulturePage>, int>(...);
final selectedCulturePageIdProvider = StateProvider<int?>(...);
final culturePageProvider = StreamProvider.autoDispose.family<CulturePage?, int>(...);
```

From lib/features/culture/data/culture_dao.dart (created in Plan 01):
```dart
class CultureDao extends DatabaseAccessor<AppDatabase> {
  Future<int> createPage({required String title, int? parentId, ...});
  Future<void> updatePage(int id, {String? title, String? content, Value<String?>? icon});
  Future<void> deletePage(int id);
  Future<void> reparentPage(int pageId, int? newParentId, int newOrdering);
  Future<void> swapOrdering(int pageIdA, int orderingA, int pageIdB, int orderingB);
  Future<int> maxSiblingOrdering(int? parentId);
}
```

From lib/features/culture/domain/page_history.dart (created in Plan 01):
```dart
class PageHistory extends ChangeNotifier {
  bool get canGoBack;
  bool get canGoForward;
  int? get currentPageId;
  void push(int pageId);
  int? goBack();
  int? goForward();
}
```

From lib/shared/widgets/app_shell.dart line 30:
```dart
_TabItem(label: 'Culture', icon: Icons.language, enabled: false, phase: 'Phase 5'),
```

From lib/router/app_router.dart line 243-250:
```dart
StatefulShellBranch(
  routes: [
    GoRoute(
      path: '/culture',
      builder: (_, _) => const _ComingSoonPage(section: 'Culture'),
    ),
  ],
),
```

From lib/features/grammar/presentation/grammar_shell.dart (shell pattern to mirror):
- Row layout: SizedBox(width: 200) sidebar + VerticalDivider + Expanded content
- Sidebar: Material(color: colorScheme.surfaceContainerLow)
- Section header: Text('GRAMMAR', style: labelSmall, alpha 0.5, letterSpacing 1.2)
- Sidebar tiles: InkWell with selected = primaryContainer bg, 2px primary left border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable Culture tab, wire router, create CultureShell + BackForwardBar</name>
  <files>
    lib/shared/widgets/app_shell.dart,
    lib/router/app_router.dart,
    lib/features/culture/presentation/culture_shell.dart,
    lib/features/culture/presentation/back_forward_bar.dart,
    lib/features/culture/presentation/culture_page_view.dart
  </files>
  <read_first>
    lib/shared/widgets/app_shell.dart,
    lib/router/app_router.dart,
    lib/features/grammar/presentation/grammar_shell.dart,
    lib/features/culture/data/culture_providers.dart,
    lib/features/culture/domain/page_history.dart
  </read_first>
  <action>
    1. In `lib/shared/widgets/app_shell.dart` line 30, change the Culture _TabItem from `enabled: false, phase: 'Phase 5'` to `enabled: true, phase: null`.

    2. In `lib/router/app_router.dart`:
       - Add import: `import '../features/culture/presentation/culture_shell.dart';`
       - Replace the Branch 3 Culture placeholder (lines 243-250) with:
       ```dart
       StatefulShellBranch(
         routes: [
           GoRoute(
             path: '/culture',
             builder: (_, _) => const CultureShell(),
           ),
         ],
       ),
       ```
       - Remove the `_ComingSoonPage` class if it is no longer referenced by any other route. If other branches still use it, leave it.

    3. Create `lib/features/culture/presentation/culture_shell.dart`:
       - ConsumerStatefulWidget (needs PageHistory as state).
       - Layout: `Row` with 240px `SizedBox` sidebar + `VerticalDivider(width: 1, thickness: 1, color: colorScheme.outlineVariant)` + `Expanded` content area.
       - Sidebar: `Material(color: colorScheme.surfaceContainerLow)` with:
         - Section header: `Padding(padding: EdgeInsets.fromLTRB(16, 16, 16, 8))` containing `Text('CULTURE', style: theme.textTheme.labelSmall?.copyWith(color: colorScheme.onSurface.withValues(alpha: 0.5), letterSpacing: 1.2))`
         - `Expanded` child: `PageTreeSidebar` (from Task 2)
         - Bottom: `TextButton.icon(icon: Icon(Icons.add), label: Text('New page'))` that calls `_createRootPage()` — uses `ref.read(projectDatabaseProvider)!.cultureDao.createPage(title: 'Untitled')` then selects the new page.
       - Content area: `Column` containing:
         - `BackForwardBar` (top, 36px)
         - `Expanded` child that reads `selectedCulturePageIdProvider`:
           - If null: empty state per UI-SPEC copywriting (icon `Icons.auto_stories_outlined` 72px alpha 0.15, heading "No pages yet" at headlineSmall alpha 0.4, body "Create your first wiki page to start documenting your conlang's world and culture." at bodyMedium alpha 0.3, "New page" TextButton.icon below)
           - If non-null: `CulturePageView(pageId: selectedId)`
       - Initialize `PageHistory` in `initState`, dispose in `dispose`.
       - On page selection (from sidebar or navigation): call `_pageHistory.push(pageId)` and `ref.read(selectedCulturePageIdProvider.notifier).state = pageId`.

    4. Create `lib/features/culture/presentation/back_forward_bar.dart`:
       - StatelessWidget taking `PageHistory` and `onNavigate(int pageId)` callback.
       - Container height 36px, `colorScheme.surfaceContainer` background.
       - Row of two `IconButton` widgets: `Icons.arrow_back` and `Icons.arrow_forward`, size 20px.
       - Disabled state: `Opacity(opacity: 0.3)` wrapping the icon (matching morphology reorder arrow pattern from Phase 2).
       - Active state: `colorScheme.primary` icon color.
       - Tooltips: "Back" and "Forward".
       - Bottom `Divider(height: 1, thickness: 1, color: colorScheme.outlineVariant)`.
       - `ListenableBuilder` wrapping the bar to rebuild when `PageHistory` notifies.

    5. Create `lib/features/culture/presentation/culture_page_view.dart`:
       - ConsumerWidget taking `pageId` int.
       - Watches `culturePageProvider(pageId)`.
       - Page header row: optional emoji icon (18px Text), page title at 20px weight 600, right-aligned timestamps at 12px alpha 0.4 (`Created: {date}  Modified: {date}`).
       - Content area: `SingleChildScrollView` with 24px horizontal padding, 16px vertical padding.
       - For now (Plan 02), render content as plain `Text` widget showing raw Markdown. Plan 03 replaces this with the block editor + rendered Markdown.
       - Include a pencil icon button in the header to allow title editing via inline `TextField` toggle.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter analyze lib/features/culture/presentation/culture_shell.dart lib/features/culture/presentation/back_forward_bar.dart lib/features/culture/presentation/culture_page_view.dart lib/shared/widgets/app_shell.dart lib/router/app_router.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/shared/widgets/app_shell.dart Culture tab has `enabled: true, phase: null`
    - lib/router/app_router.dart contains `CultureShell()` in the culture branch builder
    - lib/router/app_router.dart imports `culture_shell.dart`
    - lib/features/culture/presentation/culture_shell.dart contains `class CultureShell`
    - lib/features/culture/presentation/culture_shell.dart contains `SizedBox(width: 240`
    - lib/features/culture/presentation/culture_shell.dart contains `Text('CULTURE'`
    - lib/features/culture/presentation/culture_shell.dart contains `PageHistory`
    - lib/features/culture/presentation/culture_shell.dart contains `No pages yet`
    - lib/features/culture/presentation/back_forward_bar.dart contains `class BackForwardBar`
    - lib/features/culture/presentation/back_forward_bar.dart contains `Icons.arrow_back` and `Icons.arrow_forward`
    - lib/features/culture/presentation/culture_page_view.dart contains `class CulturePageView`
    - `flutter analyze` reports no errors on these files
  </acceptance_criteria>
  <done>Culture tab enabled and navigable. CultureShell renders 240px tree sidebar + content area with back/forward bar, empty state, and page view.</done>
</task>

<task type="auto">
  <name>Task 2: Tree sidebar with expand/collapse, drag-and-drop, and context menu CRUD</name>
  <files>
    lib/features/culture/presentation/page_tree_sidebar.dart,
    pubspec.yaml
  </files>
  <read_first>
    lib/features/culture/data/culture_providers.dart,
    lib/features/culture/data/culture_dao.dart,
    lib/features/culture/presentation/culture_shell.dart,
    pubspec.yaml,
    .planning/phases/05-culture-wiki/05-RESEARCH.md,
    .planning/phases/05-culture-wiki/05-UI-SPEC.md
  </read_first>
  <action>
    1. Add `flutter_fancy_tree_view2` dependency:
       ```bash
       flutter pub add flutter_fancy_tree_view2
       ```

    2. Create `lib/features/culture/presentation/page_tree_sidebar.dart`:

       - Define a `CulturePageNode` class that holds `CulturePage` data + `List<CulturePageNode> children` + nullable `CulturePageNode? parent`. This is the in-memory tree model built from flat DB rows.

       - Build tree from flat list: create a `_buildTree(List<CulturePage> allPages)` function that:
         a. Groups pages by parentId
         b. Recursively builds CulturePageNode tree from root pages (parentId == null)
         c. Sorts children by ordering at each level

       - `PageTreeSidebar` is a `ConsumerStatefulWidget` taking callbacks `onPageSelected(int pageId)` and `onCreatePage({int? parentId})`.

       - Watch `culturePageListProvider` to get all pages, build tree with `_buildTree`.

       - Use `TreeController<CulturePageNode>` from `flutter_fancy_tree_view2`:
         - `roots`: root nodes from built tree
         - `childrenProvider`: `(node) => node.children`
         - `parentProvider`: `(node) => node.parent`

       - Per D-02: expand first 2 levels by default. After building tree, call `_treeController.expandAll()` then collapse nodes at depth > 1:
         ```dart
         _treeController.expandAll();
         // Then collapse anything deeper than level 2
         for (final entry in _treeController.roots) {
           _collapseDeep(entry, 0);
         }
         ```
         Where `_collapseDeep` recurses and collapses nodes at depth >= 2.

       - Per D-03: Wrap each node in `TreeDraggable<CulturePageNode>` + `TreeDragTarget<CulturePageNode>`:
         - `onNodeAccepted`: call `ref.read(projectDatabaseProvider)!.cultureDao.reparentPage(draggedNode.data.id, targetNode.data.id, newOrdering)` where newOrdering = maxSiblingOrdering(targetId) + 1.
         - Drag feedback: `Material(elevation: 4, child: _TreeNodeTile(node, isSelected: false))` with slight opacity.

       - Per D-04 (Claude's discretion): right-click context menu via `GestureDetector(onSecondaryTapDown:)` showing a `PopupMenuButton`-style overlay or `showMenu()` with items:
         - "Rename" — shows inline TextField on the node for title editing
         - "Set icon" — shows a small TextField dialog for entering an emoji
         - "Add child page" — calls `cultureDao.createPage(title: 'Untitled', parentId: node.data.id)` then expands the parent node
         - "Delete page" — shows `AlertDialog` per UI-SPEC: title "Delete '{{title}}'?", body "This page and all its child pages will be permanently deleted. This cannot be undone.", Cancel/Delete Page buttons, Delete Page in `colorScheme.error`

       - Tree node tile styling per UI-SPEC:
         - Height 32px, horizontal padding 16px
         - Indent: 16px per depth level (handled by flutter_fancy_tree_view2 `TreeIndentation` widget)
         - Selected: `colorScheme.primaryContainer` background, 2px `colorScheme.primary` left border, `colorScheme.onPrimaryContainer` text
         - Default: 12px label, alpha 0.85
         - Hover: `colorScheme.surfaceContainer` background, drag handle `Icons.drag_indicator` appears at 16px alpha 0.4
         - Emoji/icon prefix: 18px `Text` widget before title
         - Expand/collapse arrow: `Icons.arrow_right` / `Icons.arrow_drop_down` for nodes with children; no arrow for leaf nodes

       - Also show `...` overflow `IconButton(icon: Icons.more_horiz, size: 14)` on hover, which triggers the same context menu as right-click.
  </action>
  <verify>
    <automated>cd /Users/neosapien/dev/conlang && flutter pub get 2>&1 | tail -3 && flutter analyze lib/features/culture/presentation/page_tree_sidebar.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `flutter_fancy_tree_view2:` dependency
    - lib/features/culture/presentation/page_tree_sidebar.dart contains `class PageTreeSidebar`
    - lib/features/culture/presentation/page_tree_sidebar.dart contains `class CulturePageNode`
    - lib/features/culture/presentation/page_tree_sidebar.dart contains `TreeController<CulturePageNode>`
    - lib/features/culture/presentation/page_tree_sidebar.dart contains `TreeDraggable` and `TreeDragTarget`
    - lib/features/culture/presentation/page_tree_sidebar.dart contains `showMenu(` or `PopupMenuButton` for context menu
    - lib/features/culture/presentation/page_tree_sidebar.dart contains `Rename` and `Delete` and `Add child page` and `Set icon` menu item strings
    - lib/features/culture/presentation/page_tree_sidebar.dart contains `reparentPage` call for drag-and-drop
    - `flutter analyze` reports no errors
  </acceptance_criteria>
  <done>Tree sidebar displays hierarchical pages with first 2 levels expanded by default. Drag-and-drop reorders/reparents. Context menu provides Rename, Set icon, Add child page, Delete page. Selected node is visually highlighted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> page title/icon | Page titles and emoji from user dialogs stored in DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T (Tampering) | Page title input | accept | Local-only app, user is sole author |
| T-05-05 | D (Denial of Service) | Deep tree recursion | accept | Wiki pages in practice are rarely deeper than 5-6 levels; no artificial limit needed |
</threat_model>

<verification>
- Culture tab enabled in AppShell (no "Coming in Phase 5" tooltip)
- Navigating to Culture shows 240px sidebar with "CULTURE" header and "New page" button
- Creating a page appears in sidebar tree
- Right-clicking a node shows context menu with 4 items
- Drag-and-drop between nodes reparents correctly
- Back/forward arrows work after navigating between pages
- Empty state shows when no pages exist
</verification>

<success_criteria>
- Culture tab is functional in the app
- Tree sidebar shows page hierarchy with expand/collapse
- First 2 tree levels expanded by default, deeper collapsed
- Drag-and-drop works for reorder and reparent
- Context menu CRUD (rename, set icon, add child, delete) all functional
- Back/forward navigation arrows work
- Empty state displays correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-culture-wiki/05-02-SUMMARY.md`
</output>
