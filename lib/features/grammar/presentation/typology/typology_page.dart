import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

/// Typology form page — Phase 4 plan 04-04.
///
/// Task 1 ships the minimal class declaration so the router compiles and
/// the route smoke test can `find.byType(TypologyPage)`. Task 3 expands
/// this into the full alignment / word-order / modality form.
class TypologyPage extends ConsumerWidget {
  const TypologyPage({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) => const SizedBox.shrink();
}
