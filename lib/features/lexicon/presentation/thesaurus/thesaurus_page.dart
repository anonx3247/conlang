import 'package:flutter/material.dart';

/// Placeholder Thesaurus page — Plan 04 will implement the full UI.
///
/// This stub satisfies the routing requirement (03-01 Task 2) and gives
/// Plan 04 a concrete insertion point for the semantic domain browser.
class ThesaurusPage extends StatelessWidget {
  const ThesaurusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Thesaurus — coming soon'));
  }
}
