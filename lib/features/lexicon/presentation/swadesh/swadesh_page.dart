import 'package:flutter/material.dart';

/// Placeholder Swadesh List page — Plan 03 will implement the full UI.
///
/// This stub satisfies the routing requirement (03-01 Task 2) and gives
/// Plan 03 a concrete insertion point for the interactive Swadesh list.
class SwadeshPage extends StatelessWidget {
  const SwadeshPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Swadesh List — coming soon'));
  }
}
